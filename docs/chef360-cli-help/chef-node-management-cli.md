# `chef-node-management-cli` Local Command Reference

> Generated by `scripts/content/capture-chef360-cli-help.sh` from the locally installed CLI. Regenerate after upgrading Chef 360 CLI packages.

## Captured Version

```text
Git SHA:         3f15962a921f24f1f11282ace427e74cc62de38e
Version:         v1.4.28
Build DateTime:  2026-06-03T14:12:36Z
```

## `chef-node-management-cli`

```text
Chef Node Management CLI

Usage:
  chef-node-management-cli [command]

Available Commands:
  completion          Generate the autocompletion script for the specified shell
  deregister-device   De-register a device
  enrollment          Commands related to enrollment
  get-default-profile Get default profile for request
  help                Help about any command
  list-profile-names  Get the list of profile names available
  management          Commands related to state service
  register-device     register a device and get api-token for device login
  set-default-profile Set default profile for request
  status              Commands related to status
  version             Chef Platform version

Flags:
  -h, --help   help for chef-node-management-cli

Additional help topics:
  chef-node-management-cli enrollment          Commands related to state service

Use "chef-node-management-cli [command] --help" for more information about a command.
```

## `chef-node-management-cli deregister-device`

```text
Use this operation to de-register a device

Usage:
  chef-node-management-cli deregister-device [flags]

Flags:
      --force            force remove profile from local list of profiles, even on error from server
  -f, --format string    to print response in format (default "json")
  -h, --help             help for deregister-device
      --profile string   name for the profile of the new api-token (default "default")
  -v, --verbose          to show debug logs
```

## `chef-node-management-cli enrollment`

```text
Commands related to enrollment

Usage:
  chef-node-management-cli enrollment [command]

Available Commands:
  bulk-enroll            Bulk enrollment of nodes with the node management
  client-node-enrollment Enroll a new node using Client-side enrollment
  enroll-node            Enrol a node with the node management

Flags:
  -h, --help   help for enrollment

Use "chef-node-management-cli enrollment [command] --help" for more information about a command.
```

## `chef-node-management-cli enrollment bulk-enroll`

```text
This operation bulk enrolls the nodes in the node management.

Usage:
  chef-node-management-cli enrollment bulk-enroll [flags]

Flags:
      --file string      Takes path of file.
      --format string    to print response in format (default "json")
  -h, --help             help for bulk-enroll
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli enrollment client-node-enrollment`

```text
### Summary:
  This endpoint allows you to enroll a new node from client side.

  ---
  ### Errors

  #### 401 (Unauthorized)
  Authentication credentials are missing or invalid

  #### 403 (Forbidden)
  The authenticated user does not have permission to enroll nodes

  #### 404 (Not Found)
  The specified cohort could not be found

  #### 409 (Conflict)
  A node with the provided nodeId already exists

  #### 422 (Unprocessable Entity)
  The request contains invalid parameters

  #### 423 (Locked)
  The resource is temporarily locked and cannot be modified

  #### 500 (Internal Server Error)
  An unexpected error occurred during enrollment

Usage:
  chef-node-management-cli enrollment client-node-enrollment [flags]

Examples:
- title: Default example
  example: |
    {
        "cohortId": "2b4a84db-a107-4b2e-9a2f-85eef74686c1",
        "nodeId": "7375ef03-794a-460b-9f7b-47f6faf7400e",
        "source": "enrollment-cli"
    }


Flags:
      --body string             Client-Side Node Enrollment
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for client-node-enrollment
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli enrollment enroll-node`

```text
This operation enrol a node in the node management.

Usage:
  chef-node-management-cli enrollment enroll-node [flags]

Examples:
- title: enroll-linux-node-with-password
  example: |
    {
        "cohortId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "credentialId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "nodeId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "outpost": "test_queue",
        "protocol": "SSH",
        "sshCredentials": {
            "password": "password123",
            "port": 22,
            "username": "ubuntu"
        },
        "url": "testurl.com"
    }
- title: enroll-linux-node-with-privateKey
  example: |
    {
        "cohortId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "credentialId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "nodeId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "outpost": "test_queue",
        "protocol": "SSH",
        "sshCredentials": {
            "key": "[private key omitted]\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQCyv...\n[private key omitted]",
            "port": 22,
            "username": "ec2-user"
        },
        "url": "testurl.com"
    }
- title: enroll-windows-node-with-password
  example: |
    {
        "cohortId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "credentialId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "nodeId": "486753ef-e003-4ced-97c3-b53e1979c181",
        "outpost": "test_queue",
        "protocol": "WinRM",
        "url": "testurl.com",
        "winRMCredentials": {
            "password": "password123",
            "username": "Administrator"
        }
    }


Flags:
      --body string             The node enrollment request
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for enroll-node
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli get-default-profile`

```text
get default profile for request

Usage:
  chef-node-management-cli get-default-profile [flags]

Flags:
  -h, --help   help for get-default-profile
```

## `chef-node-management-cli list-profile-names`

```text
Get the list of profile names available

Usage:
  chef-node-management-cli list-profile-names [flags]

Flags:
  -h, --help   help for list-profile-names
```

## `chef-node-management-cli management`

```text
Commands related to state service

Usage:
  chef-node-management-cli management [command]

Available Commands:
  assembly    Commands related to assembly
  cohort      Commands related to cohort
  filter      Commands related to filter
  list        Commands related to list
  node        Commands related to node
  setting     Commands related to setting
  skill       Commands related to skill

Flags:
  -h, --help   help for management

Use "chef-node-management-cli management [command] --help" for more information about a command.
```

## `chef-node-management-cli management assembly`

```text
Commands related to assembly

Usage:
  chef-node-management-cli management assembly [command]

Available Commands:
  create-assembly     Add a skill assembly.
  delete-assembly     Deletes a skill assembly.
  find-all-assemblies List the skill assemblies under node management.
  find-one-assembly   Get a skill assembly by its id.
  update-assembly     Updates a skill assembly rules.

Flags:
  -h, --help   help for assembly

Use "chef-node-management-cli management assembly [command] --help" for more information about a command.
```

## `chef-node-management-cli management assembly create-assembly`

```text
This operation enables the addition of a new skill assembly to the node management.

Usage:
  chef-node-management-cli management assembly create-assembly [flags]

Examples:
- title: add-multiple-skill-versions
  example: |
    {
        "description": "This is an illustrative skill assembly description.",
        "name": "my-skill-assembly",
        "skills": [
            {
                "action": "add",
                "skill": {
                    "channel": "stable",
                    "name": "chef-gohai",
                    "value": [
                        "1.2.7",
                        "1.3.0"
                    ]
                }
            }
        ]
    }
- title: add-skill-version-remove-older-versions
  example: |
    {
        "description": "This is an illustrative skill assembly description.",
        "name": "my-skill-assembly",
        "skills": [
            {
                "action": "add",
                "skill": {
                    "channel": "stable",
                    "name": "chef-gohai",
                    "value": [
                        "1.2.7"
                    ]
                }
            },
            {
                "action": "remove",
                "skill": {
                    "name": "chef-gohai",
                    "value": [
                        "\u003c 1.0.0"
                    ]
                }
            }
        ]
    }
- title: add-skill-version-remove-specific-version
  example: |
    {
        "description": "This is an illustrative skill assembly description.",
        "name": "my-skill-assembly",
        "skills": [
            {
                "action": "add",
                "skill": {
                    "channel": "stable",
                    "name": "chef-gohai",
                    "value": [
                        "1.2.7"
                    ]
                }
            },
            {
                "action": "remove",
                "skill": {
                    "name": "chef-gohai",
                    "value": [
                        "= 1.2.4"
                    ]
                }
            }
        ]
    }


Flags:
      --body string             The skill assembly to be added
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for create-assembly
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management assembly delete-assembly`

```text
Use this operation to delete a skill assembly using its ID.
Deletion of the skill assembly is not allowed when there are any cohorts associated with the given assembly ID.

Usage:
  chef-node-management-cli management assembly delete-assembly [flags]

Flags:
      --format string            to print response in format (default "json")
  -h, --help                     help for delete-assembly
      --profile string           name of the profile to be used for cmd (default "default")
      --skillAssemblyId string   Id of the skill assembly
      --verbose                  to show debug logs
```

## `chef-node-management-cli management assembly find-all-assemblies`

```text
This operation allows users to search for skill assemblies under node management in the current organization.

Usage:
  chef-node-management-cli management assembly find-all-assemblies [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for find-all-assemblies
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management assembly find-one-assembly`

```text
Use this operation to obtain a skill assembly given its id.

Usage:
  chef-node-management-cli management assembly find-one-assembly [flags]

Flags:
      --format string            to print response in format (default "json")
  -h, --help                     help for find-one-assembly
      --profile string           name of the profile to be used for cmd (default "default")
      --skillAssemblyId string   Id of the skill assembly
      --verbose                  to show debug logs
```

## `chef-node-management-cli management assembly update-assembly`

```text
Use this operation to update skill assembly rules. This will replace the entire rule set with the new one.

Usage:
  chef-node-management-cli management assembly update-assembly [flags]

Examples:
- title: Default example
  example: |
    {
        "description": "This is an updated skill assembly description.",
        "skills": [
            {
                "action": "add",
                "skill": {
                    "name": "chef-infra",
                    "value": [
                        "17.0.1"
                    ]
                }
            }
        ]
    }


Flags:
      --body string              The skill assembly to be added
      --body-file string         Path to file in the local system containing valid body parameter value
      --body-format string       Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                  to show example request body
  -f, --example-format string    Format of the --example, options: json, yaml, toml (default "json")
      --format string            to print response in format (default "json")
  -h, --help                     help for update-assembly
      --profile string           name of the profile to be used for cmd (default "default")
      --skillAssemblyId string   Id of the skill assembly
      --verbose                  to show debug logs
```

## `chef-node-management-cli management cohort`

```text
Commands related to cohort

Usage:
  chef-node-management-cli management cohort [command]

Available Commands:
  assign-setting       Update a node cohort's override settings group.
  assign-skillAssembly Update a node cohort's skill assembly.
  create-cohort        Add a node cohort.
  delete-cohort        Deletes a node cohort.
  find-all-cohorts     List the node cohorts under node management.
  find-one-cohort      Get a node cohort by its id.
  update-cohort        Update a node cohort.

Flags:
  -h, --help   help for cohort

Use "chef-node-management-cli management cohort [command] --help" for more information about a command.
```

## `chef-node-management-cli management cohort assign-setting`

```text
Use this operation to update a node cohort's associated override settings group.

Usage:
  chef-node-management-cli management cohort assign-setting [flags]

Flags:
      --cohortId string    Id of the node cohort
      --format string      to print response in format (default "json")
  -h, --help               help for assign-setting
      --profile string     name of the profile to be used for cmd (default "default")
      --settingId string   Id of the settings group
      --verbose            to show debug logs
```

## `chef-node-management-cli management cohort assign-skillAssembly`

```text
Use this operation to update a node cohort's skill assembly.

Usage:
  chef-node-management-cli management cohort assign-skillAssembly [flags]

Flags:
      --cohortId string          Id of the node cohort
      --format string            to print response in format (default "json")
  -h, --help                     help for assign-skillAssembly
      --profile string           name of the profile to be used for cmd (default "default")
      --skillAssemblyId string   Id of the skill assembly
      --verbose                  to show debug logs
```

## `chef-node-management-cli management cohort create-cohort`

```text
This operation enables the addition of a new node cohort to the node management.

Usage:
  chef-node-management-cli management cohort create-cohort [flags]

Examples:
- title: a.cohort-with-constraints
  example: |
    {
        "allowedPlatforms": "windows,linux",
        "approvalNeeded": false,
        "description": "My default node cohort with enrollment constraints",
        "ipCIDRs": "192.168.0.1/24,192.168.1.1/24",
        "ipRanges": "192.168.0.1-192.168.0.40, 192.168.0.50-192.168.0.90",
        "macAddresses": "00-11-22-33-44-55,00-11-22-33-44-56",
        "name": "my-org-cohort",
        "settingId": "56fe8dfb-9c67-4e47-8003-7452b47baf35",
        "skillAssemblyId": "9df430f9-1c43-41f1-8834-20dad9a131c2"
    }
- title: b.cohort-with-constraints-2
  example: |
    {
        "approvalNeeded": false,
        "description": "My default node cohort with enrollment constraints",
        "ipCIDRs": "192.168.0.1/24",
        "ipRanges": "192.168.0.1",
        "macAddresses": "00-11-22-33-44-55",
        "name": "my-org-cohort",
        "settingId": "56fe8dfb-9c67-4e47-8003-7452b47baf35",
        "skillAssemblyId": "9df430f9-1c43-41f1-8834-20dad9a131c2"
    }
- title: b.cohort-with-constraints-3
  example: |
    {
        "allowedPlatforms": "windows",
        "approvalNeeded": false,
        "description": "My default node cohort with enrollment constraints",
        "ipCIDRs": "2600:1f16:1e5e:8c00::/56",
        "ipRanges": "2001:0db8:85a3:0000:0000:8a2e:0370:7334",
        "macAddresses": "*",
        "name": "my-org-cohort",
        "settingId": "56fe8dfb-9c67-4e47-8003-7452b47baf35",
        "skillAssemblyId": "9df430f9-1c43-41f1-8834-20dad9a131c2"
    }
- title: c.without-enrollment-constraints
  example: |
    {
        "description": "My node cohort",
        "name": "my-org-cohort",
        "settingId": "56fe8dfb-9c67-4e47-8003-7452b47baf35",
        "skillAssemblyId": "9df430f9-1c43-41f1-8834-20dad9a131c2"
    }


Flags:
      --body string             The node cohort to be added
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for create-cohort
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management cohort delete-cohort`

```text
Use this operation to delete a node cohort using its id. The deletion of the node cohort is not allowed when there are any nodes associated with a given cohort.

Usage:
  chef-node-management-cli management cohort delete-cohort [flags]

Flags:
      --cohortId string   Id of the node cohort
      --format string     to print response in format (default "json")
  -h, --help              help for delete-cohort
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-node-management-cli management cohort find-all-cohorts`

```text
This operation allows users to search for node cohorts under node management in the current organization.

Usage:
  chef-node-management-cli management cohort find-all-cohorts [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for find-all-cohorts
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management cohort find-one-cohort`

```text
Use this operation to obtain a node cohort given its id.

Usage:
  chef-node-management-cli management cohort find-one-cohort [flags]

Flags:
      --cohortId string   Id of the node cohort
      --format string     to print response in format (default "json")
  -h, --help              help for find-one-cohort
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-node-management-cli management cohort update-cohort`

```text
Use this operation to update a node cohort using its id.

## Summary
This endpoint allows you to update the properties of an existing node cohort. You can modify:
  - **Cohort's name**
  - **Description**
  - **Approval requirements**
  - **IP ranges**
  - **IP CIDRs**
  - **MAC addresses**
  - **Allowed platforms**~

## Errors

  #### 400 (Bad Request)
  The provided id or request body is invalid.

  #### 404 (Not Found)
  The node cohort with the given id doesn't exist.

  #### 409 (Conflict)
  Another node cohort with the same name already exists.

  #### 401 (Unauthorized)
  The user is not authenticated.

  #### 403 (Forbidden)
  The user does not have permission to update the node cohort.

  #### 500 (Internal Server Error)
  An unexpected error occurred.

Usage:
  chef-node-management-cli management cohort update-cohort [flags]

Examples:
- title: Default example
  example: |
    {
        "allowedPlatforms": "windows,linux",
        "approvalNeeded": false,
        "description": "My awesome node cohort",
        "ipCIDRs": "192.168.0.1/24,192.168.1.1/24",
        "ipRanges": "192.168.0.1-192.168.0.40, 192.168.0.50-192.168.0.90",
        "macAddresses": "00-11-22-33-44-AA,00-11-22-33-44-FF",
        "name": "my-org-cohort"
    }


Flags:
      --body string             The node cohort to be updated
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --cohortId string         Id of the node cohort
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for update-cohort
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management filter`

```text
Commands related to filter

Usage:
  chef-node-management-cli management filter [command]

Available Commands:
  create-filter    Add a node filter.
  delete-filter    Delete a node filter by its id.
  find-all-filters List the node filters under node management.
  find-one-filter  Get a node filter by its id.
  run-adhocFilter  Runs an adhoc filter and returns the matching nodes.
  run-savedFilter  Runs a saved filter and returns the matching nodes.
  update-filter    Update a node filter given its id.

Flags:
  -h, --help   help for filter

Use "chef-node-management-cli management filter [command] --help" for more information about a command.
```

## `chef-node-management-cli management filter create-filter`

```text
This operation enables the addition of a new node filter to node management.
A node filter comprises a set of constraints used to identify matching nodes.

Usage:
  chef-node-management-cli management filter create-filter [flags]

Examples:
- title: linux-nodes-which-are-not-enrolled
  example: |
    {
        "constraints": {
            "attributes": [
                {
                    "name": "kernel_name",
                    "operator": "=",
                    "value": [
                        "Linux"
                    ]
                }
            ],
            "enrollmentLevel": {
                "operator": "!=",
                "value": [
                    "enrolled"
                ]
            }
        },
        "name": "linux-nodes-which-are-enrolled"
    }
- title: linux-production-nodes-with-chef-gohai
  example: |
    {
        "constraints": {
            "attributes": [
                {
                    "name": "kernel_name",
                    "operator": "=",
                    "value": [
                        "Linux"
                    ]
                },
                {
                    "name": "primary_ip",
                    "operator": "MATCHES",
                    "value": [
                        "^172\\.31.*"
                    ]
                }
            ],
            "overrideSettings": {
                "name": [
                    "production"
                ],
                "operator": "="
            },
            "skills": [
                {
                    "name": "chef-gohai",
                    "version": [
                        "\u003e= 1.0.0",
                        "\u003c= 2.0.0"
                    ]
                }
            ]
        },
        "name": "linux-production-nodes-with-chef-gohai"
    }
- title: windows-nodes-which-are-offline
  example: |
    {
        "constraints": {
            "attributes": [
                {
                    "name": "kernel_name",
                    "operator": "=",
                    "value": [
                        "Windows"
                    ]
                }
            ],
            "healthStatus": {
                "operator": "=",
                "value": [
                    "offline"
                ]
            }
        },
        "name": "windows-nodes-which-are-offline"
    }


Flags:
      --body string             The node filter to be added
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for create-filter
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management filter delete-filter`

```text
Use this operation to delete a node filter using its id.

Usage:
  chef-node-management-cli management filter delete-filter [flags]

Flags:
      --filterId string   Id of the node filter
      --format string     to print response in format (default "json")
  -h, --help              help for delete-filter
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-node-management-cli management filter find-all-filters`

```text
This operation allows users to search for node filters under node management in the current organization.

Usage:
  chef-node-management-cli management filter find-all-filters [flags]

Flags:
      --expand                Specifies whether to retrieve partial or full node details. Defaults to true to retrieve full node details. Partial retrieval will not include attributes, installed skills, and required skills details. (default true)
      --format string         to print response in format (default "json")
  -h, --help                  help for find-all-filters
      --keyword string        The search keyword to filter any list.
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management filter find-one-filter`

```text
Use this operation to obtain a node filter given its id.

Usage:
  chef-node-management-cli management filter find-one-filter [flags]

Flags:
      --filterId string   Id of the node filter
      --format string     to print response in format (default "json")
  -h, --help              help for find-one-filter
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-node-management-cli management filter run-adhocFilter`

```text
Use this operation to execute an adhoc filter.
The result of each invocation of this operation could yield a different set of nodes.

Usage:
  chef-node-management-cli management filter run-adhocFilter [flags]

Examples:
- title: filter-windows-offline-nodes
  example: |
    {
        "constraints": {
            "attributes": [
                {
                    "name": "kernel_name",
                    "operator": "=",
                    "value": [
                        "Windows"
                    ]
                }
            ],
            "healthStatus": {
                "operator": "=",
                "value": [
                    "offline"
                ]
            }
        }
    }
- title: linux-nodes-which-are-not-enrolled
  example: |
    {
        "constraints": {
            "attributes": [
                {
                    "name": "kernel_name",
                    "operator": "=",
                    "value": [
                        "Linux"
                    ]
                }
            ],
            "enrollmentLevel": {
                "operator": "!=",
                "value": [
                    "enrolled"
                ]
            }
        }
    }
- title: linux-production-nodes-with-chef-gohai
  example: |
    {
        "constraints": {
            "attributes": [
                {
                    "name": "kernel_name",
                    "operator": "=",
                    "value": [
                        "Linux"
                    ]
                },
                {
                    "name": "primary_ip",
                    "operator": "MATCHES",
                    "value": [
                        "^172\\.31.*"
                    ]
                }
            ],
            "overrideSettings": {
                "name": [
                    "production"
                ],
                "operator": "="
            },
            "skills": [
                {
                    "name": "chef-gohai",
                    "version": [
                        "\u003e= 1.0.0",
                        "\u003c= 2.0.0"
                    ]
                }
            ]
        }
    }


Flags:
      --body string             The filter to run
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --expand                  Specifies whether to retrieve partial or full node details. Defaults to true to retrieve full node details. Partial retrieval will not include attributes, installed skills, and required skills details. (default true)
      --format string           to print response in format (default "json")
  -h, --help                    help for run-adhocFilter
      --pagination.page int     what page of the pagination (default 1)
      --pagination.size int     items per page (default 10)
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management filter run-savedFilter`

```text
Use this operation to execute a saved filter.
The result of each invocation of this operation could yield a different set of nodes.

Usage:
  chef-node-management-cli management filter run-savedFilter [flags]

Flags:
      --expand                Specifies whether to retrieve partial or full node details. Defaults to true to retrieve full node details. Partial retrieval will not include attributes, installed skills, and required skills details. (default true)
      --filterId string       Id of the node filter
      --format string         to print response in format (default "json")
  -h, --help                  help for run-savedFilter
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management filter update-filter`

```text
Use this operation to update a node filter. This will replace the existing filter with the new one.

Usage:
  chef-node-management-cli management filter update-filter [flags]

Examples:
- title: Default example
  example: |
    {
        "constraints": {
            "attributes": [
                {
                    "name": "kernel_name",
                    "operator": "=",
                    "value": [
                        "Windows"
                    ]
                }
            ],
            "skills": [
                {
                    "name": "chef-infra-client"
                }
            ]
        }
    }


Flags:
      --body string             The updated filter
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --filterId string         Id of the node filter
      --format string           to print response in format (default "json")
  -h, --help                    help for update-filter
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management list`

```text
Commands related to list

Usage:
  chef-node-management-cli management list [command]

Available Commands:
  add-nodes      Add a list of node IDs to an existing node list.
  create-list    Add a node list.
  delete-list    Deletes a node list.
  find-all-lists List the node lists under node management.
  find-one-list  Get a node list by its id.
  modify-nodes   Update the node list by adding or removing a set of node IDs. The list of node IDs provided in the request will replace the existing node list, making it the final set of nodes associated with the specified list ID.
  remove-nodes   Remove a list of node ids to an existing node list.

Flags:
  -h, --help   help for list

Use "chef-node-management-cli management list [command] --help" for more information about a command.
```

## `chef-node-management-cli management list add-nodes`

```text
Use this operation to add new node ids to a node list.

Usage:
  chef-node-management-cli management list add-nodes [flags]

Examples:
- title: Default example
  example: |
    [
        "35acd975-9708-4536-9aa6-2d81893070dd",
        "755633be-0dd7-4a88-8bee-91f3742eeb5a"
    ]


Flags:
      --body string             The node ids to add
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for add-nodes
      --listId string           Id of the node list
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management list create-list`

```text
This operation enables the addition of a new node list to the node management.
A node list consists of node IDs managed under node management.

Usage:
  chef-node-management-cli management list create-list [flags]

Examples:
- title: Default example
  example: |
    {
        "name": "my-org-nodes",
        "nodeIds": [
            "54a244ed-61d7-4bf4-b215-414ef590ea31",
            "4b26f30f-eb33-46fe-bc42-a0cdc592cfc3",
            "35acd975-9708-4536-9aa6-2d81893070dd",
            "755633be-0dd7-4a88-8bee-91f3742eeb5a"
        ]
    }


Flags:
      --body string             The node list to be added
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for create-list
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management list delete-list`

```text
Use this operation to delete a node list using its id.

Usage:
  chef-node-management-cli management list delete-list [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for delete-list
      --listId string    Id of the node list
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli management list find-all-lists`

```text
This operation allows users to search for node lists under node management in the current organization.

Usage:
  chef-node-management-cli management list find-all-lists [flags]

Flags:
      --expand                Indicates whether to retrieve complete or partial details of the node list. By default, this is set to true to retrieve the full details. When set to false, only partial information is returned, which includes only the node list ID and name, excluding other details. (default true)
      --format string         to print response in format (default "json")
  -h, --help                  help for find-all-lists
      --keyword string        The search keyword to filter any list.
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management list find-one-list`

```text
Use this operation to obtain a node list given its id.

Usage:
  chef-node-management-cli management list find-one-list [flags]

Flags:
      --expand           Specifies whether to retrieve the list with node IDs or node details.
      --format string    to print response in format (default "json")
  -h, --help             help for find-one-list
      --listId string    Id of the node list
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli management list modify-nodes`

```text
Use this operation to simultaneously add and remove node IDs, ensuring the node list is updated with the specified set of nodes.

Usage:
  chef-node-management-cli management list modify-nodes [flags]

Examples:
- title: Default example
  example: |
    [
        "54a244ed-61d7-4bf4-b215-414ef590ea31",
        "4b26f30f-eb33-46fe-bc42-a0cdc592cfc3"
    ]


Flags:
      --body string             The provided node IDs will be used to define the final set of nodes associated with the specified list ID.
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for modify-nodes
      --listId string           Id of the node list
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management list remove-nodes`

```text
Use this operation to remove new node ids to a node list.

Usage:
  chef-node-management-cli management list remove-nodes [flags]

Examples:
- title: Default example
  example: |
    [
        "54a244ed-61d7-4bf4-b215-414ef590ea31",
        "4b26f30f-eb33-46fe-bc42-a0cdc592cfc3"
    ]


Flags:
      --body string             The node ids to remove
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for remove-nodes
      --listId string           Id of the node list
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management node`

```text
Commands related to node

Usage:
  chef-node-management-cli management node [command]

Available Commands:
  approve-node            Approve a node
  approve-nodes           Approve a list of nodes
  archive-node            Archive a node
  archive-nodes-bulk      Archive multiple nodes
  assign-cohort           Update a node's cohort.
  bulk-node-details       get the details of the nodes sent in the request body
  checkin-node            The subsequent check-in by the node management agent.
  create-node             Register a node with the node management
  delete-attributes       Deletes the attributes with the provided namespace on a node.
  delete-tags             Delete the tags on a node.
  find-all-nodes          List the nodes under node management.
  find-one-node           Get a node using its id.
  find-ou-nodes           Filters a list of node ids to only those nodes in the OU of the request.
  get-archive-status      Get node archive status
  get-archive-status-bulk Get archive status for multiple nodes
  modify-attributes       Modifies the attributes with the provided namespace on a node.
  modify-tags             Modify the tags on a node.
  update-attributes       Updates the attributes with the provided namespace on a node.
  update-tags             Update the tags on a node.

Flags:
  -h, --help   help for node

Use "chef-node-management-cli management node [command] --help" for more information about a command.
```

## `chef-node-management-cli management node approve-node`

```text
Approves the given node Id to be enrolled

 ### Errors

 #### 400 (Bad Request)
 The request was malformed or contains invalid parameters

 #### 401 (Unauthorized)
 User is not authenticated

 #### 403 (Forbidden)
 User does not have permission to approve nodes

 #### 404 (Not Found)
 The node with the specified ID was not found

 #### 409 (Conflict)
 The node is already approved

 #### 423 (Locked)
 The resource is temporarily locked and cannot be modified

 #### 500 (Internal Server Error)
 An unexpected error occurred on the server

Usage:
  chef-node-management-cli management node approve-node [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for approve-node
      --nodeId string    Id of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli management node approve-nodes`

```text
Approves the given list of node ids to be enrolled.

 ### Errors

 #### 400 (Bad Request)
 The request was malformed or contains invalid parameters

 #### 401 (Unauthorized)
 User is not authenticated

 #### 403 (Forbidden)
 User does not have permission to approve nodes

 #### 404 (Not Found)
 The node with the specified ID was not found

 #### 423 (Locked)
 The resource is temporarily locked and cannot be modified

 #### 500 (Internal Server Error)
 An unexpected error occurred on the server

Usage:
  chef-node-management-cli management node approve-nodes [flags]

Flags:
      --body string          Nodes to be approved
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for approve-nodes
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-node-management-cli management node archive-node`

```text
Archives a single node by its ID. Archived nodes are marked as inactive
and can be filtered from normal node listings.

### Errors

#### 400 (Bad Request)
The request was malformed or contains invalid parameters

#### 401 (Unauthorized)
User is not authenticated

#### 403 (Forbidden)
User does not have permission to archive nodes

#### 404 (Not Found)
The node with the specified ID was not found

#### 409 (Conflict)
The node is already archived

#### 500 (Internal Server Error)
An unexpected error occurred on the server

Usage:
  chef-node-management-cli management node archive-node [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for archive-node
      --nodeId string    Id of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli management node archive-nodes-bulk`

```text
Archives a list of nodes by their IDs in a single bulk operation.
Returns individual status for each node ID provided.

### Response Status Codes:
- **207 Multi-Status**: Returns status for each node (all success, partial success, or all failure)

### Errors

#### 400 (Bad Request)
The request was malformed or contains invalid parameters

#### 401 (Unauthorized)
User is not authenticated

#### 403 (Forbidden)
User does not have permission to archive nodes

#### 500 (Internal Server Error)
An unexpected error occurred on the server

Usage:
  chef-node-management-cli management node archive-nodes-bulk [flags]

Flags:
      --body string          List of node IDs to archive
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for archive-nodes-bulk
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-node-management-cli management node assign-cohort`

```text
Use this operation to update a node's cohort.

Usage:
  chef-node-management-cli management node assign-cohort [flags]

Flags:
      --cohortId string   Id of the node cohort
      --format string     to print response in format (default "json")
  -h, --help              help for assign-cohort
      --nodeId string     Id of a node
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-node-management-cli management node bulk-node-details`

```text
This operation gets info of the nodes passed in the request body in the node management.

Usage:
  chef-node-management-cli management node bulk-node-details [flags]

Examples:
- title: Default example
  example: |
    {
        "nodeIds": [
            "54a244ed-61d7-4bf4-b215-414ef590ea31",
            "4b26f30f-eb33-46fe-bc42-a0cdc592cfc3",
            "35acd975-9708-4536-9aa6-2d81893070dd",
            "755633be-0dd7-4a88-8bee-91f3742eeb5a"
        ]
    }


Flags:
      --body string             The node ids for which node info needs to be fetched
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for bulk-node-details
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management node checkin-node`

```text
This operation is intended to be used by the node management agent,
which utilizes it during subsequent check-ins."

Usage:
  chef-node-management-cli management node checkin-node [flags]

Examples:
- title: Default example
  example: |
    {
        "attributes": [
            {
                "name": "kernel_name",
                "value": "Linux"
            },
            {
                "name": "kernel_version",
                "value": "3.10.0-1160"
            },
            {
                "name": "architecture",
                "value": "x86_64"
            },
            {
                "name": "hostname",
                "value": "centos7srv"
            },
            {
                "name": "os_name",
                "value": "CentOS Linux"
            },
            {
                "name": "os_version",
                "value": "7 (Core)"
            },
            {
                "name": "primary_ip",
                "value": "172.31.18.55"
            },
            {
                "name": "netmask",
                "value": "255.255.0.0"
            },
            {
                "name": "time_zone",
                "value": "UTC-0500"
            }
        ],
        "configurationFileData": [
            {
                "configId": 1,
                "settingsId": "56fe8dfb-9c67-4e47-8003-7452b47baf35",
                "settingsVersion": 1,
                "skill": "chef-infra-client"
            }
        ],
        "installedSkills": [
            {
                "canister": {
                    "release": "20200422023028",
                    "version": "15.9.17"
                },
                "name": "chef-infra-client"
            }
        ]
    }


Flags:
      --body string             The node's checkin data
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for checkin-node
      --nodeId string           Id of a node
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management node create-node`

```text
This operation registers a new node in the node management.

Usage:
  chef-node-management-cli management node create-node [flags]

Examples:
- title: linux-node-example
  example: |
    {
        "attributes": [
            {
                "name": "kernel_name",
                "namespace": "agent",
                "value": "Linux"
            },
            {
                "name": "kernel_version",
                "namespace": "agent",
                "value": "3.10.0-1160"
            },
            {
                "name": "architecture",
                "namespace": "agent",
                "value": "x86_64"
            },
            {
                "name": "hostname",
                "namespace": "agent",
                "value": "centos7srv"
            },
            {
                "name": "os_name",
                "namespace": "agent",
                "value": "CentOS Linux"
            },
            {
                "name": "os_version",
                "namespace": "agent",
                "value": "7 (Core)"
            },
            {
                "name": "primary_ip",
                "namespace": "agent",
                "value": "172.31.18.55"
            },
            {
                "name": "netmask",
                "namespace": "agent",
                "value": "255.255.0.0"
            },
            {
                "name": "time_zone",
                "namespace": "agent",
                "value": "UTC-0500"
            }
        ],
        "cohortId": "2b4a84db-a107-4b2e-9a2f-85eef74686c1",
        "id": "56fe8dfb-9c67-4e47-8003-7452b47baf35",
        "installedSkills": [
            {
                "canister": {
                    "release": "20250526142903",
                    "version": "1.4.9"
                },
                "name": "courier-runner"
            }
        ],
        "source": "enrollment"
    }
- title: windows-node-example
  example: |
    {
        "attributes": [
            {
                "name": "kernel_name",
                "namespace": "agent",
                "value": "Windows"
            },
            {
                "name": "kernel_version",
                "namespace": "agent",
                "value": "10.0.19041"
            },
            {
                "name": "architecture",
                "namespace": "agent",
                "value": "x86_64"
            },
            {
                "name": "hostname",
                "namespace": "agent",
                "value": "win10srv"
            },
            {
                "name": "os_name",
                "namespace": "agent",
                "value": "Windows 10"
            },
            {
                "name": "os_version",
                "namespace": "agent",
                "value": "10.0.19041"
            },
            {
                "name": "primary_ip",
                "namespace": "agent",
                "value": "192.168.1.100"
            },
            {
                "name": "netmask",
                "namespace": "agent",
                "value": "255.255.255.0"
            },
            {
                "name": "time_zone",
                "namespace": "agent",
                "value": "UTC+0100"
            }
        ],
        "cohortId": "3c5a84db-a107-4b2e-9a2f-85eef74686c2",
        "id": "78fe8dfb-9c67-4e47-8003-7452b47baf36",
        "installedSkills": [
            {
                "canister": {
                    "release": "20250526144028",
                    "version": "1.2.7"
                },
                "name": "chef-gohai"
            }
        ],
        "source": "discovery"
    }


Flags:
      --body string             The node to be added
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for create-node
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management node delete-attributes`

```text
Use this operation to delete attributes within a namespace.
Attribute names can be provided through query parameters.
If names are provided, only those attributes will be deleted.
Otherwise, the entire namespace will be deleted.

Usage:
  chef-node-management-cli management node delete-attributes [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for delete-attributes
      --name strings       The name of the property (attribute or tag)
      --namespace string   An attribute's namespace
      --nodeId string      Id of a node
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-node-management-cli management node delete-tags`

```text
Use this operation to delete the tags.
Tag names can be provided through query parameters.
If names are provided, only those tags will be deleted.
Otherwise, the entire tags will be deleted.

Usage:
  chef-node-management-cli management node delete-tags [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for delete-tags
      --name strings     The name of the property (attribute or tag)
      --nodeId string    Id of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli management node find-all-nodes`

```text
This operation will allow users to search for nodes under node management
in the current organization.

Usage:
  chef-node-management-cli management node find-all-nodes [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for find-all-nodes
      --isArchived            Filter nodes by archive status
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management node find-one-node`

```text
Use this operation to retrieve data for a single node in the node management.

Usage:
  chef-node-management-cli management node find-one-node [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for find-one-node
      --nodeId string    Id of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli management node find-ou-nodes`

```text
Use this operation to provide a list of node ids from potentially mixed OUs
and receive a response of lists filtered to the OU of the request.

Usage:
  chef-node-management-cli management node find-ou-nodes [flags]

Examples:
- title: Default example
  example: |
    {
        "nodeIds": [
            "54a244ed-61d7-4bf4-b215-414ef590ea31",
            "4b26f30f-eb33-46fe-bc42-a0cdc592cfc3",
            "35acd975-9708-4536-9aa6-2d81893070dd",
            "755633be-0dd7-4a88-8bee-91f3742eeb5a"
        ]
    }


Flags:
      --body string             The list of node ids to filter
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for find-ou-nodes
      --pagination.page int     what page of the pagination (default 1)
      --pagination.size int     items per page (default 10)
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management node get-archive-status`

```text
Retrieves the archive status for a single node by its ID.
Returns whether the node is currently archived or not.

### Errors

#### 400 (Bad Request)
Invalid node ID provided

#### 401 (Unauthorized)
User is not authenticated

#### 403 (Forbidden)
User does not have permission to view node status

#### 404 (Not Found)
The node with the specified ID was not found

#### 500 (Internal Server Error)
An unexpected error occurred on the server

Usage:
  chef-node-management-cli management node get-archive-status [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-archive-status
      --nodeId string    Id of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli management node get-archive-status-bulk`

```text
Retrieves the archive status for multiple nodes by their IDs.
Returns an array of objects containing node ID and archive status for each node.

### Errors

#### 400 (Bad Request)
The request was malformed or contains invalid parameters

#### 401 (Unauthorized)
User is not authenticated

#### 403 (Forbidden)
User does not have permission to view node status

#### 500 (Internal Server Error)
An unexpected error occurred on the server

Usage:
  chef-node-management-cli management node get-archive-status-bulk [flags]

Flags:
      --body string          List of node IDs to check archive status
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for get-archive-status-bulk
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-node-management-cli management node modify-attributes`

```text
Use this operation to modify a node's namespaced attributes.
This operation replaces the existing attribute value with the
new one and adds any missing attributes.

Usage:
  chef-node-management-cli management node modify-attributes [flags]

Flags:
      --body string          The modified attributes
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for modify-attributes
      --namespace string     An attribute's namespace
      --nodeId string        Id of a node
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-node-management-cli management node modify-tags`

```text
Use this operation to modify a node's tags.
This operation replaces the existing tag value with the new one and adds any missing tags.

Usage:
  chef-node-management-cli management node modify-tags [flags]

Flags:
      --body string          The modified tags
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for modify-tags
      --nodeId string        Id of a node
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-node-management-cli management node update-attributes`

```text
Use this operation to update a node's namespaced attributes.
If any attributes exist for the given namespace, this operation
completely replaces the old attributes with the new ones.

Usage:
  chef-node-management-cli management node update-attributes [flags]

Flags:
      --body string          The updated attributes
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-attributes
      --namespace string     An attribute's namespace
      --nodeId string        Id of a node
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-node-management-cli management node update-tags`

```text
Use this operation to update a node's tags.
If any tags exist, this operation completely replaces the old tags with the new ones.

Usage:
  chef-node-management-cli management node update-tags [flags]

Flags:
      --body string          The updated tags
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-tags
      --nodeId string        Id of a node
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-node-management-cli management setting`

```text
Commands related to setting

Usage:
  chef-node-management-cli management setting [command]

Available Commands:
  create-setting            Add a node override settings
  delete-setting            Deletes a override settings group
  find-all-default-settings Get the list of default settings for all the available skills.
  find-all-settings         Get the list of override settings under node management.
  find-one-setting          Get override settings by id
  get-skill-defaultSettings Get default settings for a skill
  get-skillSettings         Get merged global and override settings by id and skill
  update-defaultSettings    Update default settings for a skill
  update-skillSettings      Create or update Override Settings for a Skill

Flags:
  -h, --help   help for setting

Use "chef-node-management-cli management setting [command] --help" for more information about a command.
```

## `chef-node-management-cli management setting create-setting`

```text
This operation will enable the addition of a node override settings based on skill groups.

Usage:
  chef-node-management-cli management setting create-setting [flags]

Examples:
- title: Default example
  example: |
    {
        "description": "My override settings",
        "name": "dev-node-settings",
        "skills": [
            {
                "settings": [
                    {
                        "name": "log_level",
                        "value": "debug"
                    }
                ],
                "skillName": "chef-gohai"
            },
            {
                "settings": [
                    {
                        "name": "log_format",
                        "value": "json"
                    },
                    {
                        "name": "log_level",
                        "value": "info"
                    }
                ],
                "skillName": "courier-runner"
            }
        ]
    }


Flags:
      --body string             The override settings to be added
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for create-setting
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management setting delete-setting`

```text
Use this operation to delete a override settings group using its id.

Usage:
  chef-node-management-cli management setting delete-setting [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for delete-setting
      --profile string     name of the profile to be used for cmd (default "default")
      --settingId string   Id of the settings group
      --verbose            to show debug logs
```

## `chef-node-management-cli management setting find-all-default-settings`

```text
Use this operation to obtain all the default settings for all the skills.

Usage:
  chef-node-management-cli management setting find-all-default-settings [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for find-all-default-settings
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management setting find-all-settings`

```text
Use this operation to obtain all the override settings for a settings group.

Usage:
  chef-node-management-cli management setting find-all-settings [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for find-all-settings
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management setting find-one-setting`

```text
Use this operation to obtain override settings for a settings group given its id.

Usage:
  chef-node-management-cli management setting find-one-setting [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for find-one-setting
      --profile string     name of the profile to be used for cmd (default "default")
      --settingId string   Id of the settings group
      --verbose            to show debug logs
```

## `chef-node-management-cli management setting get-skill-defaultSettings`

```text
Use this operation to obtain default settings for a skill.

Usage:
  chef-node-management-cli management setting get-skill-defaultSettings [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for get-skill-defaultSettings
      --profile string     name of the profile to be used for cmd (default "default")
      --skillName string   name of Skill
      --verbose            to show debug logs
```

## `chef-node-management-cli management setting get-skillSettings`

```text
Use this operation to get merged global and override settings for a skill.

Usage:
  chef-node-management-cli management setting get-skillSettings [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for get-skillSettings
      --profile string     name of the profile to be used for cmd (default "default")
      --settingId string   Id of the settings group
      --skillName string   name of Skill
      --verbose            to show debug logs
```

## `chef-node-management-cli management setting update-defaultSettings`

```text
Use this operation to update default settings for a skill.

Usage:
  chef-node-management-cli management setting update-defaultSettings [flags]

Flags:
      --body string          The updated skill override settings
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-defaultSettings
      --profile string       name of the profile to be used for cmd (default "default")
      --skillName string     name of Skill
      --verbose              to show debug logs
```

## `chef-node-management-cli management setting update-skillSettings`

```text
Use this operation to update override settings for a skill.

Usage:
  chef-node-management-cli management setting update-skillSettings [flags]

Flags:
      --body string          The updated skill override settings
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-skillSettings
      --profile string       name of the profile to be used for cmd (default "default")
      --settingId string     Id of the settings group
      --skillName string     name of Skill
      --verbose              to show debug logs
```

## `chef-node-management-cli management skill`

```text
Commands related to skill

Usage:
  chef-node-management-cli management skill [command]

Available Commands:
  create-skill    Add a skill definition
  delete-skill    Deletes a skill definition
  find-all-skills List the skill definitions under node management
  find-one-skill  Get skill definition
  modify-skill    Modify a skill definition
  update-agent    Register/Update node management agent skill along with the default settings.

Flags:
  -h, --help   help for skill

Use "chef-node-management-cli management skill [command] --help" for more information about a command.
```

## `chef-node-management-cli management skill create-skill`

```text
This method will allow the addition of a new skill definition to the node management.

Usage:
  chef-node-management-cli management skill create-skill [flags]

Examples:
- title: chef-gohai-example
  example: |
    {
        "canister": {
            "name": "chef-gohai",
            "origin": "chef-platform",
            "service": true
        },
        "configurationTemplates": [
            {
                "content": "",
                "fileName": "user.toml",
                "filePath": "/hab/user/chef-gohai/config",
                "name": "default",
                "windowsFilePath": "c:\\hab\\user\\chef-gohai\\config"
            }
        ],
        "name": "chef-gohai"
    }
- title: chef-infra-client-example
  example: |
    {
        "canister": {
            "name": "chef-infra-client",
            "origin": "chef"
        },
        "configurationTemplates": [
            {
                "content": "",
                "fileName": "config.rb",
                "filePath": "/etc/chef",
                "windowsFilePath": "c:/chef"
            }
        ],
        "name": "chef-infra-client",
        "native": {
            "versionCommand": "chef-client --version"
        }
    }


Flags:
      --body string             The skill definition to be added
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for create-skill
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli management skill delete-skill`

```text
Use this operation to delete a skill definition using its name.

Usage:
  chef-node-management-cli management skill delete-skill [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for delete-skill
      --profile string     name of the profile to be used for cmd (default "default")
      --skillName string   name of Skill
      --verbose            to show debug logs
```

## `chef-node-management-cli management skill find-all-skills`

```text
This method will list all skill definitions related to management in the current organization.

Usage:
  chef-node-management-cli management skill find-all-skills [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for find-all-skills
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli management skill find-one-skill`

```text
Use this operation to obtain a skill definition using the skill name (it is case-insensitive).

Usage:
  chef-node-management-cli management skill find-one-skill [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for find-one-skill
      --profile string     name of the profile to be used for cmd (default "default")
      --skillName string   name of Skill
      --verbose            to show debug logs
```

## `chef-node-management-cli management skill modify-skill`

```text
Use this operation to modify a skill definition using its name.
Put requests can modify canister, native definitions, as well as configuration templates.

Usage:
  chef-node-management-cli management skill modify-skill [flags]

Examples:
- title: Default example
  example: |
    {
        "canister": {
            "name": "chef-infra-client",
            "origin": "chef"
        },
        "configurationTemplates": [
            {
                "content": "",
                "fileName": "config.rb",
                "filePath": "/etc/chef",
                "windowsFilePath": "c:/chef"
            }
        ],
        "native": {
            "versionCommand": "chef-client --version"
        }
    }


Flags:
      --body string             The updated skill definition
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for modify-skill
      --profile string          name of the profile to be used for cmd (default "default")
      --skillName string        name of Skill
      --verbose                 to show debug logs
```

## `chef-node-management-cli management skill update-agent`

```text
The node management agent is a specialized skill; use this operation to update the node management agent's skill.
This operation registers the node management agent as a special skill, enabling the updating of the node management agent.
It also establishes the default settings for the node management agent to function during skill installation,
as well as for self-updating.

Usage:
  chef-node-management-cli management skill update-agent [flags]

Examples:
- title: a.default
  example: |
    {
        "bldrAuthToken": "string",
        "bldrChannel": "stable",
        "bldrUrl": "https://bldr.habitat.sh/",
        "isLogRetention": true,
        "logLevel": "error",
        "maxSize": "100MB",
        "nodeCheckinInterval": 3600,
        "retentionDays": 7,
        "updateSkillMetadataInterval": 3600
    }
- title: b.with-custom-bldr
  example: |
    {
        "bldrAuthToken": "string",
        "bldrChannel": "stable",
        "bldrUrl": "https://mybldr.domain.co/",
        "isLogRetention": true,
        "logLevel": "debug",
        "maxSize": "100MB",
        "nodeCheckinInterval": 3600,
        "retentionDays": 7,
        "updateSkillMetadataInterval": 3600
    }


Flags:
      --body string             The data for node management agent skill
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for update-agent
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli register-device`

```text
register a device and get api-token for device login

Usage:
  chef-node-management-cli register-device [flags]

Flags:
      --cafile string         path to ca file
      --device-name string    name of the device (default "device")
  -f, --format string         to print response in format (default "json")
  -h, --help                  help for register-device
  -i, --insecure              insecure skip verify
      --overwrite             this flag is used to overwrite the old profile if the profile for profile name provided already exists
      --profile-name string   name for the profile of the new api-token
      --url string            tenant url
  -v, --verbose               to show debug logs
```

## `chef-node-management-cli set-default-profile`

```text
Set default profile for request

Usage:
  chef-node-management-cli set-default-profile [flags]

Flags:
  -h, --help   help for set-default-profile
```

## `chef-node-management-cli status`

```text
Commands related to status

Usage:
  chef-node-management-cli status [command]

Available Commands:
  get-all-enrollmentIds   Get all node enrollment ids
  get-enrollmentId-status Get node(s) enrollment status using enrollment id
  get-status              Get node enrollment status
  multi-nodes-status      Multi Node Enrollment status
  update-status           update the node enrollment status

Flags:
  -h, --help   help for status

Use "chef-node-management-cli status [command] --help" for more information about a command.
```

## `chef-node-management-cli status get-all-enrollmentIds`

```text
Use this operation to obtain all the node enrollment ids

Usage:
  chef-node-management-cli status get-all-enrollmentIds [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for get-all-enrollmentIds
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli status get-enrollmentId-status`

```text
Use this operation to obtain the node(s) enrollment status using enrollment id

Usage:
  chef-node-management-cli status get-enrollmentId-status [flags]

Flags:
      --enrollmentId string   Enrollment Id of node/nodes
      --format string         to print response in format (default "json")
  -h, --help                  help for get-enrollmentId-status
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-node-management-cli status get-status`

```text
Use this operation to obtain a node enrollment status

Usage:
  chef-node-management-cli status get-status [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-status
      --nodeId string    Id of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-node-management-cli status multi-nodes-status`

```text
This operation retrieves the status of multiple nodes in bulk.

Usage:
  chef-node-management-cli status multi-nodes-status [flags]

Examples:
- title: Default example
  example: |
    {
        "nodeIds": [
            "56fe8dfb-9c67-4e47-8003-7452b47baf35",
            "486753ef-e003-4ced-97c3-b53e1979c181"
        ]
    }


Flags:
      --body string             The multi Node status request
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for multi-nodes-status
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-node-management-cli status update-status`

```text
Use this operation to update the node enrollment state and status.

Usage:
  chef-node-management-cli status update-status [flags]

Flags:
      --body string          The updated node enrollment status definition
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-status
      --nodeId string        Id of a node
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-node-management-cli version`

```text
Chef Platform version

Usage:
  chef-node-management-cli version [flags]

Flags:
  -h, --help   help for version
```
