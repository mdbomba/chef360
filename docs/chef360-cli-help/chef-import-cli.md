# `chef-import-cli` Local Command Reference

> Generated by `scripts/content/capture-chef360-cli-help.sh` from the locally installed CLI. Regenerate after upgrading Chef 360 CLI packages.

## Captured Version

```text
Git SHA:         88457ab2512083e05149aae3f587a2768a52cc49
Version:         v0.0.18
Build DateTime:  2026-06-04T08:08:55Z
```

## `chef-import-cli`

```text
Chef Utility - Import Service API

Usage:
  chef-import-cli [command]

Available Commands:
  completion          Generate the autocompletion script for the specified shell
  deregister-device   De-register a device
  get-default-profile Get default profile for request
  help                Help about any command
  importservice       Commands related to importservice
  list-profile-names  Get the list of profile names available
  register-device     register a device and get api-token for device login
  set-default-profile Set default profile for request
  version             Chef Import CLI version

Flags:
  -h, --help   help for chef-import-cli

Use "chef-import-cli [command] --help" for more information about a command.
```

## `chef-import-cli deregister-device`

```text
Use this operation to de-register a device

Usage:
  chef-import-cli deregister-device [flags]

Flags:
      --force            force remove profile from local list of profiles, even on error from server
  -f, --format string    to print response in format (default "json")
  -h, --help             help for deregister-device
      --profile string   name for the profile of the new api-token (default "default")
  -v, --verbose          to show debug logs
```

## `chef-import-cli get-default-profile`

```text
get default profile for request

Usage:
  chef-import-cli get-default-profile [flags]

Flags:
  -h, --help   help for get-default-profile
```

## `chef-import-cli importservice`

```text
Commands related to importservice

Usage:
  chef-import-cli importservice [command]

Available Commands:
  complete-import-process     Complete import process
  create-import-process       Initiate data import process
  generate-sha256             Generate SHA256 checksum for a backup file
  get-import-process-status   Get import process status
  update-import-process-stage Update import process stage

Flags:
  -h, --help   help for importservice

Use "chef-import-cli importservice [command] --help" for more information about a command.
```

## `chef-import-cli importservice complete-import-process`

```text
### Summary
Marks an import process as complete with either a success or failure result.
This endpoint should be called after all import stages have been processed.

#### Note
When the result is 'failure', the errors array should contain details about
what went wrong during the import process.

### Errors
#### 400 (Bad Request)
The request payload is malformed or missing required fields

#### 401 (Unauthorized)
Authentication credentials are missing or invalid

#### 403 (Forbidden)
The user does not have permission to complete import processes

#### 404 (Not Found)
The specified import process does not exist

#### 422 (Unprocessable Content)
The request payload does not meet validation requirements (e.g., invalid result value)

Usage:
  chef-import-cli importservice complete-import-process [flags]

Examples:
- title: failure
  summary: Failed import completion
  example: |
    {
        "errors": [
            "Failed to restore organization: invalid org structure",
            "User data validation failed: missing required fields"
        ],
        "result": "failure"
    }
- title: success
  summary: Successful import completion
  example: |
    {
        "errors": [],
        "result": "success"
    }


Flags:
      --body string
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for complete-import-process
      --importId string         The unique identifier for the import process. This is returned when initiating an import
                                via the POST /process/{importType} endpoint.

      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-import-cli importservice create-import-process`

```text
### Summary
Initiates a data import process for the specified import type. Returns a signed upload URL
that can be used to upload the import tarball to the platform's storage.

#### Note
The upload URL is time-limited based on the 'durationSeconds' parameter. If the URL expires
before the upload is complete, you can request a new URL by providing the same 'filename' parameter.

### Errors
#### 400 (Bad Request)
The request payload is malformed or missing required fields

#### 401 (Unauthorized)
Authentication credentials are missing or invalid

#### 403 (Forbidden)
The user does not have permission to perform import operations

#### 422 (Unprocessable Content)
The request payload does not meet validation requirements (e.g., invalid sha256sum format)

Usage:
  chef-import-cli importservice create-import-process [flags]

Examples:
- title: Default example
  example: |
    {
        "durationSeconds": 3600,
        "filename": "dsm-backup-2024-12-17.tar.gz",
        "sha256sum": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
    }


Flags:
      --body string
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for create-import-process
      --importType string       The type of import to process. This determines how the uploaded data will be handled by the platform.

                                Supported import types:
                                - 'dsm-import': Declarative State Management import for migrating DSM data into Chef 360

      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-import-cli importservice generate-sha256`

```text
Generate SHA256 checksum for a .tar.gz backup file.
This command reads the file in chunks to avoid loading the entire file into memory.
The generated checksum can be used with the create-import-process command.

Example:
  chef-import-cli importservice generate-sha256 --file /path/to/backup.tar.gz

Usage:
  chef-import-cli importservice generate-sha256 [flags]

Flags:
      --file string     Path to the .tar.gz file (required)
      --format string   Output format: text, json, or yaml (default "text")
  -h, --help            help for generate-sha256
```

## `chef-import-cli importservice get-import-process-status`

```text
### Summary
Retrieves the current status of an import process by its unique identifier.

#### Note
Use this endpoint to monitor the progress of an import operation after initiating it
via the POST /process/{importType} endpoint.

### Errors
#### 401 (Unauthorized)
Authentication credentials are missing or invalid

#### 403 (Forbidden)
The user does not have permission to view import process status

#### 404 (Not Found)
The specified import process does not exist

Usage:
  chef-import-cli importservice get-import-process-status [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-import-process-status
      --importId string   The unique identifier for the import process. This is returned when initiating an import
                          via the POST /process/{importType} endpoint.

      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-import-cli importservice update-import-process-stage`

```text
### Summary
Updates the current stage of an import process. This endpoint is used to track
the progress of an import operation through its various stages.

#### Note
The stage should be updated sequentially as the import process progresses through
each phase of the import workflow.

### Errors
#### 400 (Bad Request)
The request payload is malformed or missing required fields

#### 401 (Unauthorized)
Authentication credentials are missing or invalid

#### 403 (Forbidden)
The user does not have permission to update import process stage

#### 404 (Not Found)
The specified import process does not exist

#### 422 (Unprocessable Content)
The request payload does not meet validation requirements (e.g., invalid stage value)

Usage:
  chef-import-cli importservice update-import-process-stage [flags]

Examples:
- title: Default example
  example: |
    {
        "stage": "extracting"
    }


Flags:
      --body string
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for update-import-process-stage
      --importId string         The unique identifier for the import process. This is returned when initiating an import
                                via the POST /process/{importType} endpoint.

      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-import-cli list-profile-names`

```text
Get the list of profile names available

Usage:
  chef-import-cli list-profile-names [flags]

Flags:
  -h, --help   help for list-profile-names
```

## `chef-import-cli register-device`

```text
register a device and get api-token for device login

Usage:
  chef-import-cli register-device [flags]

Flags:
      --cafile string         path to ca file
      --device-name string    name of the device (default "device")
  -f, --format string         to print response in format (default "json")
  -h, --help                  help for register-device
  -i, --insecure              insecure skip verify
      --overwrite             this flag is used to overwrite the old profile if the profile for profile name provided already exists
      --profile-name string   name for the profile of the new api-token
      --url string            tenant url
  -v, --verbose               to show debug logs
```

## `chef-import-cli set-default-profile`

```text
Set default profile for request

Usage:
  chef-import-cli set-default-profile [flags]

Flags:
  -h, --help   help for set-default-profile
```

## `chef-import-cli version`

```text
Chef Import CLI version

Usage:
  chef-import-cli version [flags]

Flags:
  -h, --help   help for version
```
