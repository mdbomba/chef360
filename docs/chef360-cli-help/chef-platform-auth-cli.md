# `chef-platform-auth-cli` Local Command Reference

> Generated by `scripts/content/capture-chef360-cli-help.sh` from the locally installed CLI. Regenerate after upgrading Chef 360 CLI packages.

## Captured Version

```text
Git SHA:         f87971dd06685be6e088431059cab30c0727d566
Version:         v1.4.32
Build DateTime:  2026-06-04T06:52:12Z
```

## `chef-platform-auth-cli`

```text
Chef Platform auth CLI is use to get  authenticate

Usage:
  chef-platform-auth-cli [command]

Available Commands:
  authz               Commands for node account
  completion          Generate the autocompletion script for the specified shell
  deregister-device   De-register a device
  get-default-profile Get default profile for request
  help                Help about any command
  keygen              Generate Ed25519 private/public key pairs for signing
  license-management  Commands for license management
  license-usage       Commands for license usage
  list-profile-names  Get the list of profile names available
  log-service         Commands for log service
  node-account        Commands for node account
  notification        Commands for notifications
  register-device     register a device and get api-token for device login
  set-default-profile Set default profile for request
  system              Commands for managing system
  user-account        Commands for user account
  version             Chef Platform version

Flags:
  -h, --help   help for chef-platform-auth-cli

Use "chef-platform-auth-cli [command] --help" for more information about a command.
```

## `chef-platform-auth-cli authz`

```text
Commands for managing node account

Usage:
  chef-platform-auth-cli authz [command]

Available Commands:
  policy      Commands related to policy
  role        Commands related to role

Flags:
  -h, --help   help for authz

Use "chef-platform-auth-cli authz [command] --help" for more information about a command.
```

## `chef-platform-auth-cli authz policy`

```text
Commands related to policy

Usage:
  chef-platform-auth-cli authz policy [command]

Available Commands:
  create-policy  create a policy under the current organization
  delete-policy  Delete a policy
  disable-policy Disable a policy in the current organization
  enable-policy  Enable a policy in the current organization
  get-policy     Get details of a policy
  list-policy    List policies
  test-policy    Test a policy
  update-policy  Update a policy

Flags:
  -h, --help   help for policy

Use "chef-platform-auth-cli authz policy [command] --help" for more information about a command.
```

## `chef-platform-auth-cli authz policy create-policy`

```text
### Summary:
This operation will create a new user-defined policy under current organization. Policies allow access to API end-points based upon routing rules and enforced by the API gateway.
Policies do **not** enforce attribute based access rules (ABAC) inside a domain on an object (e.g. User A can execute courier job 1234).

#### Note:
This method will always result in a Policy which is defined by "User"

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 409 (Conflict)
Policy name already exist in this organizational unit

#### 422 (Unprocessable Content)
The policy does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz policy create-policy [flags]

Examples:
- title: adminAccessExample
  example: |
    {
        "description": "Allows full access to read data from the account domain",
        "name": "Account-All-Admin",
        "statements": [
            {
                "action": "Allow",
                "routes": [
                    "/accounts/"
                ],
                "verbs": [
                    "get"
                ]
            },
            {
                "action": "Allow",
                "routes": [
                    "/accounts/{userName}/password//change",
                    "/accounts//users/{}/update",
                    "/accounts/{userName}/password"
                ],
                "variables": [
                    {
                        "name": "userName",
                        "values": [
                            "Rohit",
                            "Rahul"
                        ]
                    }
                ],
                "verbs": [
                    "get"
                ]
            }
        ],
        "version": "1.0"
    }
- title: readOnlyAccessExample
  example: |
    {
        "description": "Allows read-only access to account data",
        "name": "Account-Read-Only",
        "statements": [
            {
                "action": "Allow",
                "routes": [
                    "/accounts/*"
                ],
                "verbs": [
                    "get"
                ]
            },
            {
                "action": "Deny",
                "routes": [
                    "/accounts/{userName}/password/*"
                ],
                "variables": [
                    {
                        "name": "userName",
                        "values": [
                            "John",
                            "Doe"
                        ]
                    }
                ],
                "verbs": [
                    "post"
                ]
            }
        ],
        "version": "1.0"
    }
- title: updateAccessExample
  example: |
    {
        "description": "Allows update access to account data",
        "name": "Account-Update-Only",
        "statements": [
            {
                "action": "Allow",
                "routes": [
                    "/accounts/{userName}/update"
                ],
                "variables": [
                    {
                        "name": "userName",
                        "values": [
                            "Alice",
                            "Bob"
                        ]
                    }
                ],
                "verbs": [
                    "put"
                ]
            }
        ],
        "version": "1.0"
    }


Flags:
      --body string             The policy to be created
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for create-policy
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli authz policy delete-policy`

```text
### Summary:
This operation will delete an existing policy under current org.

#### Note:
This method will only allow deleting of user-defined policies

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
Policy doesn't exist in this organization

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not deleted.

#### 423 (Locked)
Policy is not a user-defined Policy and thus can not be deleted

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz policy delete-policy [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-policy
      --policyId string   ID of a Policy
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz policy disable-policy`

```text
### Summary:
Use this operation when there is a need to disable a policy. This will deactivate the policy enforcement.

#### Note:
This operation can only be used to disable an already enabled User policy.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The policy requested does not exist in the organization

#### 409 (Conflict)
Expect this error code if the policy is already disabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
Policy is not a User policy

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz policy disable-policy [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for disable-policy
      --policyId string   ID of a Policy
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz policy enable-policy`

```text
### Summary:
Use this operation to enable a disabled policy. This will enable enforcement of the policy.

#### Note:
This operation can only be used to enable an already disabled User policy.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The policy requested does not exist in the organization

#### 409 (Conflict)
Expect this error code if the policy is already enabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
Policy is not a user-defined policy

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz policy enable-policy [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for enable-policy
      --policyId string   ID of a Policy
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz policy get-policy`

```text
### Summary:
Use this operation to get details of a user-defined policy or a system defined policy in the organization

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The policy requested does not exist in the organization and neither a system policy

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz policy get-policy [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-policy
      --policyId string   ID of a Policy
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz policy list-policy`

```text
### Summary:
This operation will search for user-defined policies registered in the current organization and system policies

#### Note:
Unless definedBy is specified this will default to limiting results to user defined policies

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz policy list-policy [flags]

Flags:
      --action string         the policy must contain a statement with this action
      --definedBy string      who defined the policy (default "user")
      --description string    A partial match against the description of a Policy
      --format string         to print response in format (default "json")
  -h, --help                  help for list-policy
      --name string           A partial match against the name of a Policy
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --route string          the policy must contain a statement with a partial match of this text in the route
      --status string         The Status of the policies to filter by

      --text string           A partial match against the name or description of a Policy
      --verb string           the policy must contain a statement with this verb
      --verbose               to show debug logs
```

## `chef-platform-auth-cli authz policy test-policy`

```text
### Summary:
This operation can be used to submit an array of routes and HTTP verb to test a policy

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The policy requested is neither a system policy; nor it exists in the organization

#### 422 (Unprocessable Content)
The requested payload does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz policy test-policy [flags]

Examples:
- title: Default example
  example: |
    {
        "requests": [
            {
                "method": "get",
                "path": "http://demo.saas.chef.io/accounts/nodes"
            },
            {
                "method": "patch",
                "path": "https://demo.saas.chef.io/accounts/node/37e71274-a9a4-4d53-9e68-f7bd256b4e7c/enable"
            },
            {
                "method": "post",
                "path": "/accounts/policies"
            },
            {
                "method": "get",
                "path": "/accounts/policy/82eb6f39-4c59-4428-a86c-e673fac583df/"
            }
        ]
    }


Flags:
      --body string             Array of URIs to test
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for test-policy
      --policyId string         ID of a Policy
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli authz policy update-policy`

```text
### Summary:
This operation will update an existing policy under current org.

#### Note:
This method will only allow updating of user-defined policies

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
Policy doesn't exist in this organization

#### 409 (Conflict)
Policy name already exist in this organization

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 422 (Unprocessable Content)
The policy does not meet the validation requirements. Reference the errors in the response for more details.

#### 423 (Locked)
Policy is not a user-defined Policy and thus can not be updated

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz policy update-policy [flags]

Flags:
      --If-Match string      If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string          The policy details to be updated
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-policy
      --policyId string      ID of a Policy
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli authz role`

```text
Commands related to role

Usage:
  chef-platform-auth-cli authz role [command]

Available Commands:
  assign-policies Create role and assign specified policies to the role.
  assign-policy   Assign a user-defined policy to user-defined role in the current organization
  create-role     Create a role under current organization
  delete-policy   Detach a user-defined policy from a user-defined role
  delete-role     Delete a role
  disable-role    Disable a role in the current organization
  enable-role     Enable a user-defined role in the current organization
  find-role       List roles
  get-policy      Get details of a policy assigned to the role
  get-role        Get details of a role
  list-policies   List all policies attached to a role
  modify-policies Assign specified policies to the role.
  test-role       Test a role
  update-role     Update a role

Flags:
  -h, --help   help for role

Use "chef-platform-auth-cli authz role [command] --help" for more information about a command.
```

## `chef-platform-auth-cli authz role assign-policies`

```text
### Summary:
If the role with the provided name already exists,the API will assign the specified policies to the role as a final set of policies.
If the provided role does not exist, the API will create the role and assign the specified policies to the role as a final set of policies.
---
### Errors

#### 404 (Not Found)
The policy you requested does not exist.

#### 422 (Policy validation failed)
The policy is a system defined policy not a user defined policy.

#### 423 (Locked)
The role or policy is disabled.

#### 401 (Unauthorized)
The credential used to access this resource is invalid or expired.

#### 403 (Forbidden)
A user with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected errors occurred while requesting this resource.

#### 503 (Service Unavailable)
The request service is not responsive.

Usage:
  chef-platform-auth-cli authz role assign-policies [flags]

Examples:
- title: Default example
  example: |
    {
        "description": "User defined role description",
        "name": "User-defined-role",
        "policyIds": [
            "253c0912-67f0-4fae-8723-91614ade5474",
            "253c0912-67f0-4fae-8723-91614ade6443"
        ]
    }


Flags:
      --body string             Policies to be assigned to the role
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for assign-policies
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli authz role assign-policy`

```text
### Summary:
This operation will assign a user-defined policy to the given user-defined role in the current organization

#### Note:
This method will only allow assigning a user-defined policy to a user-defined role

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role you requested does not exist in the organization

#### 409 (Conflict)
The policy is already assigned to this role

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
The requested role is not a user managed role

#### 423 (Locked)
The policy is not a user-defined policy

#### 423 (Locked)
The requested role is disabled

#### 423 (Locked)
The requested policy is disabled

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role assign-policy [flags]

Examples:
- title: Default example
  example: |
    {
        "policyId": "82eb6f39-4c59-4428-a86c-e673fac583df"
    }


Flags:
      --If-Match string         If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string             policy to add
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for assign-policy
      --profile string          name of the profile to be used for cmd (default "default")
      --roleId string           ID of a Role
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli authz role create-role`

```text
### Summary:
This operation will create a new Role. Roles are assigned to one or more policies.
Policies allow access to API end-points based upon routing rules and enforced by the API gateway.
Users can then assign the roles, when a user logs into a tenant and selects an organization, they must choose a role.

#### Note:
This method will always result in a Role which is user-defined and meant for "user". Role meant for "node" can not be created with this api.

---
### Errors

#### 400 (Bad Request)
Invalid schema for request.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 409 (Conflict)
Role name already exist in this organizational unit.

#### 422 (Unprocessable Content)
The role does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role create-role [flags]

Examples:
- title: Default example
  example: |
    {
        "description": "A basic user role with limited access",
        "name": "BasicUserRole"
    }


Flags:
      --body string             The role to be created
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for create-role
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli authz role delete-policy`

```text
### Summary:
Use this operation to remove a policy from a role.

#### Note:
This method will only allow detaching of user-defined policies from a User defined role in the current organization.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role you requested does not exist in the organization

#### 409 (Conflict)
The policy requested is not attached to the role

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
Role is not a User defined role

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role delete-policy [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-policy
      --policyId string   ID of a Policy
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz role delete-role`

```text
### Summary:
Use this operation to remove a role. This will prevent all users from selecting this role upon login or when switching roles.

**WARNING** If a user is currently using a role when the cache expires (invalidated) the
user will be prevented from performing any actions until they log out and log in again.

#### Note:
This method will only allow deleting of User defined roles

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role requested does not exist in the organization

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
Role is not a User managed role

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role delete-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-role
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz role disable-role`

```text
### Summary:
Use this operation when there is need to disable a role for all assigned users. This
will prevent all users from selecting this role upon login, or changing to the role.

**WARNING** If a user is currently using a role when the cache expires (invalidated) the
user will be prevented from performing any actions until they log out and log in again.

#### Note:
This method will only allow disabling of User or System roles. Service roles can not be disabled
using this method.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role was not found

#### 409 (Conflict)
Role is already disabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
Role is not a User managed role

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role disable-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for disable-role
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz role enable-role`

```text
### Summary:
Use this operation to enable a disabled role.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role you requested does not exist in the organization

#### 409 (Conflict)
the role is already enabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
Role is not a User managed role

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role enable-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for enable-role
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz role find-role`

```text
### Summary:
This operation will list all user-defined roles under the current organization and the system defined roles.

#### Note:
Unless definedBy is specified this will default to limiting results to user-defined roles only

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role find-role [flags]

Flags:
      --definedBy string      who defined the role (default "user")
      --description string    A partial match against the description of a Role
      --filterByIds strings   List of role ids to filter by
      --format string         to print response in format (default "json")
  -h, --help                  help for find-role
      --level string          at what level can this role be used (default "org")
      --meantFor string       to whom this role can be assigned (default "user")
      --name string           The name of the role

      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --status string         The Status of the roles to filter by
                               (default "enabled")
      --text string           A partial match against the name or description of a role
      --verbose               to show debug logs
```

## `chef-platform-auth-cli authz role get-policy`

```text
### Summary:
Use this operation to get details of a policy assigned to the role. The role can be a system defined role or a custom role in the current organization.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role you requested is neither a system defined role nor it exists in the current organization

#### 404 (Not Found)
The policy requested is not attached to the role

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role get-policy [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-policy
      --policyId string   ID of a Policy
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --verbose           to show debug logs
```

## `chef-platform-auth-cli authz role get-role`

```text
### Summary:
Use this operation to get details of a user-defined role in the organization or any system defined role

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role requested does not exist in the organization and neither a system-defined role

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role get-role [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-role
      --profile string   name of the profile to be used for cmd (default "default")
      --roleId string    ID of a Role
      --verbose          to show debug logs
```

## `chef-platform-auth-cli authz role list-policies`

```text
### Summary:
This operation will list all policies attached to the given role. The role can be a system defined role or a custom role in the current organization.

#### Note:
1. Unless roleStatus is specified it will be considered as enabled(default) and we will check if the role is enabled.
2. Unless policyStatus is specified it will be considered as enabled(default) it will get all policies which are enabled. If it's specified as all it gets all policies irrespective of their status.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

#### 404 (Not Found)
The role you requested is neither a system defined role nor it exists in the current organization.

#### 404 (Not Found)
The roleStatus is sent as disabled but the role is not disabled. So the given role doesn't match the status criteria and hence not found

#### 423 (Locked)
The roleStatus is sent as enabled but the role is disabled. Then this API should not return the associated policies.

Usage:
  chef-platform-auth-cli authz role list-policies [flags]

Flags:
      --expand                Expand the policies
      --format string         to print response in format (default "json")
  -h, --help                  help for list-policies
      --policyStatus string   The Status of the policies to filter by

      --profile string        name of the profile to be used for cmd (default "default")
      --roleId string         ID of a Role
      --roleStatus string     The Status of the roles to filter by

      --verbose               to show debug logs
```

## `chef-platform-auth-cli authz role modify-policies`

```text
### Summary:
The API will assign the specified policies to the role as a final set of policies.
---
### Errors

#### 404 (Not Found)
The policy or role you requested does not exist.

#### 422 (Policy validation failed)
The policy or role is a system defined policy not a user defined policy.

#### 423 (Locked)
The role or policy is disabled.

#### 401 (Unauthorized)
The credential used to access this resource is invalid or expired.

#### 403 (Forbidden)
A user with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected errors occurred while requesting this resource.

#### 503 (Service Unavailable)
The request service is not responsive.

Usage:
  chef-platform-auth-cli authz role modify-policies [flags]

Flags:
      --body string          Policies to be attached or detached to the role
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for modify-policies
      --profile string       name of the profile to be used for cmd (default "default")
      --roleId string        ID of a Role
      --verbose              to show debug logs
```

## `chef-platform-auth-cli authz role test-role`

```text
### Summary:
This operation can be used to test access to a route and method against a given role

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role requested is neither a system role; nor it exists in the organization

#### 422 (Unprocessable Content)
The requested payload does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role test-role [flags]

Examples:
- title: Default example
  example: |
    {
        "method": "get",
        "path": "http://demo.saas.chef.io/accounts/nodes"
    }


Flags:
      --body string             An URI and HTTP method to test
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for test-role
      --profile string          name of the profile to be used for cmd (default "default")
      --roleId string           ID of a Role
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli authz role update-role`

```text
### Summary:
This operation will update an existing role under current org.

#### Note:
This method will only allow updating of user-defined roles

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
Role doesn't exist in this organization

#### 409 (Conflict)
Role name already exist in this organization

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 422 (Unprocessable Content)
The role does not meet the validation requirements. Reference the errors in the response for more details.

#### 423 (Locked)
Role is not a user-defined role and thus can not be updated

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli authz role update-role [flags]

Flags:
      --If-Match string      If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string          The role details to be updated
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-role
      --profile string       name of the profile to be used for cmd (default "default")
      --roleId string        ID of a Role
      --verbose              to show debug logs
```

## `chef-platform-auth-cli deregister-device`

```text
Use this operation to de-register a device

Usage:
  chef-platform-auth-cli deregister-device [flags]

Flags:
      --force            force remove profile from local list of profiles, even on error from server
  -f, --format string    to print response in format (default "json")
  -h, --help             help for deregister-device
      --profile string   name for the profile of the new api-token (default "default")
  -v, --verbose          to show debug logs
```

## `chef-platform-auth-cli get-default-profile`

```text
get default profile for request

Usage:
  chef-platform-auth-cli get-default-profile [flags]

Flags:
  -h, --help   help for get-default-profile
```

## `chef-platform-auth-cli keygen`

```text
Generate Ed25519 private/public key pairs for signing purposes.

Usage:
  chef-platform-auth-cli keygen [flags]

Flags:
  -f, --force           Overwrite existing key files
  -h, --help            help for keygen
  -n, --name string     Base name for key files (will generate <name>_private.pem and <name>_public.pem) (default "ed25519")
  -o, --output string   Output directory path for key pair (default "./")
```

## `chef-platform-auth-cli license-management`

```text
Commands for managing license management

Usage:
  chef-platform-auth-cli license-management [command]

Available Commands:
  asset       Commands related to asset
  entitlement Commands related to entitlement
  feature     Commands related to feature
  license     Commands related to license
  service     Commands related to service

Flags:
  -h, --help   help for license-management

Use "chef-platform-auth-cli license-management [command] --help" for more information about a command.
```

## `chef-platform-auth-cli license-management asset`

```text
Commands related to asset

Usage:
  chef-platform-auth-cli license-management asset [command]

Available Commands:
  get-asset   Get the Asset details
  list-assets Get all assets

Flags:
  -h, --help   help for asset

Use "chef-platform-auth-cli license-management asset [command] --help" for more information about a command.
```

## `chef-platform-auth-cli license-management asset get-asset`

```text
### Summary:
Use this operation to get the asset details based on the given license ids and AssetId or AssetName.

---
### Errors

#### 400 (Bad Request)
The licenses do not exist in the tenant

#### 402 (License not entitled)
The licenses are all disabled

#### 422 (Unprocessable Content)
Both Asset Id and Asset Name cannot be blank

#### 422 (Unprocessable Content)
Either Asset Id or Asset Name should be given; Not both

Usage:
  chef-platform-auth-cli license-management asset get-asset [flags]

Flags:
      --assetId string      Asset id to fetch the details
      --assetName string    Asset name to fetch the details
      --format string       to print response in format (default "json")
  -h, --help                help for get-asset
      --licenseIds string   comma separated list of License Ids
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-platform-auth-cli license-management asset list-assets`

```text
### Summary:
Use this operation to list all the Assets for the given license Id(s)

---
### Errors

#### 404 (Not Found)
The license(s) do not exist in the tenant

Usage:
  chef-platform-auth-cli license-management asset list-assets [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for list-assets
      --licenseIds string   comma separated list of License Ids
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-platform-auth-cli license-management entitlement`

```text
Commands related to entitlement

Usage:
  chef-platform-auth-cli license-management entitlement [command]

Available Commands:
  get-entitlement   Get the Entitlement details
  list-entitlements Get all entitlements

Flags:
  -h, --help   help for entitlement

Use "chef-platform-auth-cli license-management entitlement [command] --help" for more information about a command.
```

## `chef-platform-auth-cli license-management entitlement get-entitlement`

```text
### Summary:
Use this operation to get the entitlement details based on the given license ids, quantity, date and the Entitlement Id.

---
### Errors

#### 400 (Bad Request)
The licenses do not exist in the tenant

#### 402 (License not entitled)
The licenses are all disabled

#### 422 (Unprocessable Content)
Both Entitlement Id and Entitlement Name cannot be blank

#### 422 (Unprocessable Content)
Either Entitlement Id or Entitlement Name should be given

Usage:
  chef-platform-auth-cli license-management entitlement get-entitlement [flags]

Flags:
      --date string              date to validate the license entitlement (format :- YYYY-MM-DD)
      --entitlementId string     Entitlement id to fetch the details
      --entitlementName string   Entitlement name to fetch the details
      --format string            to print response in format (default "json")
  -h, --help                     help for get-entitlement
      --licenseIds string        comma separated list of License Ids
      --profile string           name of the profile to be used for cmd (default "default")
      --quantity string          quantity of entitlement
      --verbose                  to show debug logs
```

## `chef-platform-auth-cli license-management entitlement list-entitlements`

```text
### Summary:
Use this operation to list all the Entitlements for the given license Id(s)

---
### Errors

#### 400 (Bad Request)
The licenses do not exist in the tenant

Usage:
  chef-platform-auth-cli license-management entitlement list-entitlements [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for list-entitlements
      --licenseIds string   comma separated list of License Ids
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-platform-auth-cli license-management feature`

```text
Commands related to feature

Usage:
  chef-platform-auth-cli license-management feature [command]

Available Commands:
  get-feature   Get the Feature details
  list-features Get all features

Flags:
  -h, --help   help for feature

Use "chef-platform-auth-cli license-management feature [command] --help" for more information about a command.
```

## `chef-platform-auth-cli license-management feature get-feature`

```text
### Summary:
Use this operation to get the feature details based on the given license ids and FeatureId or FeatureName.

---
### Errors

#### 400 (Bad Request)
The license(s) do not exist in the tenant

#### 402 (License not entitled)
The licenses are all disabled

#### 422 (Unprocessable Content)
Both Feature Id and Feature Name cannot be blank

#### 422 (Unprocessable Content)
Either Feature Id or Feature Name should be given

Usage:
  chef-platform-auth-cli license-management feature get-feature [flags]

Flags:
      --featureId string     Feature id to fetch the details
      --featureName string   Feature name to fetch the details
      --format string        to print response in format (default "json")
  -h, --help                 help for get-feature
      --licenseIds string    comma separated list of License Ids
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli license-management feature list-features`

```text
### Summary:
Use this operation to list all the Features for the given license Id(s)

---
### Errors

#### 400 (Bad Request)
The licenses do not exist in the tenant

Usage:
  chef-platform-auth-cli license-management feature list-features [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for list-features
      --licenseIds string   comma separated list of License Ids
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-platform-auth-cli license-management license`

```text
Commands related to license

Usage:
  chef-platform-auth-cli license-management license [command]

Available Commands:
  client-license   List client applications
  describe-license Describe a license
  disable-license  Disable a license
  download-license Download a license
  enable-license   Enable a license
  list-licenses    List licenses
  load-license     Load a license
  remove-license   Remove a license
  sync-license     Sync all licenses
  upload-license   Upload a license
  validate-license Validate a license Id

Flags:
  -h, --help   help for license

Use "chef-platform-auth-cli license-management license [command] --help" for more information about a command.
```

## `chef-platform-auth-cli license-management license client-license`

```text
### Summary:
Use this operation to provide the client applications based on given license Ids and entitlement Id

---
### Errors

#### 400 (Bad Request)
The licenses you requested does not exist in the tenant

#### 402 (License not entitled)
The licenses are all disabled

#### 422 (Unprocessable Content)
Entitlement Id cannot be blank

Usage:
  chef-platform-auth-cli license-management license client-license [flags]

Flags:
      --entitlementId string   Entitlement id to fetch the details
      --format string          to print response in format (default "json")
  -h, --help                   help for client-license
      --licenseIds string      comma separated list of License Ids
      --profile string         name of the profile to be used for cmd (default "default")
      --verbose                to show debug logs
```

## `chef-platform-auth-cli license-management license describe-license`

```text
### Summary:
Use this operation to describe a license based on given license Ids and entitlement Id

---
### Errors

#### 404 (Not Found)
The licenses you requested does not exist in the tenant

#### 402 (License not entitled)
The licenses are all disabled

#### 422 (Unprocessable Content)
Entitlement Id cannot be blank

Usage:
  chef-platform-auth-cli license-management license describe-license [flags]

Flags:
      --entitlementId string   Entitlement id to fetch the details
      --format string          to print response in format (default "json")
  -h, --help                   help for describe-license
      --licenseIds string      comma separated list of License Ids
      --profile string         name of the profile to be used for cmd (default "default")
      --verbose                to show debug logs
```

## `chef-platform-auth-cli license-management license disable-license`

```text
### Summary:
Use this operation to disable a license under a tenant in the local datastore

---
### Errors

#### 404 (Not Found)
The license you requested does not exist in the tenant

#### 400 (Bad Request)
The license you requested is not valid

#### 409 (Conflict)
The license is already disabled

Usage:
  chef-platform-auth-cli license-management license disable-license [flags]

Flags:
      --If-Match string    If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string      to print response in format (default "json")
  -h, --help               help for disable-license
      --licenseId string   the License id
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli license-management license download-license`

```text
### Summary:
Use this operation download a license in a tenant

---
### Errors

#### 404 (Not Found)
The license you requested does not exist in the tenant

#### 423 (Locked)
The license is disabled in the tenant

Usage:
  chef-platform-auth-cli license-management license download-license [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for download-license
      --licenseId string   the License id
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
      --version string     the license version (default "2")
```

## `chef-platform-auth-cli license-management license enable-license`

```text
### Summary:
Use this operation to enable a license under a tenant in the local datastore

---
### Errors


#### 404 (Not Found)
The license you requested does not exist in the tenant

#### 400 (Bad Request)
The license you requested is not valid

#### 409 (Conflict)
The license is already enabled

Usage:
  chef-platform-auth-cli license-management license enable-license [flags]

Flags:
      --If-Match string    If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string      to print response in format (default "json")
  -h, --help               help for enable-license
      --licenseId string   the License id
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli license-management license list-licenses`

```text
### Summary:
Use this operation to list all the license present in the local data store in a tenant

---
### Errors

Usage:
  chef-platform-auth-cli license-management license list-licenses [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-licenses
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli license-management license load-license`

```text
### Summary:
Use this operation to  load the license information.
This endpoint is useful in non- airgap mode. It downloads the License information from Global Service,
and then adds it to Local license datastore.

---
### Errors

#### 404 (Not Found)
The license you requested does not exist

#### 400 (Bad Request)
The license you requested is not valid

#### 400 (Bad Request)
This API can't be used in AirGap mode

Usage:
  chef-platform-auth-cli license-management license load-license [flags]

Examples:
- title: Default example
  example: |
    {
        "licenseId": "J7C16R6HE4Z4Q9WX1Y67W9J9SF"
    }


Flags:
      --body string             Load the license information in local datastore
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for load-license
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli license-management license remove-license`

```text
### Summary:
Use this operation to remove a license under a tenant in the local datastore

---
### Errors


#### 404 (Not Found)
The license you requested does not exist in the tenant

#### 400 (Bad Request)
The license you requested is not valid

#### 409 (Conflict)
The license is already removed from the tenant

Usage:
  chef-platform-auth-cli license-management license remove-license [flags]

Flags:
      --If-Match string    If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string      to print response in format (default "json")
  -h, --help               help for remove-license
      --licenseId string   the License id
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli license-management license sync-license`

```text
### Summary:
Use this operation to sync all licenses under a tenant in the local datastore. This doesn't change the state of licenses present in local datastore.

---
### Errors

#### 400 (Bad Request)
This API can't be used in AirGap mode

Usage:
  chef-platform-auth-cli license-management license sync-license [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for sync-license
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli license-management license upload-license`

```text
### Summary:
Use this operation to upload the license information.
This endpoint is useful in airgap mode. It takes the license information as input,
and then adds/updates it to Local license datastore.

---
### Errors

#### 400 (Bad Request)
The license Id present in request body is invalid

#### 400 (Bad Request)
Evidence present in request body is invalid

Usage:
  chef-platform-auth-cli license-management license upload-license [flags]

Examples:
- title: Default example
  example: |
    {
        "asset": [],
        "bundle": "Chef Automate",
        "customerVersion": "1",
        "entitlements": [
            {
                "grace": {
                    "duration": 90,
                    "limit": 0
                },
                "id": "c770f0fa-7fa1-4c5b-b694-7b5462595f35",
                "limit": 1,
                "measure": "node",
                "name": "Automate",
                "period": {
                    "end": "2024-07-31T00:00:00Z",
                    "start": "2021-07-31T00:00:00Z"
                }
            },
            {
                "grace": {
                    "duration": 90,
                    "limit": 1250
                },
                "id": "a5213d76-181f-4924-adba-4b7ed2b098b5",
                "limit": 2500,
                "measure": "node",
                "name": "Infra",
                "period": {
                    "end": "2024-07-31T00:00:00Z",
                    "start": "2021-07-31T00:00:00Z"
                }
            }
        ],
        "evidence": {
            "generated": "1706165217",
            "hash": "8e975550e20a6c70bb9d7dcf3f5958a6",
            "key_sha256": "0673e757a64ec284a1dd2f3700456ea6e44489aa3096c32f34cd67394dc22a21"
        },
        "features": [],
        "generatedOnDateUTC": "2023-12-25T06:46:57Z",
        "generator": "chef/license-(devel)",
        "id": "02e9b148-d2f6-4ea1-8998-b591506fc973",
        "refreshLastDateUTC": "2024-01-15T06:46:57Z",
        "refreshNextDateUTC": "2024-04-25T06:46:57Z",
        "services": [],
        "type": "commercial",
        "version": "2"
    }


Flags:
      --body string             Load the license information in local datastore
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for upload-license
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli license-management license validate-license`

```text
### Summary:
Use this operation validate if a licenseId is present in tenant or not

---
### Errors

#### 400 (Bad Request)
The license you requested does not exist in the tenant

#### 423 (Locked)
The license is disabled in the tenant

Usage:
  chef-platform-auth-cli license-management license validate-license [flags]

Flags:
      --format string      to print response in format (default "json")
  -h, --help               help for validate-license
      --licenseId string   the License id
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
      --version string     the license version (default "2")
```

## `chef-platform-auth-cli license-management service`

```text
Commands related to service

Usage:
  chef-platform-auth-cli license-management service [command]

Available Commands:
  get-service   Get the Service details
  list-services Get all services

Flags:
  -h, --help   help for service

Use "chef-platform-auth-cli license-management service [command] --help" for more information about a command.
```

## `chef-platform-auth-cli license-management service get-service`

```text
### Summary:
Use this operation to get the service details based on the given license ids and serviceId or serviceName.

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorized)
Missing or invalid authentication credentials

#### 402 (License not entitled)
The licenses are all disabled

#### 404 (Not Found)
The license(s) do not exist in the tenant

#### 422 (Unprocessable Content)
Both Service Id and Service Name cannot be blank

#### 422 (Unprocessable Content)
Either Service Id or Service Name should be given; Not both

Usage:
  chef-platform-auth-cli license-management service get-service [flags]

Flags:
      --format string        to print response in format (default "json")
  -h, --help                 help for get-service
      --licenseIds string    comma separated list of License Ids
      --profile string       name of the profile to be used for cmd (default "default")
      --serviceId string     Service id to fetch the details
      --serviceName string   Service name to fetch the details
      --verbose              to show debug logs
```

## `chef-platform-auth-cli license-management service list-services`

```text
### Summary:
Use this operation to list all the Services for the given license Id(s)

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorized)
Missing or invalid authentication credentials

#### 404 (Not Found)
The license(s) do not exist in the tenant

Usage:
  chef-platform-auth-cli license-management service list-services [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for list-services
      --licenseIds string   comma separated list of License Ids
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-platform-auth-cli license-usage`

```text
Commands for managing license usage

Usage:
  chef-platform-auth-cli license-usage [command]

Available Commands:
  audit       Commands related to audit

Flags:
  -h, --help   help for license-usage

Use "chef-platform-auth-cli license-usage [command] --help" for more information about a command.
```

## `chef-platform-auth-cli license-usage audit`

```text
Commands related to audit

Usage:
  chef-platform-auth-cli license-usage audit [command]

Available Commands:
  get-usage   get the audit details for a specific date
  list-audit  Lists the services audit for a specific date
  list-audits List Consumption Audits

Flags:
  -h, --help   help for audit

Use "chef-platform-auth-cli license-usage audit [command] --help" for more information about a command.
```

## `chef-platform-auth-cli license-usage audit get-usage`

```text
### Summary:
Use this to get the specific audit details for a given service for a specific date

#### Notes:
This response is polymorphic the resulting item arry will be unique to the service selected.

---
### Errors

#### 404 (Not Found)
The date requested contains no audits

#### 404 (Not Found)
The service requested was not audited on the requested date

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

Usage:
  chef-platform-auth-cli license-usage audit get-usage [flags]

Flags:
      --date string           date of an audit (format :- YYYY-MM-DD)
      --format string         to print response in format (default "json")
  -h, --help                  help for get-usage
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-platform-auth-cli license-usage audit list-audit`

```text
### Summary:
Use this operation when there is a need to view the specific services that were audited on a specific date

---
### Errors

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

Usage:
  chef-platform-auth-cli license-usage audit list-audit [flags]

Flags:
      --date string           date of an audit (format :- YYYY-MM-DD)
      --format string         to print response in format (default "json")
  -h, --help                  help for list-audit
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-platform-auth-cli license-usage audit list-audits`

```text
### Summary:
This operation will list consumption audits

### Errors

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

Usage:
  chef-platform-auth-cli license-usage audit list-audits [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for list-audits
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --status string         The Status of an Usage Audit

      --verbose               to show debug logs
```

## `chef-platform-auth-cli list-profile-names`

```text
Get the list of profile names available

Usage:
  chef-platform-auth-cli list-profile-names [flags]

Flags:
  -h, --help   help for list-profile-names
```

## `chef-platform-auth-cli log-service`

```text
Commands for managing log service

Usage:
  chef-platform-auth-cli log-service [command]

Available Commands:
  audit       Commands related to audit
  general     Commands related to general
  health      Commands related to health

Flags:
  -h, --help   help for log-service

Use "chef-platform-auth-cli log-service [command] --help" for more information about a command.
```

## `chef-platform-auth-cli log-service audit`

```text
Commands related to audit

Usage:
  chef-platform-auth-cli log-service audit [command]

Available Commands:
  get-admin-logs Retrieve audit logs for tenant administration
  get-self-logs  Retrieve audit logs for current user

Flags:
  -h, --help   help for audit

Use "chef-platform-auth-cli log-service audit [command] --help" for more information about a command.
```

## `chef-platform-auth-cli log-service audit get-admin-logs`

```text
### Summary:
Retrieve audit logs for administrative oversight within a tenant. This endpoint allows tenant administrators
to access audit logs for specified users or all users within their tenant organization.

#### Note:
- Requires tenant administrator role with log access permissions
- Supports filtering by specific user email addresses
- Time range defaults to last 1 hour with maximum of 10 days
- Returns logs as stream for efficient large data transfer
- Tenant isolation ensures users only see logs from their own tenant

### Errors
#### 400 (Bad Request)
Invalid query parameters such as malformed timestamps or email addresses

#### 401 (Unauthorized)
Authentication required - missing or invalid JWT token

#### 403 (Forbidden)
Insufficient permissions - user does not have tenant administrator role or log access permissions

#### 409 (Conflict)
Conflict - Audit log collection is disabled for the tenant

#### 500 (Internal Server Error)
Server error retrieving logs from the aggregation system

Usage:
  chef-platform-auth-cli log-service audit get-admin-logs [flags]

Flags:
      --file-name string   path to file in the local system to write response body
      --format string      to print response in format (default "json")
      --from string        Start timestamp for log retrieval in ISO 8601 format.

  -h, --help               help for get-admin-logs
      --order string       Sort order for log entries by timestamp.
                           - asc: Oldest logs first
                           - desc: Newest logs first (default)

      --override-file      to override file if already exists
      --profile string     name of the profile to be used for cmd (default "default")
      --to string          End timestamp for log retrieval in ISO 8601 format.

      --users strings      Filter audit logs by specific user email addresses. Multiple users can be specified as a comma-separated list.
                           Only available for admin audit log access.

      --verbose            to show debug logs
```

## `chef-platform-auth-cli log-service audit get-self-logs`

```text
### Summary:
Retrieve audit logs for the currently authenticated user. This endpoint allows users to access
their own audit log history to review their account activity and troubleshoot issues.

#### Note:
- User can only access their own audit logs (automatic filtering by user ID)
- Time range defaults to last 1 hour with maximum of 10 days
- Returns logs as stream for efficient data transfer
- Ensures complete data privacy and isolation between users

### Errors
#### 400 (Bad Request)
Invalid query parameters such as malformed timestamps

#### 401 (Unauthorized)
Authentication required - missing or invalid JWT token

#### 409 (Conflict)
Conflict - Audit log collection is disabled for the tenant

#### 500 (Internal Server Error)
Server error retrieving logs from the aggregation system

Usage:
  chef-platform-auth-cli log-service audit get-self-logs [flags]

Flags:
      --file-name string   path to file in the local system to write response body
      --format string      to print response in format (default "json")
      --from string        Start timestamp for log retrieval in ISO 8601 format.

  -h, --help               help for get-self-logs
      --order string       Sort order for log entries by timestamp.
                           - asc: Oldest logs first
                           - desc: Newest logs first (default)

      --override-file      to override file if already exists
      --profile string     name of the profile to be used for cmd (default "default")
      --to string          End timestamp for log retrieval in ISO 8601 format.

      --verbose            to show debug logs
```

## `chef-platform-auth-cli log-service general`

```text
Commands related to general

Usage:
  chef-platform-auth-cli log-service general [command]

Available Commands:
  get-service-logs Retrieve general service logs
  list-services    List available services for log filtering

Flags:
  -h, --help   help for general

Use "chef-platform-auth-cli log-service general [command] --help" for more information about a command.
```

## `chef-platform-auth-cli log-service general get-service-logs`

```text
### Summary:
Retrieve general service logs from Kubernetes containers across the platform. This endpoint provides
system administrators with access to application logs for monitoring, debugging, and troubleshooting.

#### Note:
- Requires install admin token authentication (no tenant restrictions)
- Supports filtering by specific service names
- Supports advanced filtering with field-value pairs using logical operators
- Defaults to all services if no service filter provided
- Time range defaults to last 1 hour with maximum of 3 hours
- Returns logs as stream for efficient large data transfer
- Aggregates logs from multiple services and containers

#### Advanced Filtering:
The filter parameter accepts an array of objects with field-value pairs:
- Empty array [] returns all logs
- Single filters: [{"request-id": "abc"}] for exact matches
- Multiple values: [{"request-id": "abc,pqr"}] uses OR logic
- Multiple filters: [{"request-id": "abc"}, {"log-level": "info"}] uses AND logic
- Complex combinations: [{"request-id": "abc,pqr"}, {"log-level": "debug,info"}]

### Errors
#### 400 (Bad Request)
Invalid query parameters such as malformed timestamps, invalid service names, malformed filter objects, or time range exceeding 24 hours

#### 401 (Unauthorized)
Authentication required - missing or invalid install admin token

#### 403 (Forbidden)
Insufficient permissions - token does not have install admin privileges

#### 409 (Conflict)
Conflict - General log collection is disabled for the tenant

#### 500 (Internal Server Error)
Server error retrieving logs from the aggregation system

Usage:
  chef-platform-auth-cli log-service general get-service-logs [flags]

Flags:
      --file-name string     path to file in the local system to write response body
      --filter stringArray   Advanced filtering for log entries using field-value pairs with flexible logical operators.

                             ### Examples:
                             - [] - Return all logs (no filter)
                             - [{"request-id": "abc"}] - Logs with request ID 'abc'
                             - [{"request-id": "abc,pqr"}] - Logs with request ID 'abc' OR 'pqr'
                             - [{"request-id": "abc"}, {"log-level": "info"}] - Logs with request ID 'abc' AND log level 'info'
                             - [{"request-id": "abc,pqr"}, {"log-level": "debug,info"}] - Logs with (request ID 'abc' OR 'pqr') AND (log level 'debug' OR 'info')

                             ### Field Behavior:
                             - Supports any field name available in the log entries
                             - Returns empty response if no logs match the specified filter criteria (no error thrown)

      --format string        to print response in format (default "json")
      --from string          Start timestamp for log retrieval in ISO 8601 format.

  -h, --help                 help for get-service-logs
      --order string         Sort order for log entries by timestamp.
                             - asc: Oldest logs first
                             - desc: Newest logs first (default)

      --override-file        to override file if already exists
      --profile string       name of the profile to be used for cmd (default "default")
      --services strings     Filter general logs by specific service names. Multiple services can be specified as a comma-separated list.
                             At least one service must be specified, with a maximum of 5 services allowed.
                             Only available for general log access.

      --to string            End timestamp for log retrieval in ISO 8601 format.

      --verbose              to show debug logs
```

## `chef-platform-auth-cli log-service general list-services`

```text
### Summary:
Retrieve a list of available service names that can be used for filtering general logs. This endpoint helps
administrators discover which services are available in the logging system before querying their logs.

#### Note:
- Requires install admin token authentication (no tenant restrictions)
- Supports regex pattern filtering to find specific services
- Returns a simple list of service names available in the log aggregation system
- Useful for discovering services before using the /general endpoint with service filters
- Pattern matching is case-insensitive

### Errors
#### 400 (Bad Request)
Invalid regex pattern provided in the pattern parameter

#### 401 (Unauthorized)
Authentication required - missing or invalid install admin token

#### 403 (Forbidden)
Insufficient permissions - token does not have install admin privileges

#### 409 (Conflict)
Conflict - General log collection is disabled for the tenant

#### 500 (Internal Server Error)
Server error retrieving service list from the aggregation system

Usage:
  chef-platform-auth-cli log-service general list-services [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-services
      --pattern string   A regex pattern used to filter the service list. The pattern is applied to match service folder names
                         in the general-logs bucket. If not provided, all services are returned.

      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli log-service health`

```text
Commands related to health

Usage:
  chef-platform-auth-cli log-service health [command]

Available Commands:
  get-health-status Get health status

Flags:
  -h, --help   help for health

Use "chef-platform-auth-cli log-service health [command] --help" for more information about a command.
```

## `chef-platform-auth-cli log-service health get-health-status`

```text
### Summary:
Check the health status of the log service system components including log-service and fluent-bit

#### Note:
This endpoint provides real-time health status information for monitoring the log service infrastructure. It returns the operational status of both the log service itself and the fluent-bit log collector.

---
### Errors

#### 404 (Not Found)
The health endpoint is not available or service is not running

#### 500 (Internal Server Error)
The log service or fluent-bit components are experiencing issues

Usage:
  chef-platform-auth-cli log-service health get-health-status [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-health-status
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli node-account`

```text
Commands for managing node account

Usage:
  chef-platform-auth-cli node-account [command]

Available Commands:
  node        Commands related to node
  self        Commands related to self

Flags:
  -h, --help   help for node-account

Use "chef-platform-auth-cli node-account [command] --help" for more information about a command.
```

## `chef-platform-auth-cli node-account node`

```text
Commands related to node

Usage:
  chef-platform-auth-cli node-account node [command]

Available Commands:
  assign-role        Assigns a node a new role
  delete-node        Delete a node
  delete-role        Deletes a node roles
  disable-node       Disable a node
  disable-role       Disable a node's role (agent/service)
  enable-node        Enable a node
  enable-role        Enable a node's role (agent/service)
  find-node          Find nodes
  get-node           Get a node
  get-nodeByRef      Get a node by node ref id
  get-role           Get a node assigned role
  list-roles         List node assigned roles
  register-node      Registers a node
  rotate-credentials Request credential rotation
  update-credentials Manual credential rotation
  verify-role        Verify a node's assigned

Flags:
  -h, --help   help for node

Use "chef-platform-auth-cli node-account node [command] --help" for more information about a command.
```

## `chef-platform-auth-cli node-account node assign-role`

```text
### Summary:
Use this operation to assign a new role to an existing node.

#### Note:
Until this node is assigned credentials it will not be able to communicate with
the platform and will remain in the registered status

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role you requested does not exist in the organization

#### 409 (Conflict)
The role is already assigned to this node

#### 409 (Conflict)
The requested node is disabled, please enable the node before requesting credential rotation

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node assign-role [flags]

Examples:
- title: Default example
  example: |
    {
        "name": "example-role-name",
        "roleId": "3fa85f64-5717-4562-b3fc-2c963f66afa6"
    }


Flags:
      --If-Match string         If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string             Role to be assigned to this node
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for assign-role
      --nodeId string           ID of a node
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli node-account node delete-node`

```text
### Summary:
Use this operation to remove a node. This will prevent all services/agents running on the node from communicating with the platform.

**WARNING** Deleted node can no longer communicate with the platform, but may still be running. Once a node has been deleted
to enable communication again it must be re-enrolled

#### Notes:
This will have no effect for services/operations not managed by Chef Platform.
e.g. chef client communicating directly to Infra Server.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node or role you requested does not exist in the organization

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node delete-node [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-node
      --nodeId string     ID of a node
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli node-account node delete-role`

```text
### Summary:
Use this operation to remove a nodes role. This will disable all services/agents running on the node that are managed by Chef Platform.

**WARNING** Deleted node roles can no longer communicate with the platform, but may still be running. Once a role has been deleted
to enable communication again from this node's role it must be assigned (authorized), and then new credentials need to be issued. If a node's role
has been deleted consider removing the skill and re-installing it or performing a node re-enrollment

#### Notes:
This will have no effect for services/operations not managed by Chef Platform.
e.g. chef client communicating directly to Infra Server.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node or role you requested does not exist in the organization

#### 404 (Not Found)
The role you requested is not assigned to this node in the organization

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node delete-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-role
      --nodeId string     ID of a node
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --verbose           to show debug logs
```

## `chef-platform-auth-cli node-account node disable-node`

```text
### Summary:
Use this operation when there is a need to disable a node without deleting items. This
will disable all services/agents running on the node that are managed by Chef Platform.

**WARNING** Disabled nodes can no longer communicate with the platform, but may still be running. All actions
preformed by this node will fail until enabled.

#### Notes:
1.) This operation does not rotate or revoke credentials for the node. Once re-enabled
the node will continue to communicate using the same credentials.
2.) This will have no effect for services/operations not managed by Chef Platform.
e.g. chef client communicating directly to Infra Server.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 409 (Conflict)
Expect this error code if the node is already disabled.

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node disable-node [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for disable-node
      --nodeId string     ID of a node
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli node-account node disable-role`

```text
### Summary:
Use this operation when there is a need to disable a single role for a node without deleting that role.
This will disable that specific (services/agents) ability to communicate with the Chef Platform.

**WARNING** Disabled node's role can no longer communicate with the platform, but may still be running. All actions
preformed by this node for this role will fail until enabled.

#### Notes:
1.) This operation does not rotate or revoke credentials for the node's role. Once re-enabled
the node's role will continue to communicate using the same credentials.
2.) This will have no effect for services/operations not managed by Chef Platform.
e.g. chef client communicating directly to Infra Server.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 404 (Not Found)
The node's role you requested is not assigned to this role for the organization

#### 409 (Conflict)
Expect this error code if the node's role is already disabled.

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node disable-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for disable-role
      --nodeId string     ID of a node
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --verbose           to show debug logs
```

## `chef-platform-auth-cli node-account node enable-node`

```text
### Summary:
Use this operation to enable a disabled node. This will allow enabled services/agents
running on the node to communicate with the platform.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 409 (Conflict)
Expect this error code if the node is already enabled.

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node enable-node [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for enable-node
      --nodeId string     ID of a node
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli node-account node enable-role`

```text
### Summary:
Use this operation to enable a disabled node's role. This will allow enabled the role (services/agents)
running on the node to communicate with the platform.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 404 (Not Found)
The node's role you requested is not assigned to this role for the organization

#### 409 (Conflict)
Expect this error code if the node's role is already enabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node enable-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for enable-role
      --nodeId string     ID of a node
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --verbose           to show debug logs
```

## `chef-platform-auth-cli node-account node find-node`

```text
### Summary:
This method will allow users to search for nodes in the current organization.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node find-node [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for find-node
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --status string         The Status of the Node to filter by

      --verbose               to show debug logs
```

## `chef-platform-auth-cli node-account node get-node`

```text
### Summary:
Use this operation get an authorized node

#### Notes
This is not the same as loading a node from node management. This will only load the authorization information about a node

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node get-node [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-node
      --nodeId string    ID of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli node-account node get-nodeByRef`

```text
### Summary:
Use this operation get an authorized node using the node ref id that is present in node management

#### Notes
This is not the same as loading a node from node management. This will only load the authorization information about a node

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node get-nodeByRef [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-nodeByRef
      --profile string   name of the profile to be used for cmd (default "default")
      --refId string     ID of a node ref
      --verbose          to show debug logs
```

## `chef-platform-auth-cli node-account node get-role`

```text
### Summary:
Use this operation to get the details on an assigned node's role

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 404 (Not Found)
The role you requested is not assigned to this node in the organization

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node get-role [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-role
      --nodeId string    ID of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --roleId string    ID of a Role
      --verbose          to show debug logs
```

## `chef-platform-auth-cli node-account node list-roles`

```text
### Summary:
Use this operation to list all of a node's assigned roles

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node list-roles [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-roles
      --nodeId string    ID of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli node-account node register-node`

```text
### Summary:
This method will allow a new node to be registered with the platform. Registered nodes are allowed to have roles assigned to them.
Node assigned roles are then used by agents (on the node) to communicate with the platform.

**WARNING** registering a node will not by itself allow the node to communicate with the plaform. A role must be created for the node
for each service/agent running on that node.

#### Note:
1.) A registered node must FIRST existing in the node management catalog before being registered

---
### Process of manually enrolling a node:
1.) Node is added to node management database
2.) Register the node with the platform accounts system
3.) Assign the desired role (for the node)
4.) Force credential rotation
5.) Install the service/agent
6.) Configure the agent with the credentials from Step 4

#### Note:
Nodes can exist in Node Management that do not exist in the Identity (accounts) database, but no node may exist as an
identity that do not exist in node management

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 409 (Conflict)
Node is already registered

#### 410 (Gone)
The attempt to register the node failed as the node is a symbolic link in node management or has been removed

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node register-node [flags]

Examples:
- title: Default example
  example: |
    {
        "nodeRefId": "72eb2d77-89a4-45a6-ab00-73d222783f56",
        "type": "node"
    }


Flags:
      --body string             The node to be registerd
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for register-node
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli node-account node rotate-credentials`

```text
### Summary:
Use this operation to request a node to rotate it's credentials for a given role (agent/service).
Post this agents will automatically rotate their keys whenever a next request is made.

#### Notes:
1.) This operation allows the agent itself to rotate it's credentials it does not revoke credentials for a node.
2.) If a node has more than one role (service/agent) running on that node, this will only rotate the credentials for the supplied role


---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 404 (Not Found)
The node's role you requested is not assigned to this node for the organization

#### 409 (Conflict)
The requested node is disabled, please enable the node before requesting credential rotation

#### 409 (Conflict)
The requested role (agent/service) is disabled, please enable the node's role before requesting credential rotation

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 422 (Unprocessable Content)
The requested payload does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node rotate-credentials [flags]

Examples:
- title: Default example
  example: |
    {
        "comment": "Provide a reason for the credential rotation",
        "limit": 43800
    }


Flags:
      --If-Match string         If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string             Credentials rotation parameters
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for rotate-credentials
      --nodeId string           ID of a node
      --profile string          name of the profile to be used for cmd (default "default")
      --roleId string           ID of a Role
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli node-account node update-credentials`

```text
### Summary:
Use this operation to manually rotate credentials for a given node's role (agent/service).

**WARNING** Use this operation with extreme care, under normal cermunstatices a node will automaticly roate it's own credentials.
This should only be used if you have implemented a custom service or credential rotation process.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 404 (Not Found)
The node's role you requested is not assigned to this node for the organization

#### 409 (Conflict)
The requested node is disabled, please enable the node before requesting credential rotation

#### 409 (Conflict)
The requested role (agent/service) is disabled, please enable the node before requesting credential rotation

#### 409 (Conflict)
The requested role (agent/service) is not scheduled for a credential rotation. Request credential rotation first

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
The credentials for this role have already expired. Please delete the role, and re-provision.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node update-credentials [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for update-credentials
      --nodeId string    ID of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --roleId string    ID of a Role
      --verbose          to show debug logs
```

## `chef-platform-auth-cli node-account node verify-role`

```text
### Summary:
Use this operation to check to see if the node has this role assigned

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in the organization

#### 404 (Not Found)
The role you requested does not exist in the organization

#### 404 (Not Found)
The role you requested is not assigned to this node in the organization

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account node verify-role [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for verify-role
      --nodeId string    ID of a node
      --profile string   name of the profile to be used for cmd (default "default")
      --roleId string    ID of a Role
      --verbose          to show debug logs
```

## `chef-platform-auth-cli node-account self`

```text
Commands related to self

Usage:
  chef-platform-auth-cli node-account self [command]

Available Commands:
  rotate-credentials           Self credential rotation
  rotate-credentials-by-roleId Self credential rotation with provided roleId

Flags:
  -h, --help   help for self

Use "chef-platform-auth-cli node-account self [command] --help" for more information about a command.
```

## `chef-platform-auth-cli node-account self rotate-credentials`

```text
### Summary:
Use this operation to rotate credentials for the current node's role (agent/service).

**WARNING** Use this operation with extreme care, under normal circumstances a node will automaticly call this
method to rotate it's own credentials. This should only be used if you have implemented a custom service or credential rotation process.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in this organisation

#### 404 (Not Found)
The role requested does not exist in this organisation

#### 404 (Not Found)
The node's role you requested is not assigned to this node for the organization

#### 409 (Conflict)
The requested node is disabled, please enable the node before requesting credential rotation

#### 409 (Conflict)
The requested role (agent/service) is disabled, please enable the node before requesting credential rotation

#### 409 (Conflict)
The requested role (agent/service) is not scheduled for a credential rotation. Request credential rotation first

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 422 (Unprocessable Content)
The requested payload does not meet the validation requirements. Reference the errors in the response for more details.

#### 423 (Locked)
The credentials for this role have already expired. Please delete the role, and re-provision.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account self rotate-credentials [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for rotate-credentials
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli node-account self rotate-credentials-by-roleId`

```text
### Summary:
Response after rotating self credentials for a role.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The node you requested does not exist in this organisation

#### 404 (Not Found)
The role requested does not exist in this organisation

#### 404 (Not Found)
The node's role you requested is not assigned to this node for the organization

#### 409 (Conflict)
The requested node is disabled, please enable the node before requesting credential rotation

#### 409 (Conflict)
The requested role (agent/service) is disabled, please enable the node before requesting credential rotation

#### 409 (Conflict)
The requested role (agent/service) is not scheduled for a credential rotaton. Request credential rotation first

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, th.is error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 422 (Unprocessable Content)
The requested payload does not meet the validation requirements. Reference the errors in the response for more details.

#### 423 (Locked)
The credentials for this role have already expired. Please delete the role, and re-provision.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli node-account self rotate-credentials-by-roleId [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for rotate-credentials-by-roleId
      --profile string   name of the profile to be used for cmd (default "default")
      --roleId string    ID of a Role
      --verbose          to show debug logs
```

## `chef-platform-auth-cli notification`

```text
Commands for managing notifications

Usage:
  chef-platform-auth-cli notification [command]

Available Commands:
  notice           Commands related to notice
  provider         Commands related to provider
  providertype     Commands related to providertype
  schedule         Commands related to schedule
  selfchannel      Commands related to selfchannel
  selfschedule     Commands related to selfschedule
  selfsubscription Commands related to selfsubscription
  templatelibrary  Commands related to templatelibrary
  topic            Commands related to topic
  topictype        Commands related to topictype
  trigger          Commands related to trigger
  user             Commands related to user
  userchannel      Commands related to userchannel
  userschedule     Commands related to userschedule
  usersubscription Commands related to usersubscription

Flags:
  -h, --help   help for notification

Use "chef-platform-auth-cli notification [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification notice`

```text
Commands related to notice

Usage:
  chef-platform-auth-cli notification notice [command]

Available Commands:
  list-notices
  load-notice
  templates-list-templates
  templates-preview-template

Flags:
  -h, --help   help for notice

Use "chef-platform-auth-cli notification notice [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification notice list-notices`

```text
List all system defined notices

Usage:
  chef-platform-auth-cli notification notice list-notices [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-notices
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --tag strings
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification notice load-notice`

```text
Get a notice

Usage:
  chef-platform-auth-cli notification notice load-notice [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for load-notice
      --noticeId string
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli notification notice templates-list-templates`

```text
List all Notice templates

Usage:
  chef-platform-auth-cli notification notice templates-list-templates [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for templates-list-templates
      --noticeId string
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification notice templates-preview-template`

```text
Preview a template for a notice

Usage:
  chef-platform-auth-cli notification notice templates-preview-template [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for templates-preview-template
      --noticeId string
      --profile string       name of the profile to be used for cmd (default "default")
      --templateId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification provider`

```text
Commands related to provider

Usage:
  chef-platform-auth-cli notification provider [command]

Available Commands:
  commit-provider
  commit-provider-with-enable
  confirm-provider            Forcefully confirms a provider, bypassing the verification workflow. This should only be used in exceptional cases where the provider cannot go through the normal verification process, and it is necessary to confirm the provider to enable it for use. Use with caution, as this can lead to unverified providers being enabled.
  create-provider
  create-provider-with-id
  delete-provider
  disable-provider
  enable-provider
  get-test-status
  list-providers
  load-provider
  test-provider
  update-provider
  verify-provider

Flags:
  -h, --help   help for provider

Use "chef-platform-auth-cli notification provider [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification provider commit-provider`

```text
Commit Provider pending changes

Usage:
  chef-platform-auth-cli notification provider commit-provider [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for commit-provider
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification provider commit-provider-with-enable`

```text
Commit Provider pending changes with setting enabled

Usage:
  chef-platform-auth-cli notification provider commit-provider-with-enable [flags]

Flags:
      --enabled
      --format string       to print response in format (default "json")
  -h, --help                help for commit-provider-with-enable
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification provider confirm-provider`

```text
Confirm Provider

Usage:
  chef-platform-auth-cli notification provider confirm-provider [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for confirm-provider
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification provider create-provider`

```text
Creates a channel provider

Usage:
  chef-platform-auth-cli notification provider create-provider [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-provider
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification provider create-provider-with-id`

```text
Create a channel provider with a specific ID

Usage:
  chef-platform-auth-cli notification provider create-provider-with-id [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-provider-with-id
      --profile string       name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification provider delete-provider`

```text
Delete a channel provider

Usage:
  chef-platform-auth-cli notification provider delete-provider [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for delete-provider
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification provider disable-provider`

```text
Disable a channel provider

Usage:
  chef-platform-auth-cli notification provider disable-provider [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for disable-provider
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification provider enable-provider`

```text
Enable a channel provider

Usage:
  chef-platform-auth-cli notification provider enable-provider [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for enable-provider
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification provider get-test-status`

```text
Get the status of a test message sent to a channel provider

Usage:
  chef-platform-auth-cli notification provider get-test-status [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for get-test-status
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification provider list-providers`

```text
Get all configured channel providers

Usage:
  chef-platform-auth-cli notification provider list-providers [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-providers
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --type string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification provider load-provider`

```text
Loads a single channel provider

Usage:
  chef-platform-auth-cli notification provider load-provider [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for load-provider
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification provider test-provider`

```text
Send a test message to a channel provider

Usage:
  chef-platform-auth-cli notification provider test-provider [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for test-provider
      --profile string       name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification provider update-provider`

```text
Updates a channel provider

Usage:
  chef-platform-auth-cli notification provider update-provider [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-provider
      --profile string       name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification provider verify-provider`

```text
Verifies a test message sent to a channel provider

Usage:
  chef-platform-auth-cli notification provider verify-provider [flags]

Flags:
      --code string
      --format string       to print response in format (default "json")
  -h, --help                help for verify-provider
      --profile string      name of the profile to be used for cmd (default "default")
      --providerId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification providertype`

```text
Commands related to providertype

Usage:
  chef-platform-auth-cli notification providertype [command]

Available Commands:
  list-provider-types
  list-service-types
  load-provider-type

Flags:
  -h, --help   help for providertype

Use "chef-platform-auth-cli notification providertype [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification providertype list-provider-types`

```text
Get all system provider types

Usage:
  chef-platform-auth-cli notification providertype list-provider-types [flags]

Flags:
      --format string        to print response in format (default "json")
  -h, --help                 help for list-provider-types
      --profile string       name of the profile to be used for cmd (default "default")
      --serviceType string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification providertype list-service-types`

```text
Get all system service types

Usage:
  chef-platform-auth-cli notification providertype list-service-types [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-service-types
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli notification providertype load-provider-type`

```text
Get a system defined provider-type

Usage:
  chef-platform-auth-cli notification providertype load-provider-type [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for load-provider-type
      --profile string          name of the profile to be used for cmd (default "default")
      --providerTypeId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification schedule`

```text
Commands related to schedule

Usage:
  chef-platform-auth-cli notification schedule [command]

Available Commands:
  create-schedule       Create a new schedule
  delete-schedule       Delete a schedule
  disable-schedule      Disable a schedule
  disable-schedule-rule Disable specific rules within a schedule
  enable-schedule       Enable a schedule
  enable-schedule-rule  Enable specific rules within a schedule
  list-schedules        Get all schedules for the current tenant with pagination
  load-schedule         Get a specific schedule by ID
  update-schedule       Update an existing schedule

Flags:
  -h, --help   help for schedule

Use "chef-platform-auth-cli notification schedule [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification schedule create-schedule`

```text
Create a new schedule

Usage:
  chef-platform-auth-cli notification schedule create-schedule [flags]

Flags:
      --body string          Schedule creation details
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-schedule
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification schedule delete-schedule`

```text
Delete a schedule

Usage:
  chef-platform-auth-cli notification schedule delete-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for delete-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification schedule disable-schedule`

```text
Disable a schedule

Usage:
  chef-platform-auth-cli notification schedule disable-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for disable-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification schedule disable-schedule-rule`

```text
Disable specific rules within a schedule

Usage:
  chef-platform-auth-cli notification schedule disable-schedule-rule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for disable-schedule-rule
      --profile string      name of the profile to be used for cmd (default "default")
      --ruleName string     The rule name
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification schedule enable-schedule`

```text
Enable a schedule

Usage:
  chef-platform-auth-cli notification schedule enable-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for enable-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification schedule enable-schedule-rule`

```text
Enable specific rules within a schedule

Usage:
  chef-platform-auth-cli notification schedule enable-schedule-rule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for enable-schedule-rule
      --profile string      name of the profile to be used for cmd (default "default")
      --ruleName string     The rule name
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification schedule list-schedules`

```text
Get all schedules for the current tenant with pagination

Usage:
  chef-platform-auth-cli notification schedule list-schedules [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-schedules
      --pagination.page int32   Page number (starting from 1)
      --pagination.size int32   Number of items per page
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification schedule load-schedule`

```text
Get a specific schedule by ID

Usage:
  chef-platform-auth-cli notification schedule load-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for load-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification schedule update-schedule`

```text
Update an existing schedule

Usage:
  chef-platform-auth-cli notification schedule update-schedule [flags]

Flags:
      --body string          Schedule update details
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-schedule
      --profile string       name of the profile to be used for cmd (default "default")
      --scheduleId string    The schedule ID
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification selfchannel`

```text
Commands related to selfchannel

Usage:
  chef-platform-auth-cli notification selfchannel [command]

Available Commands:
  create-channel
  delete-channel
  disable-channel
  enable-channel
  list-channels
  load-channel
  test-user-channel
  test-user-channel-status
  test-user-channel-verify
  update-channel

Flags:
  -h, --help   help for selfchannel

Use "chef-platform-auth-cli notification selfchannel [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification selfchannel create-channel`

```text
Create a new channel for the current user

Usage:
  chef-platform-auth-cli notification selfchannel create-channel [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-channel
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification selfchannel delete-channel`

```text
Delete a channel for the current user

Usage:
  chef-platform-auth-cli notification selfchannel delete-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for delete-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification selfchannel disable-channel`

```text
Disable a channel for the current user

Usage:
  chef-platform-auth-cli notification selfchannel disable-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for disable-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification selfchannel enable-channel`

```text
Enable a channel for the current user

Usage:
  chef-platform-auth-cli notification selfchannel enable-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for enable-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification selfchannel list-channels`

```text
Get all channels for the current user

Usage:
  chef-platform-auth-cli notification selfchannel list-channels [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-channels
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --type string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification selfchannel load-channel`

```text
Get a specific channel for the current user

Usage:
  chef-platform-auth-cli notification selfchannel load-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for load-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification selfchannel test-user-channel`

```text
Sends a test message to a user channel

Usage:
  chef-platform-auth-cli notification selfchannel test-user-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for test-user-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification selfchannel test-user-channel-status`

```text
Returns the status of a test message sent to a channel

Usage:
  chef-platform-auth-cli notification selfchannel test-user-channel-status [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for test-user-channel-status
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification selfchannel test-user-channel-verify`

```text
Verifies a test message sent to a user channel

Usage:
  chef-platform-auth-cli notification selfchannel test-user-channel-verify [flags]

Flags:
      --channelId string
      --code string
      --format string      to print response in format (default "json")
  -h, --help               help for test-user-channel-verify
      --profile string     name of the profile to be used for cmd (default "default")
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification selfchannel update-channel`

```text
Update a channel for the current user

Usage:
  chef-platform-auth-cli notification selfchannel update-channel [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --channelId string
      --format string        to print response in format (default "json")
  -h, --help                 help for update-channel
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification selfschedule`

```text
Commands related to selfschedule

Usage:
  chef-platform-auth-cli notification selfschedule [command]

Available Commands:
  create-schedule       Create a new schedule
  delete-schedule       Delete a schedule
  disable-schedule      Disable a schedule
  disable-schedule-rule Disable specific rules within a schedule
  enable-schedule       Enable a schedule
  enable-schedule-rule  Enable specific rules within a schedule
  list-schedules        Get all schedules for the current tenant with pagination
  load-schedule         Get a specific schedule by ID
  update-schedule       Update an existing schedule

Flags:
  -h, --help   help for selfschedule

Use "chef-platform-auth-cli notification selfschedule [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification selfschedule create-schedule`

```text
Create a new schedule for the current user

Usage:
  chef-platform-auth-cli notification selfschedule create-schedule [flags]

Flags:
      --body string          Schedule creation details
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-schedule
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification selfschedule delete-schedule`

```text
Delete a schedule for the current user

Usage:
  chef-platform-auth-cli notification selfschedule delete-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for delete-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification selfschedule disable-schedule`

```text
Disable a schedule for the current user

Usage:
  chef-platform-auth-cli notification selfschedule disable-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for disable-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification selfschedule disable-schedule-rule`

```text
Disable specific rules within a user's schedule

Usage:
  chef-platform-auth-cli notification selfschedule disable-schedule-rule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for disable-schedule-rule
      --profile string      name of the profile to be used for cmd (default "default")
      --ruleName string     The rule name
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification selfschedule enable-schedule`

```text
Enable a schedule for the current user

Usage:
  chef-platform-auth-cli notification selfschedule enable-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for enable-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification selfschedule enable-schedule-rule`

```text
Enable specific rules within a user's schedule

Usage:
  chef-platform-auth-cli notification selfschedule enable-schedule-rule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for enable-schedule-rule
      --profile string      name of the profile to be used for cmd (default "default")
      --ruleName string     The rule name
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification selfschedule list-schedules`

```text
Get all schedules for the current user

Usage:
  chef-platform-auth-cli notification selfschedule list-schedules [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-schedules
      --pagination.page int32   Page number (starting from 1)
      --pagination.size int32   Number of items per page
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification selfschedule load-schedule`

```text
Get a specific schedule for the current user

Usage:
  chef-platform-auth-cli notification selfschedule load-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for load-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification selfschedule update-schedule`

```text
Update a schedule for the current user

Usage:
  chef-platform-auth-cli notification selfschedule update-schedule [flags]

Flags:
      --body string          Schedule update details
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-schedule
      --profile string       name of the profile to be used for cmd (default "default")
      --scheduleId string    The schedule ID
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification selfsubscription`

```text
Commands related to selfsubscription

Usage:
  chef-platform-auth-cli notification selfsubscription [command]

Available Commands:
  create-subscription
  delete-subscription
  disable-subscription
  enable-subscription
  list-subscriptions
  load-subscription
  update-subscription

Flags:
  -h, --help   help for selfsubscription

Use "chef-platform-auth-cli notification selfsubscription [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification selfsubscription create-subscription`

```text
Create a new subscription for the current user

Usage:
  chef-platform-auth-cli notification selfsubscription create-subscription [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-subscription
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification selfsubscription delete-subscription`

```text
Delete a subscription for the current user

Usage:
  chef-platform-auth-cli notification selfsubscription delete-subscription [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for delete-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification selfsubscription disable-subscription`

```text
Disable a subscription for the current user

Usage:
  chef-platform-auth-cli notification selfsubscription disable-subscription [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for disable-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification selfsubscription enable-subscription`

```text
Enable a subscription for the current user

Usage:
  chef-platform-auth-cli notification selfsubscription enable-subscription [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for enable-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification selfsubscription list-subscriptions`

```text
Get all subscriptions for the current user

Usage:
  chef-platform-auth-cli notification selfsubscription list-subscriptions [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-subscriptions
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --type string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification selfsubscription load-subscription`

```text
Load a subscription for the current user

Usage:
  chef-platform-auth-cli notification selfsubscription load-subscription [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for load-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification selfsubscription update-subscription`

```text
Update a subscription for the current user

Usage:
  chef-platform-auth-cli notification selfsubscription update-subscription [flags]

Flags:
      --body string
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for update-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification templatelibrary`

```text
Commands related to templatelibrary

Usage:
  chef-platform-auth-cli notification templatelibrary [command]

Available Commands:
  create-template
  delete-template
  list-templates
  load-template
  preview-template
  render-template
  update-template

Flags:
  -h, --help   help for templatelibrary

Use "chef-platform-auth-cli notification templatelibrary [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification templatelibrary create-template`

```text
Create a new template

Usage:
  chef-platform-auth-cli notification templatelibrary create-template [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-template
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification templatelibrary delete-template`

```text
Delete a template

Usage:
  chef-platform-auth-cli notification templatelibrary delete-template [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for delete-template
      --profile string      name of the profile to be used for cmd (default "default")
      --templateId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification templatelibrary list-templates`

```text
List all templates

Usage:
  chef-platform-auth-cli notification templatelibrary list-templates [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-templates
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification templatelibrary load-template`

```text
Get a template

Usage:
  chef-platform-auth-cli notification templatelibrary load-template [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for load-template
      --profile string      name of the profile to be used for cmd (default "default")
      --templateId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification templatelibrary preview-template`

```text
Preview a template

Usage:
  chef-platform-auth-cli notification templatelibrary preview-template [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for preview-template
      --profile string       name of the profile to be used for cmd (default "default")
      --templateId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification templatelibrary render-template`

```text
Render a raw template

Usage:
  chef-platform-auth-cli notification templatelibrary render-template [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for render-template
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification templatelibrary update-template`

```text
Update a template

Usage:
  chef-platform-auth-cli notification templatelibrary update-template [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-template
      --profile string       name of the profile to be used for cmd (default "default")
      --templateId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification topic`

```text
Commands related to topic

Usage:
  chef-platform-auth-cli notification topic [command]

Available Commands:
  create-topic
  delete-topic
  disable-topic
  enable-topic
  list-topics
  load-topic
  templates-list-templates
  templates-preview-template
  update-topic

Flags:
  -h, --help   help for topic

Use "chef-platform-auth-cli notification topic [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification topic create-topic`

```text
Creates a topic

Usage:
  chef-platform-auth-cli notification topic create-topic [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-topic
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification topic delete-topic`

```text
Delete a topic

Usage:
  chef-platform-auth-cli notification topic delete-topic [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for delete-topic
      --profile string   name of the profile to be used for cmd (default "default")
      --topicId string
      --verbose          to show debug logs
```

## `chef-platform-auth-cli notification topic disable-topic`

```text
Disable a topic

Usage:
  chef-platform-auth-cli notification topic disable-topic [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for disable-topic
      --profile string   name of the profile to be used for cmd (default "default")
      --topicId string
      --verbose          to show debug logs
```

## `chef-platform-auth-cli notification topic enable-topic`

```text
Enable a topic

Usage:
  chef-platform-auth-cli notification topic enable-topic [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for enable-topic
      --profile string   name of the profile to be used for cmd (default "default")
      --topicId string
      --verbose          to show debug logs
```

## `chef-platform-auth-cli notification topic list-topics`

```text
Get all configured topics

Usage:
  chef-platform-auth-cli notification topic list-topics [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-topics
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --tag strings
      --type string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification topic load-topic`

```text
Load a topic

Usage:
  chef-platform-auth-cli notification topic load-topic [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for load-topic
      --profile string   name of the profile to be used for cmd (default "default")
      --topicId string
      --verbose          to show debug logs
```

## `chef-platform-auth-cli notification topic templates-list-templates`

```text
List templates for a topic

Usage:
  chef-platform-auth-cli notification topic templates-list-templates [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for templates-list-templates
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --topicId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification topic templates-preview-template`

```text
Preview a template for a topic

Usage:
  chef-platform-auth-cli notification topic templates-preview-template [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for templates-preview-template
      --profile string       name of the profile to be used for cmd (default "default")
      --templateId string
      --topicId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification topic update-topic`

```text
Update a topic

Usage:
  chef-platform-auth-cli notification topic update-topic [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-topic
      --profile string       name of the profile to be used for cmd (default "default")
      --topicId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification topictype`

```text
Commands related to topictype

Usage:
  chef-platform-auth-cli notification topictype [command]

Available Commands:
  list-topic-types
  load-topic-type

Flags:
  -h, --help   help for topictype

Use "chef-platform-auth-cli notification topictype [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification topictype list-topic-types`

```text
Get all system topic types

Usage:
  chef-platform-auth-cli notification topictype list-topic-types [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-topic-types
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --tag strings
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification topictype load-topic-type`

```text
Get a system defined topic-type

Usage:
  chef-platform-auth-cli notification topictype load-topic-type [flags]

Flags:
      --format string        to print response in format (default "json")
  -h, --help                 help for load-topic-type
      --profile string       name of the profile to be used for cmd (default "default")
      --topicTypeId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification trigger`

```text
Commands related to trigger

Usage:
  chef-platform-auth-cli notification trigger [command]

Available Commands:
  list-trigger-history Returns trigger history data for the specified trigger ID
  trigger-topic
  trigger-topic-type

Flags:
  -h, --help   help for trigger

Use "chef-platform-auth-cli notification trigger [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification trigger list-trigger-history`

```text
Get trigger history for a specific trigger

Usage:
  chef-platform-auth-cli notification trigger list-trigger-history [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-trigger-history
      --orgChannelId string
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --sortAscending
      --status string
      --triggerId string
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification trigger trigger-topic`

```text
Trigger a topic workflow

Usage:
  chef-platform-auth-cli notification trigger trigger-topic [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for trigger-topic
      --profile string       name of the profile to be used for cmd (default "default")
      --topicId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification trigger trigger-topic-type`

```text
Trigger a topic-type workflow

Usage:
  chef-platform-auth-cli notification trigger trigger-topic-type [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for trigger-topic-type
      --profile string       name of the profile to be used for cmd (default "default")
      --topicTypeId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification user`

```text
Commands related to user

Usage:
  chef-platform-auth-cli notification user [command]

Available Commands:
  delete-user

Flags:
  -h, --help   help for user

Use "chef-platform-auth-cli notification user [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification user delete-user`

```text
Delete a user and all associated data

Usage:
  chef-platform-auth-cli notification user delete-user [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for delete-user
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string
      --verbose          to show debug logs
```

## `chef-platform-auth-cli notification userchannel`

```text
Commands related to userchannel

Usage:
  chef-platform-auth-cli notification userchannel [command]

Available Commands:
  create-channel
  delete-channel
  disable-channel
  enable-channel
  list-channels
  load-channel
  test-user-channel
  test-user-channel-status
  test-user-channel-verify
  update-channel

Flags:
  -h, --help   help for userchannel

Use "chef-platform-auth-cli notification userchannel [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification userchannel create-channel`

```text
Create a new channel for the specified user

Usage:
  chef-platform-auth-cli notification userchannel create-channel [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-channel
      --profile string       name of the profile to be used for cmd (default "default")
      --userId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification userchannel delete-channel`

```text
Delete a channel for the specified user

Usage:
  chef-platform-auth-cli notification userchannel delete-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for delete-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --userId string
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification userchannel disable-channel`

```text
Disable a channel for the specified user

Usage:
  chef-platform-auth-cli notification userchannel disable-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for disable-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --userId string
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification userchannel enable-channel`

```text
Enable a channel for the specified user

Usage:
  chef-platform-auth-cli notification userchannel enable-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for enable-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --userId string
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification userchannel list-channels`

```text
Get all channels for the specified user

Usage:
  chef-platform-auth-cli notification userchannel list-channels [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-channels
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --type string
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification userchannel load-channel`

```text
Get a channel for the specified user

Usage:
  chef-platform-auth-cli notification userchannel load-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for load-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --userId string
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification userchannel test-user-channel`

```text
Send a test message to a user channel

Usage:
  chef-platform-auth-cli notification userchannel test-user-channel [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for test-user-channel
      --profile string     name of the profile to be used for cmd (default "default")
      --userId string
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification userchannel test-user-channel-status`

```text
Get the status of a test message for a user channel

Usage:
  chef-platform-auth-cli notification userchannel test-user-channel-status [flags]

Flags:
      --channelId string
      --format string      to print response in format (default "json")
  -h, --help               help for test-user-channel-status
      --profile string     name of the profile to be used for cmd (default "default")
      --userId string
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification userchannel test-user-channel-verify`

```text
Verifies a test message sent to a user channel

Usage:
  chef-platform-auth-cli notification userchannel test-user-channel-verify [flags]

Flags:
      --channelId string
      --code string
      --format string      to print response in format (default "json")
  -h, --help               help for test-user-channel-verify
      --profile string     name of the profile to be used for cmd (default "default")
      --userId string
      --verbose            to show debug logs
```

## `chef-platform-auth-cli notification userchannel update-channel`

```text
Update a channel for the specified user

Usage:
  chef-platform-auth-cli notification userchannel update-channel [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --channelId string
      --format string        to print response in format (default "json")
  -h, --help                 help for update-channel
      --profile string       name of the profile to be used for cmd (default "default")
      --userId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification userschedule`

```text
Commands related to userschedule

Usage:
  chef-platform-auth-cli notification userschedule [command]

Available Commands:
  create-user-schedule       Create a new schedule
  delete-user-schedule       Delete a schedule
  disable-user-schedule      Disable a schedule
  disable-user-schedule-rule Disable specific rules within a schedule
  enable-user-schedule       Enable a schedule
  enable-user-schedule-rule  Enable specific rules within a schedule
  list-user-schedules        Get all schedules for the current tenant with pagination
  load-user-schedule         Get a specific schedule by ID
  update-user-schedule       Update an existing schedule

Flags:
  -h, --help   help for userschedule

Use "chef-platform-auth-cli notification userschedule [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification userschedule create-user-schedule`

```text
Create a new schedule for a specific user (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule create-user-schedule [flags]

Flags:
      --body string          Schedule creation details
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-user-schedule
      --profile string       name of the profile to be used for cmd (default "default")
      --userId string        User ID to associate the schedule with
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification userschedule delete-user-schedule`

```text
Delete a schedule for a user (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule delete-user-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for delete-user-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --userId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification userschedule disable-user-schedule`

```text
Disable a schedule for a user (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule disable-user-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for disable-user-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --userId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification userschedule disable-user-schedule-rule`

```text
Disable specific rules within a user's schedule (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule disable-user-schedule-rule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for disable-user-schedule-rule
      --profile string      name of the profile to be used for cmd (default "default")
      --ruleName string     The rule name
      --scheduleId string   The schedule ID
      --userId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification userschedule enable-user-schedule`

```text
Enable a schedule for a user (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule enable-user-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for enable-user-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --userId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification userschedule enable-user-schedule-rule`

```text
Enable specific rules within a user's schedule (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule enable-user-schedule-rule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for enable-user-schedule-rule
      --profile string      name of the profile to be used for cmd (default "default")
      --ruleName string     The rule name
      --scheduleId string   The schedule ID
      --userId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification userschedule list-user-schedules`

```text
Get all schedules for a specific user (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule list-user-schedules [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-user-schedules
      --pagination.page int32   Page number (starting from 1)
      --pagination.size int32   Number of items per page
      --profile string          name of the profile to be used for cmd (default "default")
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification userschedule load-user-schedule`

```text
Get a specific schedule for a user (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule load-user-schedule [flags]

Flags:
      --format string       to print response in format (default "json")
  -h, --help                help for load-user-schedule
      --profile string      name of the profile to be used for cmd (default "default")
      --scheduleId string   The schedule ID
      --userId string
      --verbose             to show debug logs
```

## `chef-platform-auth-cli notification userschedule update-user-schedule`

```text
Update a schedule for a user (admin operation)

Usage:
  chef-platform-auth-cli notification userschedule update-user-schedule [flags]

Flags:
      --body string          Schedule update details
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-user-schedule
      --profile string       name of the profile to be used for cmd (default "default")
      --scheduleId string    The schedule ID
      --userId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification usersubscription`

```text
Commands related to usersubscription

Usage:
  chef-platform-auth-cli notification usersubscription [command]

Available Commands:
  create-subscription
  delete-subscription
  disable-subscription
  enable-subscription
  list-subscriptions
  load-subscription
  update-subscription

Flags:
  -h, --help   help for usersubscription

Use "chef-platform-auth-cli notification usersubscription [command] --help" for more information about a command.
```

## `chef-platform-auth-cli notification usersubscription create-subscription`

```text
Create a new subscription for the specified user

Usage:
  chef-platform-auth-cli notification usersubscription create-subscription [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-subscription
      --profile string       name of the profile to be used for cmd (default "default")
      --userId string
      --verbose              to show debug logs
```

## `chef-platform-auth-cli notification usersubscription delete-subscription`

```text
Delete a subscription for the specified user

Usage:
  chef-platform-auth-cli notification usersubscription delete-subscription [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for delete-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification usersubscription disable-subscription`

```text
Disable a subscription for the specified user

Usage:
  chef-platform-auth-cli notification usersubscription disable-subscription [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for disable-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification usersubscription enable-subscription`

```text
Enable a subscription for the specified user

Usage:
  chef-platform-auth-cli notification usersubscription enable-subscription [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for enable-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification usersubscription list-subscriptions`

```text
List all subscriptions for the specified user

Usage:
  chef-platform-auth-cli notification usersubscription list-subscriptions [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-subscriptions
      --pagination.page int32
      --pagination.size int32
      --profile string          name of the profile to be used for cmd (default "default")
      --type string
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification usersubscription load-subscription`

```text
Load a subscription for the specified user

Usage:
  chef-platform-auth-cli notification usersubscription load-subscription [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for load-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli notification usersubscription update-subscription`

```text
Update a subscription for the specified user

Usage:
  chef-platform-auth-cli notification usersubscription update-subscription [flags]

Flags:
      --body string
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for update-subscription
      --profile string          name of the profile to be used for cmd (default "default")
      --subscriptionId string
      --userId string
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli register-device`

```text
register a device and get api-token for device login

Usage:
  chef-platform-auth-cli register-device [flags]

Flags:
      --cafile string         path to ca file
      --device-name string    name of the device (default "device")
  -f, --format string         to print response in format (default "json")
  -h, --help                  help for register-device
  -i, --insecure              insecure skip verify
      --overwrite             this flag is used to overwrite the old profile if the profile for profile name provided already exists
      --profile-name string   name for the profile of the new api-token
      --url string            tenant url
  -v, --verbose               to show debug logs
```

## `chef-platform-auth-cli set-default-profile`

```text
Set default profile for request

Usage:
  chef-platform-auth-cli set-default-profile [flags]

Flags:
  -h, --help   help for set-default-profile
```

## `chef-platform-auth-cli system`

```text
Commands for managing system like org or tenant

Usage:
  chef-platform-auth-cli system [command]

Available Commands:
  organization Commands related to organization
  tenant       Commands related to tenant

Flags:
  -h, --help   help for system

Use "chef-platform-auth-cli system [command] --help" for more information about a command.
```

## `chef-platform-auth-cli system organization`

```text
Commands related to organization

Usage:
  chef-platform-auth-cli system organization [command]

Available Commands:
  create-dsm-organization         Add DSM organization
  create-organization             CREATE a organization
  disable-organization            DISABLE a organization
  enable-organization             ENABLE a organization
  get-organization                READ a organization
  list-org-defaults               Get organization job defaults
  list-organizations              LIST all organizations in the current logged in tenant
  my-organization                 GET the organization of the current logged in user
  my-organization-defaults-status GET the defaults status of the organization of the current logged in user
  organization-defaults-creation  create defaults for an existing organization
  update-organization             UPDATE a organization
  upsert-org-defaults             Update organization job default (UPSERT)
  verify-organization             VERIFY a organization

Flags:
  -h, --help   help for organization

Use "chef-platform-auth-cli system organization [command] --help" for more information about a command.
```

## `chef-platform-auth-cli system organization create-dsm-organization`

```text
Use this API to create a DSM organization and adding an admin user if the initial attempt fails.
This endpoint allows users to re-attempt creating the DSM organization and adding the admin user (platform user) to the organization.
If a '409' response is received during organization creation, it will be logged and ignored, allowing remaining steps to be applied.
---     ### Errors
#### 400 (Bad Request) Malformed-Request
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 404 (Not Found) The organization does not exist
#### 422 (Unprocessable Content) The requested payload does not meet the validation requirements. Reference the errors in the response for more details.
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization create-dsm-organization [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for create-dsm-organization
      --orgId string      ID of an Organizational Unit
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli system organization create-organization`

```text
Creates a new Organizational Unit in the current logged in tenant. The current user will be assigned the default admin role in the new OU.
---     ### Errors
#### 400 (Bad Request) Malformed-Request
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 409 (Conflict) Expect this error code if organization with same name already exist.
#### 422 (Unprocessable Content) The requested payload does not meet the validation requirements. Reference the errors in the response for more details.
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization create-organization [flags]

Examples:
- title: orgCreationWithDefaults
  example: |
    {
        "createDefaultSkillAssembly": true,
        "createDefaults": true,
        "description": "First OU",
        "name": "MyOU"
    }
- title: orgCreationWithoutDefaults
  example: |
    {
        "createDefaultSkillAssembly": false,
        "createDefaults": false,
        "description": "Second OU",
        "name": "MyOU"
    }


Flags:
      --body string             The organization to be created
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for create-organization
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli system organization disable-organization`

```text
Disable a currently enabled organization

**WARNING**
Disable OU will stop all existing sessions. All nodes under the OU will not be able to communicate with platform. Users won't be able to login

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The organization does not exist

#### 409 (Conflict)
Expect this error code if the organization is already disabled.

#### 422 (Unprocessable Content)
The requested payload does not meet the validation requirements. Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization disable-organization [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for disable-organization
      --orgId string      ID of an Organizational Unit
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli system organization enable-organization`

```text
Enable a currently disabled organization
---     ### Errors
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 404 (Not Found) The organization does not exist
#### 409 (Conflict) Expect this error code if the organization is already enabled.
#### 422 (Unprocessable Content) The requested payload does not meet the validation requirements. Reference the errors in the response for more details.
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization enable-organization [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for enable-organization
      --orgId string      ID of an Organizational Unit
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli system organization get-organization`

```text
Use this api to get an organization details
---     ### Errors
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 404 (Not Found) The organization does not exist
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization get-organization [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-organization
      --orgId string     ID of an Organizational Unit
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli system organization list-org-defaults`

```text
Retrieve organization-specific job default configurations.
Returns a list of configuration items for the current organization context.

Usage:
  chef-platform-auth-cli system organization list-org-defaults [flags]

Flags:
      --defaultsType string   Type of defaults to retrieve (currently only job_defaults supported)
      --format string         to print response in format (default "json")
  -h, --help                  help for list-org-defaults
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-platform-auth-cli system organization list-organizations`

```text
This method will list all organizational units in the current logged in tenant;
 optionally filtered by organizational units status and/or organization name (indicated in the query).

### Filtering Options - **status**: Filter by organization status (enabled, disabled, etc.) - **orgName**: Filter by organization name

### Default Ordering Organizations are sorted by the updated_at field in descending order. This default ordering ensures consistency across requests, which is especially important for reliable CSV exports and accurate pagination.
 ---
### Errors
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization list-organizations [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for list-organizations
      --orgName string        Filter organizations by name.

      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --status string         The Status to filter the Organization list by

      --verbose               to show debug logs
```

## `chef-platform-auth-cli system organization my-organization`

```text
This method will get organizational unit details of the current logged in user

 ---
 ### Errors

 #### 401 (Unauthorised)
 The credential used to access this resource is invalid/expired

 #### 403 (Forbidden)
 User with this role is not allowed to access the resource

 #### 500 (Internal Server Error)
 Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization my-organization [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for my-organization
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli system organization my-organization-defaults-status`

```text
This method will get default status of organizational unit of the current logged in user

 ---
 ### Errors

 #### 401 (Unauthorised)
 The credential used to access this resource is invalid/expired

 #### 403 (Forbidden)
 User with this role is not allowed to access the resource

 #### 500 (Internal Server Error)
 Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization my-organization-defaults-status [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for my-organization-defaults-status
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli system organization organization-defaults-creation`

```text
This api can be used to create the defaults for an existing organization.
---     ### Errors
#### 400 (Bad Request) Malformed-Request
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 404 (Not Found) The organization does not exist
#### 422 (Unprocessable Content) The requested payload does not meet the validation requirements. Reference the errors in the response for more details.
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization organization-defaults-creation [flags]

Examples:
- title: Default example
  example: |
    {
        "createDefaultSkillAssembly": true
    }


Flags:
      --If-Match string         If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for organization-defaults-creation
      --orgId string            ID of an Organizational Unit
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli system organization update-organization`

```text
This api can be used to update OU details.
---     ### Errors
#### 400 (Bad Request) Malformed-Request
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 404 (Not Found) The organization does not exist
#### 422 (Unprocessable Content) The requested payload does not meet the validation requirements. Reference the errors in the response for more details.
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization update-organization [flags]

Examples:
- title: Default example
  example: |
    {
        "description": "First OU",
        "name": "MyOU"
    }


Flags:
      --If-Match string         If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string             The organization to be updated
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for update-organization
      --orgId string            ID of an Organizational Unit
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli system organization upsert-org-defaults`

```text
Create or update a single job default configuration for the current organization context.
This endpoint supports UPSERT functionality:
- If the configuration key doesn't exist, it will be created
- If the configuration key exists, it will be updated
- If the value is omitted or empty, the default will be cleared/reset

Usage:
  chef-platform-auth-cli system organization upsert-org-defaults [flags]

Examples:
- title: Default example
  example: |
    {
        "key": "execution_type",
        "type": "job_defaults",
        "value": "parallel"
    }


Flags:
      --body string
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for upsert-org-defaults
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli system organization verify-organization`

```text
Verify if the organization is enabled.
---     ### Errors
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 404 (Not Found) The organization does not exist
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system organization verify-organization [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for verify-organization
      --orgId string     ID of an Organizational Unit
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli system tenant`

```text
Commands related to tenant

Usage:
  chef-platform-auth-cli system tenant [command]

Available Commands:
  get-system-preferences   GET system preferences for a tenant
  get-tenant-root-ca       GET the root CA certificate of the tenant
  upsert-system-preference Upsert System Preference

Flags:
  -h, --help   help for tenant

Use "chef-platform-auth-cli system tenant [command] --help" for more information about a command.
```

## `chef-platform-auth-cli system tenant get-system-preferences`

```text
Retrieve all system preferences for a given tenant. System preferences include feature flags and configuration settings that control functionality. Each preference includes an organization ID indicating whether it applies at the tenant level or to a specific organization within the tenant.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid or expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The tenant does not exist

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system tenant get-system-preferences [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-system-preferences
      --profile string    name of the profile to be used for cmd (default "default")
      --tenantId string   ID of Tenant
      --verbose           to show debug logs
```

## `chef-platform-auth-cli system tenant get-tenant-root-ca`

```text
Use this API to get the root CA certificate for a tenant.

---
### Errors

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system tenant get-tenant-root-ca [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-tenant-root-ca
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli system tenant upsert-system-preference`

```text
Create or update a system preference for a tenant. This operation performs an upsert that matches an existing preference using the tenant identifier from the tenantId path parameter and the featureKey provided in the request body. Any internal organization identifier used by the service is derived from the tenant context and does not need to be supplied by the caller.
--- ### Errors
#### 400 (Bad Request) Malformed-Request
#### 401 (Unauthorised) The credential used to access this resource is invalid/expired
#### 403 (Forbidden) User with this role is not allowed to access the resource
#### 422 (Unprocessable Content) The requested payload does not meet the validation requirements. Reference the errors in the response for more details.
#### 500 (Internal Server Error) Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli system tenant upsert-system-preference [flags]

Examples:
- title: arrayPreference
  summary: Array preference example
  example: |
    {
        "featureKey": "allowed_ips",
        "featureValue": [
            "192.168.1.1",
            "192.168.1.2",
            "10.0.0.1"
        ],
        "valueType": "array"
    }
- title: booleanPreference
  summary: Boolean preference example
  example: |
    {
        "featureKey": "enable_advanced_features",
        "featureValue": true,
        "valueType": "boolean"
    }
- title: numberPreference
  summary: Number preference example
  example: |
    {
        "featureKey": "max_connections",
        "featureValue": 100,
        "valueType": "number"
    }
- title: objectPreference
  summary: Object preference example
  example: |
    {
        "featureKey": "ui_settings",
        "featureValue": {
            "refresh_interval": 30,
            "show_notifications": true,
            "sidebar_position": "left"
        },
        "valueType": "object"
    }
- title: stringPreference
  summary: String preference example
  example: |
    {
        "featureKey": "default_theme",
        "featureValue": "dark",
        "valueType": "string"
    }


Flags:
      --body string             The system preference to be created or updated
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for upsert-system-preference
      --profile string          name of the profile to be used for cmd (default "default")
      --tenantId string         ID of Tenant
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account`

```text
Commands for managing user account

Usage:
  chef-platform-auth-cli user-account [command]

Available Commands:
  applicationkey Commands related to applicationkey
  identity       Commands related to identity
  jwt            Commands related to jwt
  self           Commands related to self
  user           Commands related to user

Flags:
  -h, --help   help for user-account

Use "chef-platform-auth-cli user-account [command] --help" for more information about a command.
```

## `chef-platform-auth-cli user-account applicationkey`

```text
Commands related to applicationkey

Usage:
  chef-platform-auth-cli user-account applicationkey [command]

Available Commands:
  create-applicationKey  Create application key for new node enrollment
  delete-applicationKey  Delete an application key in current tenant
  disable-applicationKey Disable an application key.
  enable-applicationKey  Enable an application key.
  get-applicationKey     Get details of an application key in the current tenant
  get-applicationKeys    Get all application keys
  rotate-applicationKey  Rotate credentials of an application key.
  update-applicationKey  Update an application key in current tenant

Flags:
  -h, --help   help for applicationkey

Use "chef-platform-auth-cli user-account applicationkey [command] --help" for more information about a command.
```

## `chef-platform-auth-cli user-account applicationkey create-applicationKey`

```text
### Summary:
Creates a new application key for client node enrollment.

---
### Errors

#### 400 (Bad Request)
Bad request

#### 401 (Unauthorised)
The credential used to access this resource is invalid or expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 404 (Not Found)
The roleId or cohortId does not exist.

#### 409 (Conflict)
The application key with the provided name already exists.

#### 423 (Locked)
The targeted resource is currently locked and cannot be modified.

#### 500 (Internal Server Error)
Some unexpected error occurred while processing the request.

Usage:
  chef-platform-auth-cli user-account applicationkey create-applicationKey [flags]

Examples:
- title: Default example
  example: |
    {
        "allowedPlatforms": "windows,linux",
        "cohortId": "2b4a84db-a107-4b2e-9a2f-85eef74686c1",
        "description": "description of application",
        "expiryAt": "2025-05-20T04:05:06Z",
        "ipCIDRs": "192.168.0.0/24",
        "ipRanges": "192.168.0.1-192.168.0.40",
        "macAddresses": "00-11-22-33-44-AA,00-11-22-33-44-FF",
        "name": "application key name",
        "roleId": "39dc4a06-d52a-435f-a2b1-6dec1ffee854",
        "type": "enrollment-cli"
    }


Flags:
      --body string             create application key
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for create-applicationKey
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account applicationkey delete-applicationKey`

```text
### Summary:
Use this operation to delete an application key in the current tenant

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the current tenant

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account applicationkey delete-applicationKey [flags]

Flags:
      --applicationKeyId string   ID of an Application Key.
      --format string             to print response in format (default "json")
  -h, --help                      help for delete-applicationKey
      --profile string            name of the profile to be used for cmd (default "default")
      --verbose                   to show debug logs
```

## `chef-platform-auth-cli user-account applicationkey disable-applicationKey`

```text
### Summary:
Use this operation when there is a need to disable an application key.

### Errors

#### 404 (Not Found)
The application key you requested does not exist in the current organizational unit.

#### 409 (Conflict)
The application key is already disabled.

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account applicationkey disable-applicationKey [flags]

Flags:
      --If-Match string           If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --applicationKeyId string   ID of an Application Key.
      --format string             to print response in format (default "json")
  -h, --help                      help for disable-applicationKey
      --profile string            name of the profile to be used for cmd (default "default")
      --verbose                   to show debug logs
```

## `chef-platform-auth-cli user-account applicationkey enable-applicationKey`

```text
### Summary:
Use this operation to enable an application key.

### Errors:

#### 404 (Not Found)
The application key you requested does not exist in the current organizational unit.

#### 409 (Conflict)
The application key is already enabled.

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
Application key with this user is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account applicationkey enable-applicationKey [flags]

Flags:
      --If-Match string           If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --applicationKeyId string   ID of an Application Key.
      --format string             to print response in format (default "json")
  -h, --help                      help for enable-applicationKey
      --profile string            name of the profile to be used for cmd (default "default")
      --verbose                   to show debug logs
```

## `chef-platform-auth-cli user-account applicationkey get-applicationKey`

```text
### Summary:
Use this operation to get an application key in the current tenant

---
### Errors

#### 404 (Not Found)
The application key you requested does not exist in the tenant

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account applicationkey get-applicationKey [flags]

Flags:
      --applicationKeyId string   ID of an Application Key.
      --format string             to print response in format (default "json")
  -h, --help                      help for get-applicationKey
      --profile string            name of the profile to be used for cmd (default "default")
      --verbose                   to show debug logs
```

## `chef-platform-auth-cli user-account applicationkey get-applicationKeys`

```text
### Summary:
Returns a paginated list of application keys.
The response includes detailed information about each application key.

The results can be filtered by:
- Status (enabled/disabled)
- Type (e.g., enrollment-cli)

### Errors

#### 400 (Bad Request)
The request parameters are invalid or malformed.

#### 401 (Unauthorized)
Authentication credentials are missing or invalid.

#### 403 (Forbidden)
The authenticated user does not have permission to access this resource.

#### 500 (Internal Server Error)
An unexpected error occurred while processing the request.

Usage:
  chef-platform-auth-cli user-account applicationkey get-applicationKeys [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for get-applicationKeys
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --status string         the status of the application key
      --type string           The type of the application key
      --verbose               to show debug logs
```

## `chef-platform-auth-cli user-account applicationkey rotate-applicationKey`

```text
### Summary:
This API will generate an encrypted base64 encoded binary config file for user download.

### Errors:

#### 400 (Bad Request)
Invalid request parameters. Ensure the applicationKeyId is correct.

#### 404 (Not Found)
The requested application key does not exist.

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 423 (Locked)
The requested application key is disabled, please enable it before requesting credential rotation.

#### 423 (Locked)
The requested application key is already expired.

#### 500 (Internal Server Error)
Some unexpected error occurred while processing this request.

Usage:
  chef-platform-auth-cli user-account applicationkey rotate-applicationKey [flags]

Flags:
      --applicationKeyId string   ID of an Application Key.
      --file-name string          path to file in the local system to write response body
      --format string             to print response in format (default "json")
  -h, --help                      help for rotate-applicationKey
      --override-file             to override file if already exists
      --profile string            name of the profile to be used for cmd (default "default")
      --verbose                   to show debug logs
```

## `chef-platform-auth-cli user-account applicationkey update-applicationKey`

```text
### Summary:
This operation will update an application key

---
### Errors

#### 409 (Conflict)
Application Key name is already in use

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 422 (Unprocessable Content)
The Application key does not meet the validation requirements. Reference the errors in the response for more details.

#### 423 (Locked)
The Application key is disabled/expired

#### 404 (Not Found)
The application key you requested does not exist in the tenant

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account applicationkey update-applicationKey [flags]

Examples:
- title: Default example
  example: |
    {
        "allowedPlatforms": "linux",
        "cohortId": "2b4a84db-a107-4b2e-9a2f-85eef74686c1",
        "description": "description of application",
        "expiryAt": "2025-05-20T04:05:06Z",
        "id": "a852c042-f9b4-45c7-a988-035412692685",
        "ipCIDRs": "192.168.0.0/24",
        "ipRanges": "192.168.0.1-192.168.0.40",
        "macAddresses": "00-11-22-33-44-AA,00-11-22-33-44-FE",
        "name": "application key name",
        "roleId": "39dc4a06-d52a-435f-a2b1-6dec1ffee854",
        "type": "enrollment-cli"
    }


Flags:
      --If-Match string           If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --applicationKeyId string   ID of an Application Key.
      --body string               The updated application key information
      --body-file string          Path to file in the local system containing valid body parameter value
      --body-format string        Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                   to show example request body
  -f, --example-format string     Format of the --example, options: json, yaml, toml (default "json")
      --format string             to print response in format (default "json")
  -h, --help                      help for update-applicationKey
      --profile string            name of the profile to be used for cmd (default "default")
      --verbose                   to show debug logs
```

## `chef-platform-auth-cli user-account identity`

```text
Commands related to identity

Usage:
  chef-platform-auth-cli user-account identity [command]

Available Commands:
  authorise-device      Device authorisation
  change-password       PATCH the current users' password with a new password
  create-identity       Create a "local" user
  delete-identity       Delete a user in current tenant
  device-api-token      Create tokens for the device, if device auth code got authorised successfully.
  device-deregister     Deregister given device
  device-register       Get auth code with authorisation url for given device register request
  disable-device        Disable device by id
  disable-identity      Disable the user associated with the given userId
  enable-device         Enable device by id
  enable-identity       Enable the user associated with the given userId
  expire-password       Expire the users password
  get-authorise-page    Device authorisation page (UI) for user.
  get-identity          Get a user in current tenant
  get-jwt               Get jwt token by exchanging the oauth code and state
  get-login-page        Login page UI for user.
  get-password-set-page Get Password set UI page for user.
  get-self-user         Get Current Logged in User details
  list-users            Get all users present in current tenant
  login-identity        Get oauth code for given credentials (email and password)
  logout-all-identity   Logout all the session of the user
  logout-identity       Logout current user and current session
  lookup-user-tenants   Lookup tenants by user email
  refresh-jwt           Get jwt token by exchanging the access token and refresh token.
  reset-password        Password Reset
  retry-dsm-creation    Create DSM user only
  set-password          Set Password for user
  update-identity       Update a user in current tenant
  update-self-user      Update the current user

Flags:
  -h, --help   help for identity

Use "chef-platform-auth-cli user-account identity [command] --help" for more information about a command.
```

## `chef-platform-auth-cli user-account identity authorise-device`

```text
### Summary:
This operation will authorise the requested device.

#### Note:
This method will only work with local users (not OAUTH)

---
### Process
1. The user needs to enter the auth code received over cli
2. The auth code will be validated against the request

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 409 (Conflict)
The auth code already authorised

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity authorise-device [flags]

Examples:
- title: Default example
  example: |
    {
        "expiration": "2024-12-31T11:42:23-05:00",
        "oauthCode": "123456"
    }


Flags:
      --body string             Auth Code for device authorisation
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --deviceId string         ID of an device
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for authorise-device
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account identity change-password`

```text
This method will change the users password

---
### Error(s)

#### 409 (Conflict)
If the current user who has requested a password reset is not a "local" user

#### 422 (Unprocessable Content)
If the users current password does not match or the new password does not meet the password requirements

#### 423 (Locked)
If the user's account is currently locked

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity change-password [flags]

Flags:
      --body string          The password to be changed
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for change-password
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli user-account identity create-identity`

```text
### Summary:
Use this operation to create a local user

### Note:
for "local" users:
1. If a user is created without a password, they will be emailed a set password link with token/OTP
2. If a user is created with a password, they will be emailed with a created user details

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 404 (Not Found)
The role ID specified does not exist in the organization

#### 409 (Conflict)
User is already registered

#### 422 (Unprocessable Content)
The Reference the errors in the response for more details.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity create-identity [flags]

Examples:
- title: createUserOfKindLocal
  example: |
    {
        "$kind": "local",
        "email": "user@example.com",
        "firstName": "John",
        "lastName": "Doe",
        "password": "P@ssw0rd123"
    }
- title: createUserOfKindLocalWithPasswordReset
  example: |
    {
        "$kind": "local",
        "email": "user@example.com",
        "firstName": "Jane",
        "lastName": "Doe"
    }


Flags:
      --body string             The user to be created
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
  -n, --example-number int      Example number ranges from 1 to the number of examples present. If value is 0, it will show all examples
      --format string           to print response in format (default "json")
  -h, --help                    help for create-identity
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account identity delete-identity`

```text
### Summary:
Use this operation to delete a user in the current tenant

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the current tenant

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity delete-identity [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for delete-identity
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity device-api-token`

```text
### Summary:
This will return api token for given deviceId

---
### Errors

#### 400 (Bad Request)
When code is not authorized.

#### 404 (Not Found)
The device does not exist.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity device-api-token [flags]

Examples:
- title: Default example
  example: |
    {
        "stateCode": "abc123"
    }


Flags:
      --body string             State Code for device token creation
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --deviceId string         ID of an device
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for device-api-token
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account identity device-deregister`

```text
### Summary:
Use this operation to deregister given device

---
### Errors

#### 409 (Conflict)
The device is already deregistered

#### 404 (Not Found)
Tenant not found

#### 404 (Not Found)
Org not found

#### 404 (Not Found)
Role not found

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity device-deregister [flags]

Flags:
      --deviceId string   ID of an device
      --format string     to print response in format (default "json")
  -h, --help              help for device-deregister
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account identity device-register`

```text
### Summary:
Use this operation to get auth code with authorisation url for given device register request

---
### Errors

#### 400 (Bad Request)
Request body validation failed

#### 404 (Not Found)
Tenant not found

#### 423 (Locked)
Device is Disabled

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity device-register [flags]

Examples:
- title: Default example
  example: |
    {
        "appType": "type",
        "deviceName": "device_name",
        "deviceUser": "device_user",
        "macAddress": "mac_address",
        "stateCode": "abc123"
    }


Flags:
      --body string             Device metadata
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for device-register
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account identity disable-device`

```text
### Summary:
This will disable the device

---
### Process
1) This api will require a deviceId which consist of (mac + deviceUser + appType)

---
### Errors

#### 404 (Not Found)
The device you requested does not exist in the current organizational unit

#### 409 (Conflict)
The device is already disabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity disable-device [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --deviceId string   ID of an device
      --format string     to print response in format (default "json")
  -h, --help              help for disable-device
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account identity disable-identity`

```text
### Summary:
This operation will disable the user associated with the given userId. All sessions for this userId will stop working. When a user is disabled, their status is updated in both the platform and Declarative State Management (DSM).
#### Note:
This operation updates the user status in DSM to ensure consistency across the platform.

---
### Errors

#### 409 (Conflict)
Email is already disabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity disable-identity [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for disable-identity
      --profile string    name of the profile to be used for cmd (default "default")
      --userId string     ID of an User
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account identity enable-device`

```text
### Summary:
This will enable the device

---
### Process
1. This api will require a deviceId which consist of (mac + deviceUser + appType)

---
### Errors

#### 404 (Not Found)
The device you requested does not exist in the current organizational unit

#### 409 (Conflict)
The device is already enabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity enable-device [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --deviceId string   ID of an device
      --format string     to print response in format (default "json")
  -h, --help              help for enable-device
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account identity enable-identity`

```text
### Summary:
This operation will enable the user associated with the given userId. When a user is enabled, their status is updated in both the platform and Declarative State Management (DSM).

#### Note:
This operation updates the user status in DSM to ensure consistency across the platform.

---
### Errors

#### 409 (Conflict)
Email is already enabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity enable-identity [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for enable-identity
      --profile string    name of the profile to be used for cmd (default "default")
      --userId string     ID of an User
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account identity expire-password`

```text
### Summary:
Use this operation to require the user to change their password upon next login

**WARNING** this will not impact any currently logged in users.

#### Note:
This method will only work with locally users (not OAUTH)

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the current tenant unit

#### 409 (Conflict)
The user's password is already set to expire

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.
 This method will list all roles for a user

#### 423 (Locked)
User is not a local user

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity expire-password [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --emailId string    Email ID of an User
      --format string     to print response in format (default "json")
  -h, --help              help for expire-password
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account identity get-authorise-page`

```text
### Summary:
This operation will load the HTML form page

---
### Process
1. The user need to enter the OAUTH code
2. Then submit to authorize the device

---
### Errors

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity get-authorise-page [flags]

Flags:
      --appType string      Application Type
      --deviceId string     ID of an device
      --deviceName string   Device Name
      --format string       to print response in format (default "json")
  -h, --help                help for get-authorise-page
      --oauthCode string    OAuth code
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-platform-auth-cli user-account identity get-identity`

```text
### Summary:
Use this operation to get a user in the current tenant

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the tenant

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity get-identity [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-identity
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity get-jwt`

```text
### Summary:
Use this operation to get jwt token by exchanging the oauth code and state.

#### Note:
1. In return we will get access token, refresh token and expiry
2. Access token will have tenantID,userID,roleId etc along with baseOrgId

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 422 (Unprocessable Content)
The given values for oauth code and state is not in correct format

#### 423 (Locked)
This identity is locked/disabled

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity get-jwt [flags]

Examples:
- title: Default example
  example: |
    {
        "oauthCode": "oauth-code",
        "state": "random-string"
    }


Flags:
      --body string             Get JWT token by exchanging the oauth code and state
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for get-jwt
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account identity get-login-page`

```text
### Summary:
This operation will load the HTML page to login

---
### Process
1. The user need to enter the email and valid password
2. Then submit to get oauth code and state

---
### Errors

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity get-login-page [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-login-page
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity get-password-set-page`

```text
### Summary:
This operation will load the HTML page to set password using OTP

#### Note:
This method will only work with locally users (not OAUTH)

---
### Process
1. The user needs to enter the OTP received over email in this page or auto-filled from the URL.
2. Then submit to set a new password

### Errors

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity get-password-set-page [flags]

Flags:
      --emailId string   Email ID of an User
      --format string    to print response in format (default "json")
  -h, --help             help for get-password-set-page
      --otp string       OTP to set the new password
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity get-self-user`

```text
### Summary:
Use this operation get the current logged in user details

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity get-self-user [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-self-user
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity list-users`

```text
### Summary:
This operation will get all users present in current tenant

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity list-users [flags]

Flags:
      --email string          email of the user
      --format string         to print response in format (default "json")
  -h, --help                  help for list-users
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-platform-auth-cli user-account identity login-identity`

```text
### Summary:
Use this operation to get oauth code for given credentials (email and password)

---
### Errors

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorized)
User credentials are not correct or doesn't belong to the tenant

#### 404 (Not Found)
Resource not found

#### 423 (Locked)
This identity is locked/disabled

#### 422 (Unprocessable Content)
Document validation failed

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity login-identity [flags]

Examples:
- title: Default example
  example: |
    {
        "email": "mickey.mouse@disney.com",
        "password": "password",
        "state": "random-string"
    }


Flags:
      --body string             Login with email and password
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for login-identity
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account identity logout-all-identity`

```text
### Summary:
Use this operation to logout all the sessions of the user by expiring them

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 423 (Locked)
Resource targeted cannot be modified

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity logout-all-identity [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for logout-all-identity
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity logout-identity`

```text
### Summary:
Use this operation to logout current user and current session by expiring the session

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 423 (Locked)
Resource targeted cannot be modified

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity logout-identity [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for logout-identity
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity lookup-user-tenants`

```text
### Summary
Retrieve a list of tenants associated with a user's email address.

#### Note
- The email parameter must be base64 encoded before calling this API
- Clients must set the X-Lookup-Tenant-Id header with the returned tenant ID for subsequent requests
- This API is public only when base_fqdn is configured in the gateway

### Validation
- Input validation is performed on base64 format and decoded email format
- Returns 422 if email format is invalid after decoding

### Errors
#### 400 (Bad Request)
Invalid base64 encoding or malformed request

#### 422 (Unprocessable Content)
Invalid email format after base64 decoding

#### 404 (Not Found)
No tenants found for the specified email address

Usage:
  chef-platform-auth-cli user-account identity lookup-user-tenants [flags]

Flags:
      --base64EncodedEmail string   Base64 encoded email address of the user
      --format string               to print response in format (default "json")
  -h, --help                        help for lookup-user-tenants
      --profile string              name of the profile to be used for cmd (default "default")
      --verbose                     to show debug logs
```

## `chef-platform-auth-cli user-account identity refresh-jwt`

```text
### Summary:
Use this operation to get new jwt access token by exchanging refresh token with valid access token.

#### Note
In return we will get a new access token, refresh token and it's expiry.
Refresh token will be the same as before if refreshed to get new access token with same OrgId and RoleId.
The roleId and OrgId in the request can be used to switch role/org post login.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 423 (Locked)
This identity is locked/disabled

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity refresh-jwt [flags]

Examples:
- title: Default example
  example: |
    {
        "orgId": "2936634b-9760-40a2-9bdd-73291345cfdd",
        "refreshToken": "refresh-token",
        "roleId": "2936634b-9760-40a2-9bdd-73291345cfdd"
    }


Flags:
      --body string             Get JWT token by exchanging the access token and refresh token
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for refresh-jwt
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account identity reset-password`

```text
### Summary:
This operation will begin the password reset process.

**WARNING** this will not impact any currently logged in users.

#### Note:
This method will only work with local users (not OAUTH)

---
### Process
1. The user will be emailed a link to change their password
2. The email & link will contain a one time passcode (OTP)
3. The user have to click the link and proceed with the given otp

---
### Errors

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.
 This method will list all roles for a user

#### 423 (Locked)
User is not a local user

#### Note for record not found
When the user you requested (by email-id) does not exist in the current tenant unit,
instead of showing email-id not found it will send a 200 not exposing if the user is present in the system or not

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity reset-password [flags]

Flags:
      --emailId string   Email ID of an User
      --format string    to print response in format (default "json")
  -h, --help             help for reset-password
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity retry-dsm-creation`

```text
### Summary:
This operation creates a user account in Declarative State Management (DSM) using the 'dsm_username' provided in the request.

#### Use Case:
When a user creation in DSM fails due to transient errors, this endpoint allows retrying the creation without requiring the user to provide information again.

#### Note:
- The endpoint requires no request body
- It uses the 'dsm_username' already stored from the previous attempt
- A 409 response from DSM (user already exists) is treated as a success

---
### Errors

#### 412 (Precondition Failed)
The 'dsm_username' is not found for the specified userId. This indicates no previous creation attempt was made.

#### 500 (Internal Server Error)
The request to DSM failed with an error other than 409. The error is wrapped in a standard platform error response.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

Usage:
  chef-platform-auth-cli user-account identity retry-dsm-creation [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for retry-dsm-creation
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account identity set-password`

```text
### Summary:
This operation will set the password for the user.

#### Note:
This method will only work with locally users (not OAUTH)

---
### Process
1. The user need to enter the OTP received over email and a valid new password
2. The OTP will be validated against the email
3. Password history rules will be checked.
4. If the new password value passes the rules then new password will be set against the identity

---
### Errors

#### 400 (Bad request)
Bad request

#### 401 (Unauthorized)
Invalid credentials

#### 404 (Not Found)
Resource not found

#### 423 (Locked)
User is disabled

#### 422 (Unprocessable Content)
Password validation failed

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity set-password [flags]

Examples:
- title: Default example
  example: |
    {
        "emailId": "chef@progress.com",
        "otp": "123456",
        "password": "new-password"
    }


Flags:
      --body string             Password to be set using OTP
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for set-password
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account identity update-identity`

```text
### Summary:
This operation will update a user

**WARNING** this will update the user's information in the current tenant

#### Note:
This method will only allow updating of local users

---
### Errors

#### 409 (Conflict)
Email address already in use

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 422 (Unprocessable Content)
The policy does not meet the validation requirements. Reference the errors in the response for more details.

#### 423 (Locked)
User is not a local user

#### 400 (Bad Request)
Malformed-Request

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity update-identity [flags]

Flags:
      --If-Match string      If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string          The updated user information
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-identity
      --profile string       name of the profile to be used for cmd (default "default")
      --userId string        ID of an User
      --verbose              to show debug logs
```

## `chef-platform-auth-cli user-account identity update-self-user`

```text
### Summary:
This operation will update the current user

#### Note:
This method will only allow updating of local user any other type of user (Node, Token, OAuth will fail)

---
### Errors

#### 409 (Conflict)
Email address already in use

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 422 (Unprocessable Content)
The policy does not meet the validation requirements. Reference the errors in the response for more details.

#### 423 (Locked)
User is not a local user

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account identity update-self-user [flags]

Flags:
      --If-Match string      If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string          The updated user information
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-self-user
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli user-account jwt`

```text
Commands related to jwt

Usage:
  chef-platform-auth-cli user-account jwt [command]

Available Commands:
  jwt-expiry-set    Update the Expiry durations for tenant.
  rotate-jwt-secret Rotate the jwt secret

Flags:
  -h, --help   help for jwt

Use "chef-platform-auth-cli user-account jwt [command] --help" for more information about a command.
```

## `chef-platform-auth-cli user-account jwt jwt-expiry-set`

```text
### Summary:
Use this operation to update the JWT Token expiry duration, Oauth expiry duration and the Session  expiry duration. The duration should always be set in seconds.

---
### Errors

#### 422 (Unprocessable Content)
Request Body should have at least one field present

Usage:
  chef-platform-auth-cli user-account jwt jwt-expiry-set [flags]

Flags:
      --body string          The duration update information (in seconds).
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for jwt-expiry-set
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-platform-auth-cli user-account jwt rotate-jwt-secret`

```text
### Summary:
Rotate the jwt secret for the given tenant.

---
### Errors
#### 404 (Not Found)
The requested data does not exist in the current organizational unit

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account jwt rotate-jwt-secret [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for rotate-jwt-secret
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account self`

```text
Commands related to self

Usage:
  chef-platform-auth-cli user-account self [command]

Available Commands:
  create-token       create a new API token for the signed in user
  delete-token       Deletes current user's API token
  get-organization   GET the organization of the current logged in user
  get-role           Get current Users active role
  get-token          Get current user's API token information
  list-organizations Get all organizations current user is assigned to
  list-roles         List current Users roles
  list-tokens        List all the tokens for the signed in user in current organization
  revoke-token       revoke current user's API Token

Flags:
  -h, --help   help for self

Use "chef-platform-auth-cli user-account self [command] --help" for more information about a command.
```

## `chef-platform-auth-cli user-account self create-token`

```text
### Summary:
Use this operation to assign a new api token for a user

---
### Errors

#### 422 (Unprocessable Content)
Field validation error

#### 423 (Locked)
The role is not a valid user assignable role

#### 409 (Conflict)
Resource is already in use

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self create-token [flags]

Examples:
- title: Default example
  example: |
    {
        "expiration": "2024-12-31T11:42:23-05:00",
        "name": "CI-CD Token"
    }


Flags:
      --body string             Token to be created
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for create-token
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account self delete-token`

```text
### Summary:
Use this operation to delete current user's API token in the current organization and role

---
### Errors

#### 404 (Not Found)
The token requested does not exist in the organization or is not assigned to the user

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self delete-token [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-token
      --profile string    name of the profile to be used for cmd (default "default")
      --tokenId string    ID of a API Token
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account self get-organization`

```text
This method will get organizational unit details of the current logged in user

 ---
 ### Errors

 #### 401 (Unauthorised)
 The credential used to access this resource is invalid/expired

 #### 403 (Forbidden)
 User with this role is not allowed to access the resource

 #### 500 (Internal Server Error)
 Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self get-organization [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-organization
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account self get-role`

```text
### Summary:
Use this operation to get the user's currently active role.

---
### Errors

#### 404 (Not Found)
The role you requested does not exist in the organization

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self get-role [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-role
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account self get-token`

```text
### Summary:
Use this operation get current user's API token information

---
### Errors

#### 404 (Not Found)
The requested token is not assigned to the user

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self get-token [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-token
      --profile string   name of the profile to be used for cmd (default "default")
      --tokenId string   ID of a API Token
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account self list-organizations`

```text
### Summary:
This operation will search for all organizations in which current user is assigned.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self list-organizations [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for list-organizations
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-platform-auth-cli user-account self list-roles`

```text
### Summary:
Use this operation to list all the roles assigned to the current user. Roles can be filtered based on the Organization Id by sending the id in the query params.

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self list-roles [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-roles
      --orgId string     the organization id
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account self list-tokens`

```text
### Summary:
Use this operation to list all of a tokens assigned to the user in the current organization and role

---
### Errors

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self list-tokens [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-tokens
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account self revoke-token`

```text
### Summary:
Use this operation to revoke (immediately expire) but not delete an API token in the current organization and role

---
### Errors

#### 404 (Not Found)
The token you requested does not exist in the current organizational unit or is not assigned to the user

#### 409 (Conflict)
The token is already revoked

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorised)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account self revoke-token [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for revoke-token
      --profile string    name of the profile to be used for cmd (default "default")
      --tokenId string    ID of a API Token
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account user`

```text
Commands related to user

Usage:
  chef-platform-auth-cli user-account user [command]

Available Commands:
  add-user             Add a user to current organization
  assign-dsm-org-admin Assign DSM org-admin role to a user
  assign-role          Assigns a user a new role in current organization unit.
  assign-roles         Create bulk users and assign respective roles to each user.
  create-token         create a new API token for the user in current organization
  delete-role          Delete a given role for a given user.
  delete-token         Deletes a user's API token in current organization
  disable-role         Disable a given role for a given user in the current organization unit
  disable-user         Disable a user for the current organizational unit
  enable-role          Enable a given role for a given user in the current organization unit
  enable-user          Enable a user for the current organizational unit
  get-role             Get details of a given role for a given user.
  get-token            Get a user's API token in current organization
  getJwt-apiToken      Get jwt token by exchanging the oauth code and state for the selected tenant, org, role and user
  list-roles           List roles assigned to given user
  list-tokens          List all the tokens for the user in current organization
  list-users           Find all users present in current organization unit.
  login-apiToken       Get oauth code for given TenantId and API token
  remove-user          Delete a user from current organization
  revoke-token         revoke the API Token for an user in teh current organization
  verify-api           Verify the token for login

Flags:
  -h, --help   help for user

Use "chef-platform-auth-cli user-account user [command] --help" for more information about a command.
```

## `chef-platform-auth-cli user-account user add-user`

```text
### Summary:
If user exists it will add to organization. If user does not exist it creates the user, sends password set email and then adds them to organization

**WARNING** A user can not login unless that have at least one enabled role.

#### Note:
1. The user must already exist as a local user in the tenant for this operation to succeed
2. This is commonly only used for local users, for OAUTH users the OAUTH flow can be configured to automaticly add the user and roles to the organizations

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the tenant

#### 423 (Locked)
The user you requested is locked/disabled in the tenant

#### 409 (Conflict)
user already exists in this organization

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user add-user [flags]

Examples:
- title: Default example
  example: |
    {
        "email": "test@test.com",
        "firstName": "test",
        "lastName": "test",
        "userType": "local"
    }


Flags:
      --body string             User to be created
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for add-user
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account user assign-dsm-org-admin`

```text
### Summary:
Use this operation to assign the DSM org-admin role to an existing user in the DSM organization.

#### Note:
This endpoint is specifically designed for assigning the org-admin role in DSM organizations.

---
### Errors

#### 404 (Not Found)
The user does not exist in the organization

#### 409 (Conflict)
The role is already assigned to this user

#### 422 (Unprocessable Content)
The requested payload does not meet the validation requirements

#### 423 (Locked)
The role/user is disabled

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User with this role is not allowed to access the resource

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource

Usage:
  chef-platform-auth-cli user-account user assign-dsm-org-admin [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for assign-dsm-org-admin
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account user assign-role`

```text
### Summary:
Use this operation to assign a role to an existing user

#### Note:
Until a user is asigned at least one role they will not be able to login

---
### Errors

#### 404 (Not Found)
The role you requested does not exist in the organization

#### 409 (Conflict)
The role is already assigned to this user

#### 422 (Role validation failed)
The roles is a System/Service role not a user role

#### 423 (Locked)
The role/user is disabled

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user assign-role [flags]

Examples:
- title: Default example
  example: |
    {
        "name": "string",
        "roleId": "3fa85f64-5717-4562-b3fc-2c963f66afa6"
    }


Flags:
      --body string             Role to be assigned to this user
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for assign-role
      --profile string          name of the profile to be used for cmd (default "default")
      --userId string           ID of an User
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account user assign-roles`

```text
### Summary:
If the user with the provided email already exists,the API will assign the specified role(s) to the user as a final set of roles.
If the user does not exist, the API will create the user, and the API will assign the specified role(s) to the user as a final set of roles.
If the provided role does not exist, the API returns an error.
List of users in request object is limited.If the limit exceeded server will through an error.
---
### Errors

#### 404 (Not Found)
The role you requested does not exist in the organization.

#### 409 (Conflict)
The role is already assigned to this user.

#### 422 (Role validation failed)
The role is a system or service role, not a user role.

#### 423 (Locked)
The role or user is disabled.

#### 401 (Unauthorized)
The credential used to access this resource is invalid or expired.

#### 403 (Forbidden)
A user with this role is not allowed to access the resource.

#### 413 (Content Too Large)
Request entity is larger than the limits defined by the server

#### 500 (Internal Server Error)
Some unexpected errors occurred while requesting this resource.

#### 502 (Bad Gateway Error)
Communication error between two web servers.

#### 503 (Service Unavailable)
The request service is not responsive.

Usage:
  chef-platform-auth-cli user-account user assign-roles [flags]

Examples:
- title: Default example
  example: |
    [
        {
            "email": "test@test.com",
            "firstName": "test",
            "lastName": "test",
            "roleIds": [
                "253c0912-67f0-4fae-8723-91614ade5474",
                "253c0912-67f0-4fae-8723-91614ade6443"
            ]
        }
    ]


Flags:
      --body string             Roles to be assigned to the list of provided user
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for assign-roles
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account user create-token`

```text
### Summary:
Use this operation to assign a new api token for a user

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the organization

#### 404 (Not Found)
The role you requested is not assigned to the user

#### 409 (Conflict)
The user is disabled

#### 423 (Locked)
The user is locked

#### 422 (Validation failed)
The expiration date is incorrect

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user create-token [flags]

Examples:
- title: Default example
  example: |
    {
        "expiration": "2024-12-31T11:42:23-05:00",
        "name": "CI-CD Token",
        "roleId": "dd999d26-bed0-488b-bb5b-db41c0e4c8ab"
    }


Flags:
      --body string             Token to be created
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for create-token
      --profile string          name of the profile to be used for cmd (default "default")
      --userId string           ID of an User
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account user delete-role`

```text
### Summary:
Use this operation to remove a role from a user. This will prevent the user from accessing this role upon the next login.

---
### Errors

#### 404 (Not Found)
The user requested does not exist in the organization

#### 404 (Not Found)
The role you requested does not exist in the organization

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user delete-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-role
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --userId string     ID of an User
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account user delete-token`

```text
### Summary:
Use this operation to delete a users API token.

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the organization

#### 404 (Not Found)
The token requested does not exist in the organization or is not assigned to the user

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 409 (Conflict)
The user is disabled

#### 423 (Locked)
The user is locked

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user delete-token [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-token
      --profile string    name of the profile to be used for cmd (default "default")
      --tokenId string    ID of a API Token
      --userId string     ID of an User
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account user disable-role`

```text
### Summary:
Use this operation to disable a role for given user in current organization unit.

**WARNING** This will enabled this role only for given user in the current organization unit

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the current organization unit

#### 404 (Not Found)
The role you requested is not assigned to this user in the organization unit

#### 409 (Conflict)
The role for given user is already disabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user disable-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for disable-role
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --userId string     ID of an User
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account user disable-user`

```text
### Summary:
Use this operation when there is a need to disable a user without deleting them.

**WARNING** This will disable this user only for the current organizational unit

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the current organizational unit

#### 409 (Conflict)
The user is already disabled

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user disable-user [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for disable-user
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account user enable-role`

```text
### Summary:
Use this operation to enable a role for given user in current organization unit.

**WARNING** This will enabled this role only for given user in the current organization unit

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the current organization unit

#### 409 (Conflict)
The role for given user is already enabled

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user enable-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for enable-role
      --profile string    name of the profile to be used for cmd (default "default")
      --roleId string     ID of a Role
      --userId string     ID of an User
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account user enable-user`

```text
### Summary:
Use this operation to enable a user in current organization unit.

**WARNING** This will enabled this user only for the current organizational unit

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the current organizational unit

#### 409 (Conflict)
The user is already enabled

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user enable-user [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for enable-user
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account user get-role`

```text
### Summary:
Use this operation to get the details on a role assigned to a user

---
### Errors

#### 404 (Not Found)
The user requested does not exist in the organization

#### 404 (Not Found)
The role you requested is not assigned to this user in the organization

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user get-role [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-role
      --profile string   name of the profile to be used for cmd (default "default")
      --roleId string    ID of a Role
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account user get-token`

```text
### Summary:
Use this operation to get a users API token

---
### Errors

#### 404 (Not Found)
The policy requested does not exist in the organization

#### 409 (Conflict)
The user is disabled

#### 423 (Locked)
The user is locked

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user get-token [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for get-token
      --profile string   name of the profile to be used for cmd (default "default")
      --tokenId string   ID of a API Token
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account user getJwt-apiToken`

```text
### Summary:
Use this operation to get jwt token by exchanging the oauth code and state for the selected tenant, org, role and user

---
### Errors

#### 403 (Unauthorized)
The given oauth code and state is not valid

#### 422 (Unprocessable Content)
The given values for oauth code and state is not in correct format

#### 423 (Locked)
This identity is locked/disabled

#### 423 (Locked)
This user is disabled in the org where the token belongs

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user getJwt-apiToken [flags]

Examples:
- title: Default example
  example: |
    {
        "oauthCode": "oauth-code",
        "state": "random-string"
    }


Flags:
      --body string             Get JWT token by exchanging the oauth code and state
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for getJwt-apiToken
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account user list-roles`

```text
### Summary:
Use this operation to list all roles assigned to a user

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the organization

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user list-roles [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-roles
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account user list-tokens`

```text
### Summary:
Use this operation to list all tokens assigned to the user

---
### Errors

#### 404 (Not Found)
The user is not found in the organization

#### 409 (Conflict)
The user is disabled

#### 423 (Locked)
The user is locked

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user list-tokens [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-tokens
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account user list-users`

```text
### Summary:
This operation will search for users in the current organization.

---
### Errors

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 404 (Not Found)
The server could not located the requested resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user list-users [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for list-users
      --includeRoles          Include user roles in the response
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --roleId string         the role id
      --verbose               to show debug logs
```

## `chef-platform-auth-cli user-account user login-apiToken`

```text
### Summary:
This operation will get oauth code for given TenantId and API token

---
### Errors

#### 401 (Unauthorized)
The API token doesn't belong to the tenant

#### 401 (Unauthorized)
The API token in expired

#### 423 (Locked)
This identity is locked/disabled

#### 423 (Locked)
This user is disabled in the org in which the token belongs

#### 422 (Unprocessable Content)
The request query string options does not meet the validation requirements. Reference the errors in the response for more details.

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user login-apiToken [flags]

Examples:
- title: Default example
  example: |
    {
        "accessKey": "access-key",
        "secretKey": "secret-key",
        "state": "random-string"
    }


Flags:
      --body string             Get oauth code for given tenant and api token
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for login-apiToken
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli user-account user remove-user`

```text
### Summary:
Use this operation to remove a user from the current organization.

#### Notes:
This will NOT remove the user from the tenant, it will only remove this user from the current organization.

---
### Errors

#### 404 (Not Found)
The node or role you requested does not exist in the organization

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user remove-user [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for remove-user
      --profile string   name of the profile to be used for cmd (default "default")
      --userId string    ID of an User
      --verbose          to show debug logs
```

## `chef-platform-auth-cli user-account user revoke-token`

```text
### Summary:
Use this operation to revoke (immediately expire) but not delete an API token

---
### Errors

#### 404 (Not Found)
The user you requested does not exist in the current organizational unit

#### 404 (Not Found)
The token you requested does not exist in the current organizational unit or is not assigned to the user

#### 409 (Conflict)
The token is already revoked

#### 412 (Precondition Failed)
If the "If-Match" header is included in the request, this error status indicates that
the value of "If-Match" (the ETag information) did not match and therefore the entity was not updated.

#### 423 (Locked)
The user is locked

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired.

#### 403 (Forbidden)
User with this role is not allowed to access the resource.

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user revoke-token [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for revoke-token
      --profile string    name of the profile to be used for cmd (default "default")
      --tokenId string    ID of a API Token
      --userId string     ID of an User
      --verbose           to show debug logs
```

## `chef-platform-auth-cli user-account user verify-api`

```text
### Summary:
Use this operation to verify the API token and API secret.

#### Note:
1. This is used by the Authentication service, and thus allows anonymous access

**WARNING**: The Expand parameter may require change in the future it is intended to be used by
the authentication system and not directly by end users. This may need to move to a header varaible with a token
to allow the expand operation to occur

---
### Errors

#### 401 (Unauthorized)
API Key or Token are not valid for the tenant

#### 404 (Not Found)
The tenant does not exist

#### 422 (Unprocessable Content)
The requested payload does not meet the validation requirements. Reference the errors in the response for more details.

#### 423 (Locked)
This API token has expired

#### 500 (Internal Server Error)
Some unexpected error occurred requesting this resource.

Usage:
  chef-platform-auth-cli user-account user verify-api [flags]

Examples:
- title: Default example
  example: |
    {
        "secret": "example-secret-value",
        "tenantId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
        "token": "example-token-value"
    }


Flags:
      --body string             Tenant, API Key and Secret key
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for verify-api
      --profile string          name of the profile to be used for cmd (default "default")
      --userId string           ID of an User
      --verbose                 to show debug logs
```

## `chef-platform-auth-cli version`

```text
Chef Platform version

Usage:
  chef-platform-auth-cli version [flags]

Flags:
  -h, --help   help for version
```
