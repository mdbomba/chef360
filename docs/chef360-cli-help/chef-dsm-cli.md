# `chef-dsm-cli` Local Command Reference

> Generated by `scripts/content/capture-chef360-cli-help.sh` from the locally installed CLI. Regenerate after upgrading Chef 360 CLI packages.

## Captured Version

```text
Git SHA:         344d8dd1ee4e2f208cfdc0587e5a57833f08b7f8
Version:         v0.0.13
Build DateTime:  2026-06-04T07:59:11Z
```

## `chef-dsm-cli`

```text
Chef DSM CLI

Usage:
  chef-dsm-cli [command]

Available Commands:
  completion          Generate the autocompletion script for the specified shell
  deregister-device   De-register a device
  get-default-profile Get default profile for request
  help                Help about any command
  list-profile-names  Get the list of profile names available
  register-device     register a device and get api-token for device login
  set-default-profile Set default profile for request
  store               Commands related to DSM store service
  version             Chef DSM CLI

Flags:
  -h, --help   help for chef-dsm-cli

Use "chef-dsm-cli [command] --help" for more information about a command.
```

## `chef-dsm-cli deregister-device`

```text
Use this operation to de-register a device

Usage:
  chef-dsm-cli deregister-device [flags]

Flags:
      --force            force remove profile from local list of profiles, even on error from server
  -f, --format string    to print response in format (default "json")
  -h, --help             help for deregister-device
      --profile string   name for the profile of the new api-token (default "default")
  -v, --verbose          to show debug logs
```

## `chef-dsm-cli get-default-profile`

```text
get default profile for request

Usage:
  chef-dsm-cli get-default-profile [flags]

Flags:
  -h, --help   help for get-default-profile
```

## `chef-dsm-cli list-profile-names`

```text
Get the list of profile names available

Usage:
  chef-dsm-cli list-profile-names [flags]

Flags:
  -h, --help   help for list-profile-names
```

## `chef-dsm-cli register-device`

```text
register a device and get api-token for device login

Usage:
  chef-dsm-cli register-device [flags]

Flags:
      --cafile string         path to ca file
      --device-name string    name of the device (default "device")
  -f, --format string         to print response in format (default "json")
  -h, --help                  help for register-device
  -i, --insecure              insecure skip verify
      --overwrite             this flag is used to overwrite the old profile if the profile for profile name provided already exists
      --profile-name string   name for the profile of the new api-token
      --url string            tenant url
  -v, --verbose               to show debug logs
```

## `chef-dsm-cli set-default-profile`

```text
Set default profile for request

Usage:
  chef-dsm-cli set-default-profile [flags]

Flags:
  -h, --help   help for set-default-profile
```

## `chef-dsm-cli store`

```text
Commands related to DSM store service

Usage:
  chef-dsm-cli store [command]

Available Commands:
  client       Commands related to client
  cookbook     Commands related to cookbook
  databag      Commands related to databag
  environment  Commands related to environment
  node         Commands related to node
  policyfile   Commands related to policyfile
  policygroup  Commands related to policygroup
  role         Commands related to role
  search       Commands related to search
  user         Commands related to user

Flags:
  -h, --help   help for store

Use "chef-dsm-cli store [command] --help" for more information about a command.
```

## `chef-dsm-cli store client`

```text
Commands related to client

Usage:
  chef-dsm-cli store client [command]

Available Commands:
  add-key       Add a key to client
  create-client Create a new API client
  delete-client Delete an API client
  get-client    Get API client details
  get-key       Get client key details
  list-clients  List API clients
  list-keys     List client keys
  update-client Update an API client

Flags:
  -h, --help   help for client

Use "chef-dsm-cli store client [command] --help" for more information about a command.
```

## `chef-dsm-cli store client add-key`

```text
Add a new key to a Chef API client. Provide a public key to register.

Usage:
  chef-dsm-cli store client add-key [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --clientName string    The name of the client
      --format string        to print response in format (default "json")
  -h, --help                 help for add-key
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store client create-client`

```text
Create a new API client on the Chef Infra Server.

An API client is used for authenticating to the Chef Server.
A new RSA key pair will be generated and the private key
will be returned in the response (this is the only time the private key is available).

Note: Either name or clientName must be specified to create a client.

Usage:
  chef-dsm-cli store client create-client [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-client
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store client delete-client`

```text
Delete a specific API client from the Chef Infra Server.

Usage:
  chef-dsm-cli store client delete-client [flags]

Flags:
      --clientName string   The name of the API client to delete
      --format string       to print response in format (default "json")
  -h, --help                help for delete-client
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-dsm-cli store client get-client`

```text
Retrieve detailed information about a specific API client.

Usage:
  chef-dsm-cli store client get-client [flags]

Flags:
      --clientName string   The name of the API client to retrieve
      --format string       to print response in format (default "json")
  -h, --help                help for get-client
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-dsm-cli store client get-key`

```text
Retrieve complete details of a specific key for a Chef API client,
including the public key.

Usage:
  chef-dsm-cli store client get-key [flags]

Flags:
      --clientName string   The name of the client
      --format string       to print response in format (default "json")
  -h, --help                help for get-key
      --keyName string      The name of the key
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-dsm-cli store client list-clients`

```text
Retrieve a list of API clients from the Chef Infra Server.
Returns an array of client names.

Usage:
  chef-dsm-cli store client list-clients [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for list-clients
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --query string          Search query using Chef search syntax (default: '*:*' for all results). Supports wildcards, boolean operators (AND, OR, NOT), ranges, and fuzzy matching.
      --verbose               to show debug logs
```

## `chef-dsm-cli store client list-keys`

```text
Retrieve a list of all keys associated with a specific Chef API client.

Usage:
  chef-dsm-cli store client list-keys [flags]

Flags:
      --clientName string   The name of the client
      --format string       to print response in format (default "json")
  -h, --help                help for list-keys
      --profile string      name of the profile to be used for cmd (default "default")
      --verbose             to show debug logs
```

## `chef-dsm-cli store client update-client`

```text
Update an existing API client on the Chef Infra Server.

Can be used to rename a client or change its validator status.
Note: The admin field is deprecated and ignored as of Chef Server 12.1.0.

Usage:
  chef-dsm-cli store client update-client [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --clientName string    The current name of the API client to update
      --format string        to print response in format (default "json")
  -h, --help                 help for update-client
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store cookbook`

```text
Commands related to cookbook

Usage:
  chef-dsm-cli store cookbook [command]

Available Commands:
  get-cookbook   Get cookbook versions
  get-version    Get cookbook version details
  list-cookbooks List cookbooks

Flags:
  -h, --help   help for cookbook

Use "chef-dsm-cli store cookbook [command] --help" for more information about a command.
```

## `chef-dsm-cli store cookbook get-cookbook`

```text
Retrieve all available versions for a specific Chef cookbook from the Chef Infra Server.

Usage:
  chef-dsm-cli store cookbook get-cookbook [flags]

Flags:
      --cookbookName string   The name of the cookbook
      --format string         to print response in format (default "json")
  -h, --help                  help for get-cookbook
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-dsm-cli store cookbook get-version`

```text
Retrieve complete details for a specific version of a Chef cookbook from the Chef Infra Server.
This includes metadata, files, recipes, attributes, and dependencies.

Usage:
  chef-dsm-cli store cookbook get-version [flags]

Flags:
      --cookbookName string   The name of the cookbook
      --format string         to print response in format (default "json")
  -h, --help                  help for get-version
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
      --version string        The version of the cookbook
```

## `chef-dsm-cli store cookbook list-cookbooks`

```text
Retrieve a list of cookbooks from the Chef Infra Server for the authenticated organization.

This endpoint returns cookbook information including available versions. The num_versions query parameter
controls how many cookbook versions to include in the response.

The organization context is determined from the authenticated user's credentials.

Usage:
  chef-dsm-cli store cookbook list-cookbooks [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for list-cookbooks
      --num_versions string   The number of cookbook versions to include in the response.
                              - Use a number (e.g., 3) to return that many latest versions in descending order (newest to oldest)
                              - Use 'all' to return all cookbook versions
                              - If not specified, returns a single cookbook version (the latest)
                              - Using 0 returns an empty array for versions of each cookbook

      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-dsm-cli store databag`

```text
Commands related to databag

Usage:
  chef-dsm-cli store databag [command]

Available Commands:
  create-databag     Create a new data bag with multiple items
  create-databagitem Create a new data bag item
  delete-databag     Delete a data bag
  delete-databagitem Delete a data bag item
  get-databagitem    Get a data bag item
  list-databagitems  List data bag items
  list-databags      List data bags
  update-databagitem Update a data bag item

Flags:
  -h, --help   help for databag

Use "chef-dsm-cli store databag [command] --help" for more information about a command.
```

## `chef-dsm-cli store databag create-databag`

```text
Create a new Chef data bag with multiple items on the Chef Infra Server.

Usage:
  chef-dsm-cli store databag create-databag [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-databag
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store databag create-databagitem`

```text
Create a new item in a Chef data bag on the Chef Infra Server.

Usage:
  chef-dsm-cli store databag create-databagitem [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --dataBagName string   The unique name of the data bag (string identifier, not an ID)
      --format string        to print response in format (default "json")
  -h, --help                 help for create-databagitem
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store databag delete-databag`

```text
Delete a Chef data bag and all its items from the Chef Infra Server.

Usage:
  chef-dsm-cli store databag delete-databag [flags]

Flags:
      --dataBagName string   The unique name of the data bag (string identifier, not an ID)
      --format string        to print response in format (default "json")
  -h, --help                 help for delete-databag
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store databag delete-databagitem`

```text
Delete a specific item from a Chef data bag on the Chef Infra Server.

Usage:
  chef-dsm-cli store databag delete-databagitem [flags]

Flags:
      --dataBagName string   The unique name of the data bag (string identifier, not an ID)
      --format string        to print response in format (default "json")
  -h, --help                 help for delete-databagitem
      --itemName string      The unique name of the data bag item (string identifier, not an ID)
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store databag get-databagitem`

```text
Retrieve a specific item from a Chef data bag on the Chef Infra Server.

Usage:
  chef-dsm-cli store databag get-databagitem [flags]

Flags:
      --dataBagName string   The unique name of the data bag (string identifier, not an ID)
      --format string        to print response in format (default "json")
  -h, --help                 help for get-databagitem
      --itemName string      The unique name of the data bag item (string identifier, not an ID)
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store databag list-databagitems`

```text
Retrieve a paginated list of items in a Chef data bag from the Chef Infra Server.

Usage:
  chef-dsm-cli store databag list-databagitems [flags]

Flags:
      --dataBagName string    The unique name of the data bag (string identifier, not an ID)
      --format string         to print response in format (default "json")
  -h, --help                  help for list-databagitems
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-dsm-cli store databag list-databags`

```text
Retrieve a paginated list of Chef data bags from the Chef Infra Server.

Usage:
  chef-dsm-cli store databag list-databags [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for list-databags
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --verbose               to show debug logs
```

## `chef-dsm-cli store databag update-databagitem`

```text
Update an existing item in a Chef data bag on the Chef Infra Server.

Usage:
  chef-dsm-cli store databag update-databagitem [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --dataBagName string   The unique name of the data bag (string identifier, not an ID)
      --format string        to print response in format (default "json")
  -h, --help                 help for update-databagitem
      --itemName string      The unique name of the data bag item (string identifier, not an ID)
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store environment`

```text
Commands related to environment

Usage:
  chef-dsm-cli store environment [command]

Available Commands:
  create-environment Create a new environment
  delete-environment Delete an environment
  get-environment    Get environment details
  list-cookbooks     List cookbooks for an environment
  list-environments  List environments
  list-recipes       List recipes for an environment
  update-environment Update an environment

Flags:
  -h, --help   help for environment

Use "chef-dsm-cli store environment [command] --help" for more information about a command.
```

## `chef-dsm-cli store environment create-environment`

```text
Create a new environment on the Chef Infra Server.

An environment provides a way to map organizational process to infrastructure.
Every Chef Infra organization begins with a single environment called the _default environment,
which cannot be modified or deleted.

Usage:
  chef-dsm-cli store environment create-environment [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-environment
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store environment delete-environment`

```text
Delete a specific environment from the Chef Infra Server.
Note: The _default environment cannot be deleted.

Usage:
  chef-dsm-cli store environment delete-environment [flags]

Flags:
      --environmentName string   The name of the environment to delete
      --format string            to print response in format (default "json")
  -h, --help                     help for delete-environment
      --profile string           name of the profile to be used for cmd (default "default")
      --verbose                  to show debug logs
```

## `chef-dsm-cli store environment get-environment`

```text
Retrieve detailed information about a specific environment.

Usage:
  chef-dsm-cli store environment get-environment [flags]

Flags:
      --environmentName string   The name of the environment to retrieve
      --format string            to print response in format (default "json")
  -h, --help                     help for get-environment
      --profile string           name of the profile to be used for cmd (default "default")
      --verbose                  to show debug logs
```

## `chef-dsm-cli store environment list-cookbooks`

```text
Retrieve the list of cookbooks available to a specific environment with their version constraints.

Returns cookbook information filtered by the environment's cookbook version constraints.
The num_versions query parameter controls how many cookbook versions to include per cookbook.

This endpoint corresponds to the Chef Infra Server API: GET /environments/:name/cookbooks

Usage:
  chef-dsm-cli store environment list-cookbooks [flags]

Flags:
      --environmentName string   The name of the environment
      --format string            to print response in format (default "json")
  -h, --help                     help for list-cookbooks
      --num_versions string      The number of cookbook versions to include in the response for each cookbook.
                                 - Use a number (e.g., 3) to return that many latest versions in descending order (newest to oldest)
                                 - Use 'all' to return all cookbook versions
                                 - If not specified, returns a single cookbook version (the latest) for each cookbook

      --pagination.page int      what page of the pagination (default 1)
      --pagination.size int      items per page (default 10)
      --profile string           name of the profile to be used for cmd (default "default")
      --verbose                  to show debug logs
```

## `chef-dsm-cli store environment list-environments`

```text
Retrieve a list of environments from the Chef Infra Server.
Returns an array of environment names.

Usage:
  chef-dsm-cli store environment list-environments [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for list-environments
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --query string          Search query using Chef search syntax (default: '*:*' for all results). Supports wildcards, boolean operators (AND, OR, NOT), ranges, and fuzzy matching.
      --verbose               to show debug logs
```

## `chef-dsm-cli store environment list-recipes`

```text
Retrieve the list of recipes available to a specific environment.

Returns an array of recipe names that are available within the environment based on
the cookbooks that are available to the environment.

Recipe names are returned in the format "cookbook_name::recipe_name".

This endpoint corresponds to the Chef Infra Server API: GET /environments/:name/recipes

Usage:
  chef-dsm-cli store environment list-recipes [flags]

Flags:
      --environmentName string   The name of the environment
      --format string            to print response in format (default "json")
  -h, --help                     help for list-recipes
      --pagination.page int      what page of the pagination (default 1)
      --pagination.size int      items per page (default 10)
      --profile string           name of the profile to be used for cmd (default "default")
      --verbose                  to show debug logs
```

## `chef-dsm-cli store environment update-environment`

```text
Update an existing environment on the Chef Infra Server.

Used to modify environment attributes, description, or cookbook version constraints.

Usage:
  chef-dsm-cli store environment update-environment [flags]

Flags:
      --body string
      --body-file string         Path to file in the local system containing valid body parameter value
      --body-format string       Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --environmentName string   The name of the environment to update
      --format string            to print response in format (default "json")
  -h, --help                     help for update-environment
      --profile string           name of the profile to be used for cmd (default "default")
      --verbose                  to show debug logs
```

## `chef-dsm-cli store node`

```text
Commands related to node

Usage:
  chef-dsm-cli store node [command]

Available Commands:
  create-node  Create a new node
  delete-node  Delete a node
  get-node     Get a node
  get-runlists Get node run list
  head-node    Check if node exists
  list-nodes   List nodes
  update-node  Update a node

Flags:
  -h, --help   help for node

Use "chef-dsm-cli store node [command] --help" for more information about a command.
```

## `chef-dsm-cli store node create-node`

```text
Create a new Chef node on the Chef Infra Server.

Usage:
  chef-dsm-cli store node create-node [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-node
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store node delete-node`

```text
Delete a Chef node from the Chef Infra Server.

Usage:
  chef-dsm-cli store node delete-node [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-node
      --nodeName string   The name of the node
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-dsm-cli store node get-node`

```text
Retrieve details for a specific Chef node from the Chef Infra Server.

Usage:
  chef-dsm-cli store node get-node [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-node
      --nodeName string   The name of the node
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-dsm-cli store node get-runlists`

```text
Retrieve the run list for a specific Chef node.

This is a convenience endpoint that returns only the run list without the full node object.
The run list defines the ordered list of recipes and roles that should be applied to the node.

Usage:
  chef-dsm-cli store node get-runlists [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-runlists
      --nodeName string   The name of the node
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-dsm-cli store node head-node`

```text
Check whether a Chef node exists on the Chef Infra Server without retrieving its full data.

Usage:
  chef-dsm-cli store node head-node [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for head-node
      --nodeName string   The name of the node
      --profile string    name of the profile to be used for cmd (default "default")
      --verbose           to show debug logs
```

## `chef-dsm-cli store node list-nodes`

```text
Retrieve a paginated list of Chef nodes from the Chef Infra Server.

Usage:
  chef-dsm-cli store node list-nodes [flags]

Flags:
      --expand                Specifies whether to return full resource details or just resource names/identifiers.
                              - true: Returns full resource objects with all attributes
                              - false: Returns only resource names/identifiers (default)

                              This parameter is only applicable to list operations. When false, the API returns
                              a simple array of strings (resource names). When true, the API returns an array
                              of objects with full resource details.

      --format string         to print response in format (default "json")
  -h, --help                  help for list-nodes
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --query string          Search query using Chef search syntax (default: '*:*' for all results). Supports wildcards, boolean operators (AND, OR, NOT), ranges, and fuzzy matching.
      --verbose               to show debug logs
```

## `chef-dsm-cli store node update-node`

```text
Update an existing Chef node on the Chef Infra Server.

Usage:
  chef-dsm-cli store node update-node [flags]

Flags:
      --If-Match string      If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-node
      --nodeName string      The name of the node
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store policyfile`

```text
Commands related to policyfile

Usage:
  chef-dsm-cli store policyfile [command]

Available Commands:
  delete-policy-file       Delete a policyFile
  get-policy-file-revision Get policyFile revision details
  list-policy-files        List policyFiles
  list-policy-revisions    List policyFile revisions

Flags:
  -h, --help   help for policyfile

Use "chef-dsm-cli store policyfile [command] --help" for more information about a command.
```

## `chef-dsm-cli store policyfile delete-policy-file`

```text
Delete a Chef policyFile from the Chef Infra Server.

This endpoint removes all revisions of the specified policyFile from the server.

### Use Cases
- Removing obsolete policyFiles that are no longer used
- Cleaning up test or development policies
- Decommissioning policies that have been replaced

### Important Notes
- Deleting a policyFile removes all its revisions
- This action cannot be undone
- Ensure the policy is not currently applied to any policy groups before deletion
- Consider backing up the policyFile before deletion if needed for audit purposes

### Errors
#### 401 (Unauthorized)
Authentication required or invalid credentials

#### 403 (Forbidden)
Insufficient permissions to delete policyFiles

#### 404 (Not Found)
The specified policyFile does not exist

#### 412 (Precondition Failed)
The If-Match header does not match the current resource version

#### 500 (Internal Server Error)
An error occurred while deleting the policyFile

Usage:
  chef-dsm-cli store policyfile delete-policy-file [flags]

Flags:
      --If-Match string         If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string           to print response in format (default "json")
  -h, --help                    help for delete-policy-file
      --policyFileName string   The name of the policyFile to delete
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-dsm-cli store policyfile get-policy-file-revision`

```text
Retrieve detailed information for a specific policyFile revision from the Chef Infra Server.

This endpoint returns comprehensive details about a policyFile revision, including:
- Policy metadata (name, revision ID)
- Run list (ordered list of recipes and roles)
- Named run lists for different scenarios
- Included policy locks from other policies
- Cookbook locks with version information
- Solution dependencies with version constraints
- Attributes (default and override)

### Use Cases
- Viewing complete details of a specific policy revision
- Understanding cookbook dependencies for a policy
- Auditing policy configuration and locked cookbook versions
- Preparing to deploy or promote a specific policy revision

### Response Details
- The revisionId is a SHA256 hash uniquely identifying this policy version
- cookbookLocks contains all cookbooks with their locked versions and identifiers
- solutionDependencies shows the dependency tree with version constraints
- defaultAttributes and overrideAttributes are returned as flexible JSON objects (interface{})
- namedRunLists provides alternative run lists for different deployment scenarios

### Errors
#### 401 (Unauthorized)
Authentication required or invalid credentials

#### 403 (Forbidden)
Insufficient permissions to access the policyFile revision

#### 404 (Not Found)
The specified policyFile or revision does not exist

#### 500 (Internal Server Error)
An error occurred while retrieving the policyFile revision details

Usage:
  chef-dsm-cli store policyfile get-policy-file-revision [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for get-policy-file-revision
      --policyFileName string   The name of the policyFile
      --profile string          name of the profile to be used for cmd (default "default")
      --revisionId string       The revision ID (SHA256 hash) of the policyFile
      --verbose                 to show debug logs
```

## `chef-dsm-cli store policyfile list-policy-files`

```text
Retrieve a list of all policyFiles from the Chef Infra Server.

This endpoint returns policyFile information including the policy name and revision ID (SHA256 hash).

### Response Details
- The response includes all policyFiles in the organization
- Each policyFile is uniquely identified by the combination of name and revisionId
- Revision IDs are SHA256 hashes representing the specific version of the policy

### Errors
#### 401 (Unauthorized)
Authentication required or invalid credentials

#### 403 (Forbidden)
Insufficient permissions to access policyFiles

#### 404 (Not Found)
The organization does not exist or has no policyFiles

#### 500 (Internal Server Error)
An error occurred while retrieving policyFiles

Usage:
  chef-dsm-cli store policyfile list-policy-files [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-policy-files
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-dsm-cli store policyfile list-policy-revisions`

```text
Retrieve a list of all revisions for a specific policyFile from the Chef Infra Server.

This endpoint returns all available revision IDs (SHA256 hashes) for the specified policyFile.
Each revision represents a specific version of the policyFile that has been uploaded to the server.

### Response Details
- The response includes all revisions for the specified policyFile
- Revision IDs are SHA256 hashes representing unique versions of the policy
- Revisions are typically returned in chronological order (most recent first)

### Use Cases
- Viewing the version history of a policyFile
- Selecting a specific policy revision for deployment
- Auditing policyFile changes over time

### Errors
#### 401 (Unauthorized)
Authentication required or invalid credentials

#### 403 (Forbidden)
Insufficient permissions to access policyFile revisions

#### 404 (Not Found)
The specified policyFile does not exist or has no revisions

#### 500 (Internal Server Error)
An error occurred while retrieving policyFile revisions

Usage:
  chef-dsm-cli store policyfile list-policy-revisions [flags]

Flags:
      --format string           to print response in format (default "json")
  -h, --help                    help for list-policy-revisions
      --policyFileName string   The name of the policyFile
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-dsm-cli store policygroup`

```text
Commands related to policygroup

Usage:
  chef-dsm-cli store policygroup [command]

Available Commands:
  get-policy-group   Get a policy group
  list-policy-groups List policy groups

Flags:
  -h, --help   help for policygroup

Use "chef-dsm-cli store policygroup [command] --help" for more information about a command.
```

## `chef-dsm-cli store policygroup get-policy-group`

```text
Retrieve details for a specific policy group including all its policies from the Chef Infra Server.

Usage:
  chef-dsm-cli store policygroup get-policy-group [flags]

Flags:
      --format string            to print response in format (default "json")
  -h, --help                     help for get-policy-group
      --policyGroupName string   The name of the policy group
      --profile string           name of the profile to be used for cmd (default "default")
      --verbose                  to show debug logs
```

## `chef-dsm-cli store policygroup list-policy-groups`

```text
Retrieve a list of policies across different policy groups from the Chef Infra Server.

Usage:
  chef-dsm-cli store policygroup list-policy-groups [flags]

Flags:
      --format string    to print response in format (default "json")
  -h, --help             help for list-policy-groups
      --profile string   name of the profile to be used for cmd (default "default")
      --verbose          to show debug logs
```

## `chef-dsm-cli store role`

```text
Commands related to role

Usage:
  chef-dsm-cli store role [command]

Available Commands:
  create-role      Create a new role
  delete-role      Delete a role
  get-environments Get role environment-specific run lists
  get-role         Get a role
  get-runlist      Get role run list for specific environment
  get-runlists     Get role run list
  list-roles       List roles
  update-role      Update a role

Flags:
  -h, --help   help for role

Use "chef-dsm-cli store role [command] --help" for more information about a command.
```

## `chef-dsm-cli store role create-role`

```text
Create a new Chef role on the Chef Infra Server.

Usage:
  chef-dsm-cli store role create-role [flags]

Flags:
      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for create-role
      --profile string       name of the profile to be used for cmd (default "default")
      --verbose              to show debug logs
```

## `chef-dsm-cli store role delete-role`

```text
Delete a Chef role from the Chef Infra Server.

Usage:
  chef-dsm-cli store role delete-role [flags]

Flags:
      --If-Match string   If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --format string     to print response in format (default "json")
  -h, --help              help for delete-role
      --profile string    name of the profile to be used for cmd (default "default")
      --roleName string   The name of the role
      --verbose           to show debug logs
```

## `chef-dsm-cli store role get-environments`

```text
Retrieve environment-specific run lists configured for a Chef role.

Usage:
  chef-dsm-cli store role get-environments [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-environments
      --profile string    name of the profile to be used for cmd (default "default")
      --roleName string   The name of the role
      --verbose           to show debug logs
```

## `chef-dsm-cli store role get-role`

```text
Retrieve details for a specific Chef role from the Chef Infra Server.

Usage:
  chef-dsm-cli store role get-role [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-role
      --profile string    name of the profile to be used for cmd (default "default")
      --roleName string   The name of the role
      --verbose           to show debug logs
```

## `chef-dsm-cli store role get-runlist`

```text
Retrieve the run list for a Chef role in a specific environment.

Usage:
  chef-dsm-cli store role get-runlist [flags]

Flags:
      --environmentName string   The name of the environment
      --format string            to print response in format (default "json")
  -h, --help                     help for get-runlist
      --profile string           name of the profile to be used for cmd (default "default")
      --roleName string          The name of the role
      --verbose                  to show debug logs
```

## `chef-dsm-cli store role get-runlists`

```text
Retrieve the run list for a specific Chef role.

This is a convenience endpoint that returns only the run list without the full role object.
The run list defines the ordered list of recipes and roles that should be applied.

Usage:
  chef-dsm-cli store role get-runlists [flags]

Flags:
      --format string     to print response in format (default "json")
  -h, --help              help for get-runlists
      --profile string    name of the profile to be used for cmd (default "default")
      --roleName string   The name of the role
      --verbose           to show debug logs
```

## `chef-dsm-cli store role list-roles`

```text
Retrieve a paginated list of Chef roles from the Chef Infra Server.

Usage:
  chef-dsm-cli store role list-roles [flags]

Flags:
      --expand                Specifies whether to return full resource details or just resource names/identifiers.
                              - true: Returns full resource objects with all attributes
                              - false: Returns only resource names/identifiers (default)

                              This parameter is only applicable to list operations. When false, the API returns
                              a simple array of strings (resource names). When true, the API returns an array
                              of objects with full resource details.

      --format string         to print response in format (default "json")
  -h, --help                  help for list-roles
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --query string          Search query using Chef search syntax (default: '*:*' for all results). Supports wildcards, boolean operators (AND, OR, NOT), ranges, and fuzzy matching.
      --verbose               to show debug logs
```

## `chef-dsm-cli store role update-role`

```text
Update an existing Chef role on the Chef Infra Server.

Usage:
  chef-dsm-cli store role update-role [flags]

Flags:
      --If-Match string      If-Match will compare the supplied etag to the resource requested. If the etag does not match the operation will be aborted.

      --body string
      --body-file string     Path to file in the local system containing valid body parameter value
      --body-format string   Format of the --body or --body-file, options: json, yaml, toml (default "json")
      --format string        to print response in format (default "json")
  -h, --help                 help for update-role
      --profile string       name of the profile to be used for cmd (default "default")
      --roleName string      The name of the role
      --verbose              to show debug logs
```

## `chef-dsm-cli store search`

```text
Commands related to search

Usage:
  chef-dsm-cli store search [command]

Available Commands:
  search-index Search an index

Flags:
  -h, --help   help for search

Use "chef-dsm-cli store search [command] --help" for more information about a command.
```

## `chef-dsm-cli store search search-index`

```text
Perform a search query on a specific index on the Chef Infra Server.

Usage:
  chef-dsm-cli store search search-index [flags]

Flags:
      --format string         to print response in format (default "json")
  -h, --help                  help for search-index
      --index string          The name of the index to search (e.g., node, role, client, environment)
      --pagination.page int   what page of the pagination (default 1)
      --pagination.size int   items per page (default 10)
      --profile string        name of the profile to be used for cmd (default "default")
      --query string          Search query using Chef search syntax (default: '*:*' for all results). Supports wildcards, boolean operators (AND, OR, NOT), ranges, and fuzzy matching.
      --sortOrder string      Field to sort by with optional direction (ASC or DESC)
      --verbose               to show debug logs
```

## `chef-dsm-cli store user`

```text
Commands related to user

Usage:
  chef-dsm-cli store user [command]

Available Commands:
  create-key  Create DSM User Key

Flags:
  -h, --help   help for user

Use "chef-dsm-cli store user [command] --help" for more information about a command.
```

## `chef-dsm-cli store user create-key`

```text
### Summary:
Creates a new key for a DSM user with multitenancy support. This endpoint acts as a wrapper
around the core DSM service to securely generate user key on behalf of the DSM user.

#### Key Features:
- DSM generates a new private key and returns it base64 encoded
- Automatic tenant ID suffixing for multitenancy
- Secure key management without exposing sensitive credentials to browsers
- Integration with DSM /user/{dsmUserName}/keys endpoint

#### Behavior:
- User identification is done via email with tenant ID suffixing for proper multitenancy
- Key names are auto-generated as chef360-platform-#{yyyymmdd-hhmmss}

### Errors

#### 400 (Bad Request)
Invalid request payload or missing required fields

#### 401 (Unauthorized)
The credential used to access this resource is invalid/expired

#### 403 (Forbidden)
User does not have permission to create keys for the specified DSM user

#### 409 (Conflict)
Key with the same name already exists for the user

#### 422 (Unprocessable Content)
Invalid public key format or validation errors

#### 500 (Internal Server Error)
Unexpected error occurred while creating the key

Usage:
  chef-dsm-cli store user create-key [flags]

Examples:
- title: Default example
  example: |
    {
        "publicKey": ""
    }


Flags:
      --body string             Key creation request payload
      --body-file string        Path to file in the local system containing valid body parameter value
      --body-format string      Format of the --body or --body-file, options: json, yaml, toml (default "json")
  -e, --example                 to show example request body
  -f, --example-format string   Format of the --example, options: json, yaml, toml (default "json")
      --format string           to print response in format (default "json")
  -h, --help                    help for create-key
      --profile string          name of the profile to be used for cmd (default "default")
      --verbose                 to show debug logs
```

## `chef-dsm-cli version`

```text
Chef DSM CLI

Usage:
  chef-dsm-cli version [flags]

Flags:
  -h, --help   help for version
```
